interface:
  display_name: "Implementation Plan Parallel"
  short_description: "Create parallel-ready implementation plans with Target Files"
  default_prompt: "Use the implementation-plan-parallel workflow and follow its SKILL.md exactly."
