## 14:51 Claude skill model override commit review

무엇/왜
- Commit `67c6b99`에서 Claude Code skill group에 추가된 `--model` subagent override 문서 변경을 확인했다.

결과
- `Agent(...)`의 `model` 입력 자체는 Claude Code SDK 문서와 정합했다.
- 다만 여섯 개 skill frontmatter의 `argument-hint`가 YAML array로 들어가 있어, 공식 frontmatter reference의 scalar hint 설명과 어긋날 수 있는 리스크를 발견했다.
- `git diff --check 67c6b99^ 67c6b99`는 통과했다.

포인터
- Commit: `67c6b992feafc44b8b1f02c59da94e157ed48e5a`
- Files: `.claude/skills/{feature-draft,implementation-plan,implementation-review,implementation,plan-review,pr-review}/SKILL.md`

## 14:54 Codex skill model override feasibility check

무엇/왜
- Claude skill `--model` override와 유사한 기능을 Codex matching skill에 구현 가능한지 확인했다.

결과
- 현재 Codex `spawn_agent` tool schema는 `model`, `reasoning_effort`, `service_tier` override 필드를 지원한다.
- `gpt-5.5-medium` 같은 입력은 단일 model 값이 아니라 `model="gpt-5.5"` + `reasoning_effort="medium"`으로 파싱해야 한다.
- Codex manual은 custom agent 파일의 `model` / `model_reasoning_effort` 설정을 설명하지만, 런타임 호출 override는 현재 세션 tool schema의 `reasoning_effort` 필드를 사용해야 한다.

포인터
- Codex manual cache: `/var/folders/d9/1077c60x6ds1j1l8z9_75rdm0000gn/T/openai-docs-cache/codex-manual.md`
- Candidate files: `.codex/skills/{feature-draft,implementation-plan,implementation-review,implementation,plan-review,pr-review}/SKILL.md`

## 15:00 Codex skill model/effort override implementation

무엇/왜
- Codex matching skill 6개에 subagent `model` / `reasoning_effort` override 옵션을 추가했다.

결과
- `--model <gpt-5.5|gpt-5.4|gpt-5.4-mini>`와 `--effort <low|medium|high|xhigh>`를 canonical 분리형 옵션으로 문서화했다.
- `gpt-5.5-high` 같은 결합형 값은 거절하고 `--model gpt-5.5 --effort high`로 안내하도록 계약을 명시했다.
- 6개 `SKILL.md` frontmatter version을 각각 Claude matching 변경과 같은 minor version으로 bump했다.

포인터
- Files: `.codex/skills/{feature-draft,implementation-plan,implementation-review,implementation,plan-review,pr-review}/SKILL.md`

## 16:28 Spec sync and README update for subagent model override

무엇/왜
- Claude/Codex planning/implementation skill group의 subagent model override 구현을 global spec과 README에 동기화했다.

결과
- `_sdd/spec/main.md`를 v4.5.1 / 2026-07-01로 갱신하고 runtime-specific per-call override 규칙을 추가했다.
- `_sdd/spec/logs/changelog.md`와 spec sync report를 추가했다.
- `README.md`에 Claude/Codex별 사용 예시와 기본값 상속 규칙을 추가했다.

포인터
- Commits used as evidence: `67c6b99`, `5b40460`
- Report: `_sdd/spec/logs/spec_sync_report_2026-07-01_subagent_model_override.md`

## 19:58 drafts/work_log를 소비 repo 커밋 자산으로 승격

무엇/왜
- feature draft가 소비 repo의 구현 로그 역할을 한다는 관찰에 따라, `_sdd/drafts/`와 `_sdd/work_log/`를 로컬 전용 process artifact에서 커밋 자산으로 승격했다(형님 지시: drafts + work_log).

결과
- 부트스트랩 스킬 `SDD-WORKSPACE` 마커 블록 4곳(`{claude,codex}/skills/{spec-create,spec-upgrade}/SKILL.md`)에서 `_sdd/drafts/`·`_sdd/work_log/` 줄 제거 → process artifact 4종(`discussion/implementation/pipeline/pr`)으로 축소.
- SKILL 본문 산문(spec-create 3d / spec-upgrade Step 6)과 harness 템플릿 §2(4개 미러, md5 동일 유지)의 커밋셋 문구를 `spec/·guides/·env.md·drafts/·work_log/`로 갱신.
- 스펙 표면 동기화: `decision_log`에 2026-07-01 결정 entry 추가(2026-06-20 #1 supersede 표시), `changelog` v4.5.2 추가, `main.md`/`components.md`/`usage-guide.md` 현재상태 문구 갱신.
- census: 잔존 stale 없음. 과거 이력 entry(changelog:39, decision_log:77)와 무관한 6종(spec-sync 입력 소스), drafts/work_log 경로 참조(feature-draft 출력·§1/§5)는 정상 유지. 이 repo 자체 `.gitignore`는 메타 repo 예외라 무변경.

포인터
- Files: `{claude,codex}/skills/{spec-create,spec-upgrade}/{SKILL.md,references/agents-harness-template.md}`, `_sdd/spec/{decision_log,main,components,usage-guide,logs/changelog}.md`

## 20:05 승격 변경 implementation-review + fix

무엇/왜
- 위 승격 변경을 correctness ∥ simplicity 2-reviewer 병렬 리뷰. Critical/High 없음, Medium 2 + Low 3 relay. 발견 결함을 바로 반영.

결과
- C1(correctness Medium): `main.md` Spec Version 4.5.1→4.5.2 (changelog v4.5.2와 정합). 내가 changelog만 올리고 main bump를 누락했던 drift.
- S1(simplicity Medium): 커밋셋 산문 "…자산이라 커밋" 꼬리 제거 — spec-create 산문 2곳 + harness §2 4곳. 형제 surface(spec-upgrade/usage-guide) leaner 형과 density 일치. harness 4개 md5 재동일화(`5b018885…`).
- S2(Low): decision_log 신규 entry에서 "메타 예외"(#3)와 "구 #1 대체"(#4) 분리, changelog도 대체 관계를 별도 bullet로.
- S3(Low): usage-guide gitignore bullet을 "무엇을 ignore/commit" + "병합 규칙" 두 줄로 분할.
- census 재검증: harness 4 md5 동일, spec-create claude=codex 동일, `자산이라 커밋` 잔존 0.

포인터
- Reports: 인라인 반환(문서 리뷰라 파일 미생성)
- Files: `_sdd/spec/main.md`, `{claude,codex}/skills/spec-create/{SKILL.md,references/agents-harness-template.md}`, `{claude,codex}/skills/spec-upgrade/references/agents-harness-template.md`, `_sdd/spec/{decision_log,usage-guide,logs/changelog}.md`
