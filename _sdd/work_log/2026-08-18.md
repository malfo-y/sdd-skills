# 2026-08-18

## 1/11:05 custom agent 전면 폐지 — simplicity 계약 프롬프트 주입 전환 (fable-5)

- 무엇/왜: Codex 플러그인/Agent Plugins 1.0이 custom agent를 번들하지 못하는 배포 제약(웹 검증 완료 — 표준 포터블 = skills+MCP, hooks는 Codex 클라이언트 지원, agents만 불가) 때문에 마지막 custom agent(simplicity-review-agent)를 폐지. 계약을 implementation-review/references/simplicity-contract.md(미러 2벌)로 이동, 스킬 4벌이 전문 verbatim 주입 + claude general-purpose/codex explorer dispatch. agents 디렉토리 2개 삭제·marketplace 등록 해제·README/installer skills-only 전환.
- 결과: plan-review gate 1 (fable-5): CLEAR, L1 반영. impl-review gate 1 (fable-5, 첫 agentless 게이트 — 새 방식 자체로 수행): correctness L1 fix + simplicity L1 advisory, 두 dispatch 모두 계약 준수 반환. check 49/49, census 잔존 0(의도 4건), installer 변이 확인. spec v4.22.0.
- 포인터: PR 예정, decision_log 2026-08-18 entry, _processed_2026-08-18_feature_draft_agentless_simplicity_dispatch.md

## 2/11:20 spec-rewrite — main.md 다이어트 v4.23.0 (fable-5)

- 무엇/왜: /spec-rewrite 호출. main.md 80KB(거대 불릿 3~4KB급 7개)의 Canonical Fit 위반 진단 → 계획 저장·사용자 승인 후 압축. 결정·가드·재제안 금지 전량 보존, 폐지 구조 서사·수치는 decision_log 포인터화, §2↔§3 이중 서술 해소, v4.22.0 미반영 stale 표면 5곳 drift 수정.
- 결과: 61.7KB(-23%), 2500B+ 줄 0개, 링크 무결, 재제안 금지 6건 잔존 grep 확인. 목표 밴드(40±10KB) 미달은 의도적 중지(잔여=결정 본문).
- 포인터: logs/spec-rewrite-plan.md·logs/rewrite_report.md, decision_log·changelog v4.23.0 entry, PR 예정

## 3/11:30 spec-rewrite 2차 — 과감 압축 (fable-5)

- 무엇/왜: 사용자 지시("main.md는 현재 스펙만, 핵심 결정은 decision_log")로 1차(61.7KB) 압축을 강화 — 종결 관측 3건·full 레인/경량 경로/agent 폐지 연대기·supersede 서사 전면 삭제, 재제안 금지 가드는 1줄 존치.
- 결과: 최종 53KB(-34%), 링크 무결, 가드·핵심 원칙 문구 잔존 grep 확인. 제거 이력 목록은 decision_log entry가 정본.
- 포인터: PR #80 갱신

## 4/14:16 Codex 플러그인 전환 가능성 검토 (GPT-5)

- 무엇/왜: custom agent 폐지 후 남은 Codex 스킬 19개와 lifecycle hook 4개를 단일 설치 가능한 플러그인으로 패키징할 수 있는지 현재 repo 구조, built-in plugin-creator 계약, 공식 OpenAI plugin/hook 문서와 대조했다.
- 결과: 단일 `sdd-skills` 플러그인 패키징 가능. 최종 권장 layout은 `.agents/plugins/marketplace.json` + `plugins/sdd-skills/{.codex-plugin/plugin.json,skills/}`이며 현 `.codex/skills/` 19개를 복사 없이 이동한다. enabled+trusted plugin hook의 전역·중복 실행을 피하려고 **skills-only plugin + `spec-create`/`spec-upgrade`의 project-local hook 설치 유지**를 채택한다. `.claude/skills`, repo self-host `.claude/hooks`·`.codex/hooks.json`은 유지한다. 단, Codex가 `.agents/plugins/marketplace.json`과 legacy-compatible `.claude-plugin/marketplace.json`을 모두 읽으므로 새 marketplace top-level 이름은 기존 `sdd-skills`와 구분하고, 두 source 공존 시 listing/선택이 하나로 명확한지 acceptance에서 검증한다. 이동 시 active 경로 참조(README, installer, env/spec)도 함께 갱신한다.
- 포인터: `.codex/skills/`, `.codex/hooks.json`, `.claude/hooks/`, `.claude/skills/spec-create/references/hook-installation.md`

## 4/15:35 Codex 플러그인 레이아웃 공존 — plugins/sdd-skills-codex 이사 (fable-5)

- 무엇/왜: Codex 플러그인의 skills/ 고정 발견 위치와 클코 배포를 한 repo에 공존. .codex/skills 19개 → plugins/sdd-skills-codex/skills 이동(rename 54), Codex manifest 2개 신설(이름 sdd-skills-codex — 클코 legacy 읽기 충돌 회피, 사용자 지시), installer/README/spec 경로 갱신.
- 결과: plan-review gate 1: M1(root plugin.json 병설 → YAGNI 결정 기록) fix. impl-review gate 1: correctness 0(UNTESTED 1 — codex 스키마 실수용은 설치 실측 대기)·simplicity 2묶음 PASS. check 14/14. spec v4.24.0.
- 포인터: PR 예정, decision_log entry, _processed_2026-08-18_feature_draft_codex_plugin_layout.md

## 5/15:49 Codex 플러그인 레이아웃 검토 (GPT-5)

- 무엇/왜: `origin/main...HEAD`의 Codex skills-only 플러그인 이사를 Git 추적 상태, built-in `plugin-creator` validator, 이동 불변성, installer 발견 경로, 훅 격리, active 문서 참조 기준으로 검토했다.
- 결과: High 2·Medium 2. `.agents/plugins/marketplace.json`이 `.gitignore`에 걸려 커밋에서 누락됐고, `plugin.json`은 필수 `interface` 부재로 validator 실패. 로컬 marketplace entry의 policy/category 누락과 `_sdd/env.md` 구경로 2건도 남았다. 반면 54파일은 R100, SKILL.md 19개·installer 발견 19개, 구 skills 경로 소멸, sibling reference, JSON 문법, plugin hook 미포함, `git diff --check`는 통과했다.
- 포인터: `.gitignore`, `.agents/plugins/marketplace.json`, `plugins/sdd-skills-codex/.codex-plugin/plugin.json`, `_sdd/env.md`

## 6/15:54 Codex 플러그인 레이아웃 검토 finding 수정 (GPT-5)

- 무엇/왜: 검토에서 확인한 배포 blocker와 문서 drift 4건을 최소 수정했다. `.agents` ignore를 marketplace 파일 하나만 추적하도록 좁히고, repo marketplace entry와 plugin interface를 Codex 생성기 계약에 맞추며 env의 구경로를 새 canonical 경로로 교체했다.
- 결과: built-in plugin validator PASS, marketplace 생성기 계약 PASS, JSON 2개 PASS, installer discovery 19, hooks/layout 격리 PASS, `git diff --check` PASS. marketplace는 이제 `git status`에 신규 파일로 노출된다.
- 포인터: `.gitignore`, `.agents/plugins/marketplace.json`, `plugins/sdd-skills-codex/.codex-plugin/plugin.json`, `_sdd/env.md`

## 7/16:18 Codex 플러그인 설치 절차 확인 (GPT-5)

- 무엇/왜: repo-local marketplace에서 `sdd-skills-codex`를 최초 설치하는 현재 Codex CLI 0.147.0 명령과 기존 등록 상태를 확인했다.
- 결과: 현재 marketplace·plugin 모두 미등록. repo 루트를 `codex plugin marketplace add`로 등록한 뒤 `codex plugin add sdd-skills-codex@sdd-skills-codex`로 설치하는 2단계 절차를 확정했다. 새 task에서 로딩 확인이 필요하다.
- 포인터: `.agents/plugins/marketplace.json`, `plugins/sdd-skills-codex/.codex-plugin/plugin.json`

## 8/16:26 README Codex 기본 설치 경로 재검토 (GPT-5)

- 무엇/왜: 네이티브 Codex 플러그인 배포 구조가 완성된 뒤에도 README가 번들 복사 스크립트를 권장하는 우선순위 드리프트를 검토했다.
- 결과: GitHub marketplace 등록+plugin add를 기본 경로로 올리고, 번들 스크립트는 `codex plugin` 미지원 환경의 fallback으로 내리는 것을 권고했다. LobeHub 경로는 네이티브 경로와 중복이므로 기본 문서에서 제거하는 편이 적절하다. README 본문은 사용자 결정 전 미수정.
- 포인터: `README.md` Installation > Codex

## 9/16:28 README Codex 기본 설치를 GitHub 플러그인으로 전환 (GPT-5)

- 무엇/왜: 사용자 결정에 따라 Codex Installation의 권장 경로를 번들 복사 스크립트에서 GitHub repo marketplace 기반 네이티브 플러그인 설치로 전환했다.
- 결과: `marketplace add` + `plugin add` 2단계와 확인 명령을 Option A로 추가하고, skills-only라 설치만으로 훅이 걸리지 않는 경계를 명시했다. 번들 스크립트는 Option B fallback으로 유지하고 LobeHub 중복 경로는 제거했다. plugin validator·문서 grep·`git diff --check` PASS.
- 포인터: `README.md` Installation > Codex

## 5/16:00 Codex 스키마 보정 반영 (fable-5 검증, 수정은 Codex)

- 무엇/왜: Codex가 manifest 2개를 실제 스키마로 보정(marketplace source 객체화·policy·interface, plugin.json skills 경로·interface). 검증 중 발견: ① 구 .gitignore의 `.agents` 전체 무시로 marketplace.json이 PR #81 커밋에서 조용히 누락됐었음(Codex가 화이트리스트 패턴으로 해결 — 내 check가 파일 존재만 보고 추적 여부를 안 본 갭) ② census 범위에서 _sdd/env.md 누락(구경로 2건 — Codex 갱신).
- 결과: json·이름 일관성·경로 해석·check 14/14 재검증 통과, PR #81에 커밋.
- 포인터: PR #81 두 번째 커밋

## 10/17:37 Codex 플러그인 전환용 legacy bundle 제거 도구 (GPT-5)

- 무엇/왜: 네이티브 플러그인과 기존 `$CODEX_HOME/skills` 복사본의 중복 로딩을 막되 사용자 수정본 오삭제를 피하도록 legacy bundle cleanup 도구를 추가했다. manifest는 이름만 보유해 수정 여부를 판별할 수 없으므로 기본 preview, 명시적 `--yes`, timestamp backup 이동을 안전 경계로 채택했다.
- 결과: manifest 기반 skill/agent와 manifest 없는 19개 bundle 이름을 식별하고, 후보 외 개인 스킬·plugin cache·config를 보존한다. unittest 5/5, py_compile, 실제 환경 preview(19 skills+manifest, 삭제 미실행), plugin validator, `git diff --check` PASS. README 마이그레이션 명령을 추가하고 사용자 승인에 따른 spec-sync로 components code map을 갱신했다(main.md 무변경).
- 포인터: `tools/uninstall-codex-skill-bundle.py`, `tools/tests/test_uninstall_codex_skill_bundle.py`, `README.md`, `_sdd/spec/components.md`
