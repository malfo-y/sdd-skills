# Work Log — 2026-07-08

## 11:00 pr-review correctness를 dispatched agent로 추출

무엇/왜
- 형님 지적: `pr-review`가 simplicity는 agent에 위임하는데 correctness(코드 리뷰)는 메인 스킬 inline이라 `--model` override가 correctness에 안 걸리는 비대칭. `implementation-review`(두 렌즈 모두 agent)와도 구조가 어긋남.
- 결정(형님 confirm 2회): (1) `pr-review-agent` 신설로 방향 결정, (2) 리포트 형태는 대칭형 — correctness도 자기 리포트를 직접 쓰고 orchestrator는 verdict + 두 요약 + 두 detail 참조만.

결과
- **신규 agent**: `.claude/agents/pr-review-agent.md` + `.codex/agents/pr-review-agent.toml`. PR correctness review 계약 단일 소스(code-only 항상 + spec 존재 시 spec-based). read-only leaf, verdict 미판정(correctness 신호만 반환), 자기 리포트 `_sdd/pr/..._pr_correctness_<slug>.md`만 write. `simplicity-review-agent`와 표적 disjoint(정확성-중복 잔존/형태-중복 위임).
- **orchestrator 전환**: `pr-review` claude+codex SKILL을 2-reviewer orchestrator로 전환(v3.1.0→3.2.0). Step: branch check → PR 데이터/공유 slug → from-branch spec → 두 렌즈 병렬 dispatch(둘 다 `--model`/codex `--effort`) → verdict 합성 → 통합 리포트. 통합 Output Format을 Correctness/Simplicity 요약 섹션 + 두 detail 경로 참조로 재구성.
- **등록**: `pr-review-agent`를 `.claude-plugin/marketplace.json` agents 배열 + `.codex/agents/README.md`(Agent Set·Inline Writing) 양쪽에 추가([[plugin-agent-registration-gap]] 회피).
- **spec surface**: `main.md` guardrail sub-bullet(§74) + 결정 테이블 `직교 2-렌즈 review 렌즈` 행(§130)을 "correctness=`pr-review-agent` dispatch, model override 균일"로 갱신, Spec Version 4.5.2→4.5.3. `components.md` `pr-review` 행에 agent 경로 4개 + orchestrator 구조 반영. `decision_log` 2026-07-08 entry 추가. `changelog` v4.5.3 추가.

포인터
- Files: `.claude/agents/pr-review-agent.md`, `.codex/agents/pr-review-agent.toml`, `{.claude,.codex}/skills/pr-review/SKILL.md`, `.claude-plugin/marketplace.json`, `.codex/agents/README.md`, `_sdd/spec/{main,components,decision_log,logs/changelog}.md`
- Decision: `_sdd/spec/decision_log.md` 2026-07-08 entry

## 12:10 model override 배치 보정 (claude)

무엇/왜
- 형님 지적: claude pr-review SKILL이 model override를 상단 Hard Rules에만 두고 실제 dispatch(Step 3)엔 연결이 없어, dispatch 절차를 따라가면 model 적용이 빠진 것처럼 보임. 기준 예시 `.claude/skills/plan-review/SKILL.md:L14`는 dispatch 절차 바로 앞에 blockquote로 둠.

결과
- claude pr-review SKILL: Hard Rules의 `Subagent Model Override` bullet 제거 → Step 3(Parallel Dispatch)의 `Agent(...)` 스니펫 바로 앞 `> **Model override**:` blockquote로 이동(plan-review/implementation-review 패턴 정합, 중복 없음). AC6는 유지.
- codex는 무변경: model override가 `## Codex Runtime Adapter`에서 spawn 템플릿과 한자리 → codex implementation-review와 동일 패턴이라 dispatch 지점 연결 이미 존재.

포인터
- Files: `.claude/skills/pr-review/SKILL.md` (Hard Rules, Step 3)
