# 2026-07-13 Work Log

## 1/12:50 하네스 §3에 "단계 = 동명 SDD 스킬 호출" 규칙 추가

- **무엇/왜**: spec-create·spec-upgrade가 생성하는 AGENTS.md 하네스 §3이 SDD 단계 화살표를 phase 이름으로만 제시하고 "설치된 SDD 스킬 사용"이라는 추상 지시만 둬서, 소비 repo에서 모델이 feature-draft 등을 스킬 대신 자작하던 문제(사용자 관찰). 단계 이름=동명 스킬 호출 + 직접 재구현 금지 규칙을 명시하되, 카탈로그-비복사 원칙(스킬 rename/추가 시 stale 회피)은 유지. 강화 방식은 규칙 한 줄 vs 명시 매핑 테이블 중 규칙 한 줄 선택(사용자 결정).
- **결과**: 하네스 템플릿 4개 미러(claude·codex × spec-create·spec-upgrade references) §3에 규칙 한 줄 추가(byte-identical, md5 일치), 이 repo dogfooding AGENTS.md §3 동반 갱신. spec v4.5.4→4.5.5(main.md 버전·Last Updated, decision_log 2026-07-13 entry, changelog v4.5.5). 검증: `동명의 SDD 스킬` grep 5곳 존재, 4 미러 md5 동일.
- **포인터**: `_sdd/spec/decision_log.md` 2026-07-13 entry, `_sdd/spec/logs/changelog.md` v4.5.5

## 2/15:18 하네스 §3 화살표에서 implementation-plan 제거

- **무엇/왜**: spec planning precedence 결정(feature-draft 기본, implementation-plan은 phase/task 세분화 필요시 follow-up)이 하네스 §3 화살표에 반영 안 돼, 화살표가 여전히 `feature-draft → (spec-sync) → (implementation-plan) → implementation`로 implementation-plan을 순차 optional 단계처럼 나열 → 모델이 default planning으로 오인할 여지(사용자 관찰). 직전 커밋의 규칙 예시에도 포함돼 오해 강화.
- **결과**: §3 화살표에서 `(implementation-plan)` 제거, 괄호 optional 설명 `(spec-sync)`만, 규칙 예시에서도 제거 — 5곳(4 미러 byte-identical + AGENTS.md). implementation-plan 스킬 자체는 무변경(계층 분리: 하네스=기본 흐름, spec=조건부 상세). spec v4.5.5→4.5.6(main.md 버전, decision_log 2026-07-13 두 번째 entry, changelog v4.5.6). 검증: `implementation-plan` grep 하네스 5곳 0건, 4 미러 md5 일치, `git diff --check` clean.
- **포인터**: `_sdd/spec/decision_log.md` 2026-07-13 두 번째 entry, `_sdd/spec/logs/changelog.md` v4.5.6

## 3/19:52 task-ordering-agent 계약 리뷰

- **무엇/왜**: `feature-draft`의 flat task-set을 dependency·phase·Checkpoint·병렬 wave가 포함된 ordered plan으로 변환하는 `.codex/agents/task-ordering-agent.toml`을 producer(`feature-draft-agent`)와 consumer(`implementation`, `sdd-autopilot`) 계약에 대조해 review-only로 점검.
- **결과**: TOML 파싱과 Claude/Codex 본문 parity는 정상. 역할 경계와 producer/consumer 연결도 일관됨. 다만 입력 보존 범위(`Touchpoints`, optional `Non-Goals`, Overview/Scope) 누락 가능성, Validation Plan 부재 시 AC와 best-effort 처리의 충돌, dependency 방향·cycle·topological wave 규칙 미정의, heuristic phase 판단을 결정론적 파생으로 규정한 표현을 후속 수정 후보로 식별. agent 파일은 수정하지 않음.
- **포인터**: `.codex/agents/task-ordering-agent.toml`, `.codex/agents/feature-draft-agent.toml`, `.codex/skills/implementation/SKILL.md`, `.codex/skills/sdd-autopilot/references/orchestrator-contract.md`

## 4/20:08 task-ordering-agent 복잡성 유입 원인 분석

- **무엇/왜**: 사용자가 원한 최소 산출(`2026-07-09_feature_draft_task_ordering_late_binding.md` L294-L300의 phase/병렬/dependency 요약) 대비 현재 178줄 agent가 비대해진 원인을 discussion→feature draft→최초 구현 커밋→simplicity review 흐름으로 추적.
- **결과**: 점진적 팽창이 아니라 최초 커밋에서 178줄 전체가 생성됨을 확인. 삭제된 393줄 `implementation-plan-agent`의 dependency·phase strategy·6-field phase metadata·Checkpoint·full-plan artifact 계약을 “기존 서술 이동” 명목으로 보존 이관하면서, ordering overlay가 full implementation-plan 재생성기로 바뀐 것이 주원인. AC/Hard Rules/Process/Error Handling 반복을 house style로 인정한 simplicity review가 중복 제거를 막아 복잡성이 고착됨. 후속 단순화 방향은 입력 draft 비수정 + ordering summary overlay만 생성하는 얇은 agent로 환원.
- **포인터**: `_sdd/drafts/2026-07-09_feature_draft_task_ordering_late_binding.md` T2·L294-L300, `_sdd/discussion/2026-07-09_discussion_task_ordering_late_binding.md` D1-D4, `_sdd/implementation/2026-07-10_simplicity_review_task_ordering_late_binding.md`, commit `4d8c10a`

## 5/20:15 task-ordering 결과의 transient handoff 판단

- **무엇/왜**: task-ordering 결과를 별도 `_sdd/implementation/*_implementation_plan_*.md`로 저장해야 하는지 재검토.
- **결과**: 부모 orchestrator가 agent 완료를 기다린 뒤 즉시 소비하는 재계산 가능한 control data이므로 persistent artifact가 불필요하다고 판단. task-ordering-agent는 read-only로 원본 feature draft를 읽고 dependency·wave·single/multi-phase·checkpoint 요약을 최종 반환하며, orchestrator가 이를 직접 소비하는 transient handoff가 최소 계약. 실제 실행 결과의 영속 기록이 필요하면 기존 progress/report의 단일 작성자인 orchestrator가 기록하는 것이 적절함.
- **포인터**: `.codex/agents/task-ordering-agent.toml`, `_sdd/drafts/2026-07-09_feature_draft_task_ordering_late_binding.md` L294-L300

## 6/20:22 축소 task-ordering-agent 계약 초안

- **무엇/왜**: persistent implementation-plan artifact를 제거하고 부모 orchestrator에 ordering 결과를 직접 반환하는 최소 custom-agent 계약 초안을 설계.
- **결과**: agent 책임을 지정된 feature draft read → task dependency/parallel wave/phase/checkpoint 판단 → 고정 Markdown 응답 반환으로 제한. 파일 write·task 정의 전사·full plan schema·phase 6-field metadata·validation/risk 복사·review loop를 제거하고, 입력 부재/판정 불가만 `BLOCKED`로 반환하는 약 40줄 수준의 방향을 확정. 실제 agent/consumer 파일은 수정하지 않음.
- **포인터**: `.codex/agents/task-ordering-agent.toml`, `.codex/skills/implementation/SKILL.md`, `.codex/skills/sdd-autopilot/references/orchestrator-contract.md`

## 7/20:43 task-ordering transient handoff 구현

- **무엇/왜**: 승인된 최소 계약에 맞춰 `task-ordering-agent`를 full implementation-plan 생성기에서 read-only ordering overlay로 축소하고, 파일 artifact 없이 부모 orchestrator가 최종 응답을 직접 소비하도록 implementation·sdd-autopilot 계약을 동기화.
- **결과**: Codex agent 178→67줄, Claude agent 177→70줄로 축소. 응답을 `Status`·`Source`·`Mode`·`Execution`·`Dependencies`·`Checkpoints`·`Notes`로 제한하고 task 본문은 `Source` feature draft에서 읽도록 변경. `Execution`은 topological parallel wave, `Checkpoints`는 중간 review boundary만 담고 마지막 phase gate는 implicit으로 통일. implementation/autopilot pair, contract·reasoning reference·sample·validator와 global spec을 함께 갱신. 독립 리뷰에서 발견한 구 phase별 Checkpoint 규칙, final review의 phase/group 혼동, validator 누락도 수정.
- **포인터**: `.codex/agents/task-ordering-agent.toml`, `.claude/agents/task-ordering-agent.md`, `.codex/skills/implementation/SKILL.md`, `.codex/skills/sdd-autopilot/references/orchestrator-contract.md`, `_sdd/spec/main.md`
- **요약**: `git diff --check`, 양쪽 orchestrator sample validator, Python compile, Codex TOML parse, Claude/Codex agent 본문 parity, validator negative regression 검사 통과.
