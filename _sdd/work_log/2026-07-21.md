# 2026-07-21

## 1/14:56 agent 구조 다이어트 토론 — 방향 3건 확정
- **무엇/왜**: "요즘 모델은 큰 그림만 줘도 잘한다"는 문제의식에서 feature-draft 상세 유지·test-author 병합·task-ordering 제거를 검토. 판단 기준을 "재도출 불가능한 downstream 소비자가 있는가"로 정리.
- **결과**: ① feature-draft-agent 출력 다이어트 계속(절차적 how·자명한 근거 삭감, Contracts·AC/Validation·Target Files 유지 — 탐색 부산물이라 거의 공짜 + plan-review 반증 앵커). ② test-author-agent 존치 — 분리는 인센티브 구조(출제자·응시자 분리, CONTRACT_MISMATCH·RED 게이트가 load-bearing), 병합 기각. 대신 (c) test-free triage만 카테고리 기반 확대. ③ task-ordering-agent 존치 — 유지 비용 ~0 vs 제거 전파 리스크 실재.
- **포인터**: 후속 draft는 아래 2번 항목.

## 2/15:20 feature-draft: test-free triage 카테고리 확대 (draft + plan-review loop 완료)
- **무엇/왜**: RED 게이트 (c) test-free 자격을 카테고리 기반 2개 확대 — ① 전-2등급(정성 rubric) task는 (c)로 분류하고 rubric 판정을 checkpoint review reviewer에 위임(판정 주체 이동), ② proxy-check 함의 기준 명시화(check 통과가 AC 충족을 함의 못하면 동어반복과 동급). 난이도 기반 확대 금지·가드레일(근거 기록·회귀 스윕·리뷰 게이트 불면제) 유지.
- **결과**: draft 생성 → plan-review loop 2 iteration으로 exit(critical=high=medium=0). Iter 1: M1(T4 diff baseline 미정의)+L1~L3 → fix mode 전건 해소. Iter 2: CLEAR, 신규 L4(advisory — baseline 기록 의무 위치)만 잔존. 구현 착수 시 orchestrator가 baseline 기록을 선행 조치로 처리하면 소거.
- **포인터**: `_sdd/drafts/2026-07-21_feature_draft_test_free_triage_expansion.md` · `_sdd/implementation/2026-07-21_plan_review_test_free_triage_expansion.md`

## 3/15:55 feature-draft-lite 스킬 신설 (개밥 실험용)
- **무엇/왜**: 위 2번 draft가 30줄 산문 변경에 17분/300k 토큰을 소모한 실측(ceremony가 변경 규모가 아니라 하네스 규모에 비례)을 계기로, 단일 컨텍스트 규모 변경의 기본 경로로 경량 draft 스킬을 신설. 메인 루프 직접 작성(무dispatch·무review-loop), 구조 = Part 1(마커 유지: Change Summary·Scope) + Part 2(task당 의도·Contracts(있을 때만)·AC(복수, falsifiable)·Target Files) + Open Questions. lite가 덜어내는 건 계약의 집이 아니라 장부(coverage index·V* 매핑·census·loop). 승격 규칙 3: coverage 눈검산 불가 / 단일 컨텍스트 초과 / census형 sweep — 리트머스 "머리 하나에 다 안 담기는가". "새 contract 존재"를 승격 사유로 삼는 안과 "병렬 이득" 승격 사유는 토론에서 기각(계약은 task 인라인 가능, 병렬화는 실행단 판단).
- **결과**: `.claude/skills/feature-draft-lite/`(SKILL.md+skill.json) 생성, `.codex/skills/` 동일 사본 미러(dispatch 없어 적응 delta 불요 — goal-init 전례), marketplace.json skills 배열 등록(JSON 검증 OK). README는 subagent override 목록만 스킬을 열거하므로 해당 없음. 개밥 baseline: full 17분/300k — lite 목표 동급 변경 ~5분/50k + 구현 후 rework 빈도 관찰. 개밥 후 full/lite 라우팅(autopilot·full SKILL 안내문 추가) 여부 결정 예정.
- **포인터**: `.claude/skills/feature-draft-lite/SKILL.md` · 토론 기록은 본 파일 1번 항목
- **교정(15:50)**: 사용자 지적으로 self-contained 재작성 — full 어휘(coverage index·V* 매핑·census) negative 재천명("lite는 X를 만들지 않는다")을 삭제하고 외부 참조는 포인터+inline 목적으로만 유지. Required Output 템플릿이 산출물의 positive 단일 정의. 미러 재동기화(parity 확인).
- **교정(15:51)**: task 정의 명문화 — "단일 의도 + 자기 AC만으로 완료 판정이 닫히는 실행 단위" (Process 3).
- **교정(15:54, 사용자 리뷰 코멘트)**: ① 품질 게이트를 구현 후 implementation-review에서 **작성 후 plan-review 단일 패스**로 교체(loop 없음, finding은 메인 루프가 직접 반영). ② Integration에 `plan-review`·`implementation`(Part 2를 flat task-set 입력으로 소비) 추가. 미러 재동기화.

## 4/16:05 plan-review-agent에 Tier 2-lite 신설 (plan-review-lite 별도 쌍은 기각)
- **무엇/왜**: plan-review의 Verification Weakness/DRY 검사가 full 구조(coverage index·V* 1:1·Touchpoints)를 하드 체크해 lite draft에 가짜 finding을 낼 문제. 별도 plan-review-lite 쌍은 기각(리뷰 비용은 입력 크기 비례로 자연 감소, 게이트 품질 희석 금지, 쌍 증식=하네스 2배) — 기존 agent에 Tier 2-lite 분기 추가로 해결.
- **결과**: `plan-review-agent`에 Tier 2-lite 신설 — 식별(파일명 `*_feature_draft_lite_*` 또는 `# Feature Draft (Lite)` 제목), 형식 부재≠결함(full 전용 구조 검사 스코프 아웃, AC falsifiability·Target Files·task boundary·scope는 유지), **Lite 적격 검사 추가**(승격 규칙 위반 강행 시 High + full 승격 권고; 규칙 canonical은 lite SKILL 소유). Review Mode 목록·Integration 갱신. codex TOML 동일 지점 3-way 적용, Tier 섹션 parity diff OK. 호출자(lite SKILL) 변경 불요 — reviewer가 자가 식별.
- **포인터**: `.claude/agents/plan-review-agent.md` · `.codex/agents/plan-review-agent.toml` · lite 스킬은 본 파일 3번 항목
