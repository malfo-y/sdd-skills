# 2026-07-26

## 1/10:10 ralph-loop-init 리뷰 + 수정 feature-draft 작성
- **무엇/왜**: 사용자 요청으로 `ralph-loop-init`이 (a) ralph loop을 돌리기에 적합한지 (b) 지금도 쓸 만한지 리뷰. 양쪽 미러(.claude/.codex) SKILL.md 전문 + 인접 스킬 경계(`investigate`·`goal-init`) + 등록 census(marketplace.json, .codex/agents/README.md) + 2026-03-19 범용화 토론 기록을 대조. 이어서 findings 수정용 feature-draft 작성.
- **결과**: 리뷰 결론 — 설계(디스크 상태 + fresh context + action.sh 분리)와 Step 2 hard gate(기계 검증 가능한 성공조건 강제)는 여전히 유효하나, 런타임 안전장치 3건이 비어 "현 상태로 돌리기엔 조건부". P1 = ① iteration 상한·wall-clock 예산 부재(종료 압력이 전적으로 LLM 자기판단) ② DONE 감지가 action.sh 실행보다 앞서 DONE 전환 iteration의 action 유실(→ AC6가 init엔 통과, 런타임에 깨짐) ③ claude 미러의 claude/python3 preflight 부재(stream-json 필터가 python3 무조건 사용, 실패 메시지는 오진 유도). P2 = placeholder 검사가 리터럴 `<placeholder>`만 봐서 실효 없음 / `bash -n` 부재 + AC2·AC3 비-falsifiable / `decisions.md` 미생성·tail 지침 부재. P3 = state.md dead schema 3필드, codex→claude backport 3건, "언제 쓰지 말 것" 경계 부재. 현대성 판정 — 세션 수명 초과·무인 격리 실행에선 여전히 유일 경로, 단일 세션 내 반복은 `/loop`·백그라운드 태스크·`/goal`이 대체하므로 진입 조건이 좁아짐(경계 명시 필요). draft 10 task(적격 판정, 미러 census 검증 task 포함), plan-review 단일 패스 후 High 2(상한 의미론 미확정 → 누적 기준·phase 보존·exit 1로 확정 / placeholder 검사 자기참조·범위 이중 진술 → Part 1 단일 소스화) + Medium 2 + Low 4 반영.
- **포인터**: `_sdd/drafts/2026-07-26_feature_draft_ralph_loop_init_hardening.md` · 리뷰 대상 `.claude/skills/ralph-loop-init/SKILL.md`(621줄)·`.codex/.../SKILL.md`(579줄)
- **후속 결정(사용자)**: Task 1의 상한 축을 iteration에서 **wall-clock 주도**로 교체 — ralph는 iteration당 소요가 5분~6시간으로 편차가 커 iteration 수가 실행 시간의 대리지표가 못 된다. `MAX_RUNTIME_MINUTES`(기본 720=반나절, 실행 1회 기준·재실행 시 리셋, **soft** 판정으로 진행 중 action은 안 죽임) 주 상한 + `MAX_ITERATIONS`(기본 200, 누적 기준) 폭주 backstop 병행. backstop을 남긴 이유는 LLM이 매번 성공하면서 action.sh를 안 쓰는 무진전 공회전이 시간이 아니라 토큰을 태워 wall-clock으로 안 막히기 때문. 고정 변수 3개→4개.
## 2/10:55 ralph-loop-init hardening 구현 (10 task, 4.0.1→4.1.0)
- **무엇/왜**: 위 draft의 10개 task를 구현. Triage — Task 1·2·3·4·5·6·7·9는 (b) structural-check(SKILL.md에서 run.sh 코드블록을 추출해 `bash -n` + grep 판정 가능), Task 8은 (c) test-free(경계 문구는 존재 확인이 동어반복), Task 10은 census 자체가 check. 스크래치패드에 80개 항목 check harness 작성 → RED 14 PASS/64 FAIL 관찰 후 구현.
- **결과**: 80 PASS / 0 FAIL. 구조 check 외에 **기능 검증 3건**을 CLI 스텁(claude/codex 대체, API 호출 0)으로 실행 — ① iteration backstop: phase 보존·notes 갱신·exit 1 확인 ② wall-clock deadline: 올바른 상한 종류 메시지 + phase 보존 확인 ③ DONE 순서: DONE 전환 iteration의 action.sh가 실행되고 archive됨(양쪽 미러). ③은 `git show HEAD`의 변경 전 템플릿으로 동일 픽스처를 돌려 **유실 버그가 재현됨**을 확인해 RED→GREEN을 실증했다. 주요 구현: `MAX_RUNTIME_MINUTES`(720, per-run, soft) + `MAX_ITERATIONS`(200, 누적) 상한 분기, DONE 판정을 action 실행 뒤로 이동, claude 미러에 claude/python3 preflight + Hard Rule 6, placeholder 검사를 범위 명시 `grep -nE '<[^>]+>'`로 교체(PROMPT.md는 4개 섹션만 — Final Report/action.sh Rules의 `<...>`는 서식 지시라 제외), Step 8에 `bash -n`, AC2/AC3를 init 시점 판정 가능하게 재작성, `decisions.md` 6번째 산출물로 승격 + tail 읽기, state.md dead field 3개 제거, "언제 ralph가 아닌가" 경계.
- **포인터**: 4 files / +209 −56 · check harness는 스크래치패드(비영속, repo에 테스트 프레임워크 없음)
## 3/11:20 implementation-review 게이트 + fix 1회
- **무엇/왜**: 2-reviewer 병렬(correctness + simplicity) 단일 패스. Blocker 0(Critical/High 0), Medium 7 / Low 13. 규범대로 fix 1회로 C/H/M 반영.
- **계약 오류 선언**: Task 2 AC4의 "DONE 판정 grep 정확히 1회" 가정이 **틀렸다**. correctness M1 실측 — 판정을 본문 끝으로 *이동*만 하면 이미 `phase: DONE`인 state로 재실행할 때 변경 전(즉시 break)과 달리 무인 LLM 턴 1회 + 그 턴이 쓴 임의 action.sh 실행을 추가로 받는다. `--dangerously-skip-permissions` 루프에서 이번 변경 주제(무인 실행 경계 강화)와 역행. 진입 가드(exit 0)와 사후 판정은 역할이 다른 별개 지점이므로 **2곳이 맞다**. check와 draft AC4를 먼저 고치고 RED 재관찰(76/4) 후 구현.
- **결과**: 80 PASS / 0 FAIL, 기능 회귀 3건 재통과 + 신규 2건(DONE 재시작 가드: exit 0·iteration 불변·LLM 턴 0회 / zero-padded `08`: `value too great` 소멸). 반영 — correctness M1(진입 가드), M2(슬롯 검사 범위를 H1 포함 머리말+Known Errors로 교체, 초안 범위가 동기였던 `<project name>`을 놓치고 있었음), L1(notes 없으면 append), L3(`10#` 10진 강제), L6(codex 도입부에 `/goal` 명시 = Task 8 AC2 PARTIAL 해소), L7·L8(주석 보완); simplicity M1(술어 이중 평가 → if/elif), M2(헤더 주석은 사실만, 근거는 인라인 1곳), M3(Step 4 고정 변수 재열거 제거 → AC7 재해석), M5(preflight가 죽인 "python3 조정 허용" 지시 삭제, 허용 수정 1개로), L1(생성물 CHECKS 항목 자립 문구), L5(positive+negative 한 문장 병합).
- **미반영(보고)**: correctness L2(상한 도달 시 notes가 LLM의 마지막 note를 덮어씀 — 단일 필드 구조상 보존하면 형태가 복잡해져 보류), L4(20자리 정수가 가드를 통과해 DEADLINE 오버플로 — 현실 값 범위 밖), simplicity M4(스킬 AC3가 AC5+CHECKS에 포섭 — 삭제 시 AC 6개 재번호가 2미러+draft+check로 파급, 이득 대비 churn 과다), simplicity L2/L3/L4(soft 3회 언급·`LOOP_START` 1회용 변수·Security Notice↔Hard Rule 1 선행 중복).
- **포인터**: 4 files / +228 −64 · draft의 Task 2 Contracts·AC4, Task 1 AC7, Part 1 검사 범위 문단을 바뀐 계약으로 갱신

- **발견(별건)**: 이 repo엔 회귀 suite가 없어 마감 회귀 1회를 돌릴 대상이 없다. ralph run.sh 템플릿처럼 추출→`bash -n`→기능 스텁 검증이 가능한 자산이 늘고 있으므로, check harness를 repo에 영속화할지는 별도 판단 대상.

## 4/11:55 spec-sync (implemented, evidence 있음)
- **무엇/왜**: draft Part 1의 4개 contract를 global spec에 반영. 체인 종료.
- **결과**: `components.md` ralph 행을 v4.1.0 계약으로 재서술(6파일·고정 변수 4개·iteration 순서·슬롯 검사 단일 소스=Step 8·진입 경계·codex flag delta 보존). **기존 `5파일 생성` 카운트 drift가 실재했고 이번에 해소** — implementation-review correctness는 "components.md가 산출물을 열거하지 않아 drift 없음"으로 봤으나 실제로는 카운트가 박혀 있었다. `main.md`는 **본문 무변경**, 헤더만 4.6.7→4.6.8/2026-07-26 — 런타임 상한·판정 순서·검사 범위는 Repo-wide Invariant Test #1(한두 파일로 복구 불가)·#2(2개 이상 feature 공통) 둘 다 미통과라 guardrail이 아니라 component reference 소관이라는 판정. `decision_log.md`에 2026-07-26 entry 신규(rationale 6항: 상한 축 분리, soft 판정의 대가, phase 보존+exit 1, DONE 판정 2곳 계약 정정, 슬롯 검사 실효화, 경계를 guardrail 아닌 components에 둔 판단). changelog는 main.md 본문 무변경이라 미갱신(직전 v4.6.7 sync와 동일 처리). draft는 `_processed_` prefix로 마킹.
- **미반영 finding은 planned todo로 고정하지 않음**: 전부 단일 스킬 내부의 Low급 코드 형태라 transient risk log 소관이고, global spec에 올리면 thin core가 두꺼워진다. 기록은 이 work_log 항목 3에 남아 있다.
- **포인터**: `_sdd/spec/components.md:39` · `_sdd/spec/decision_log.md` 2026-07-26 entry · `_sdd/drafts/_processed_2026-07-26_feature_draft_ralph_loop_init_hardening.md`

- **발견(별건)**: 2026-03-19 토론 결정 #3/#5(autopilot에 "장시간·반복 디버깅이면 ralph 고려" 힌트 제공)가 미반영 — 현재 autopilot 어디에도 ralph 언급이 없어 ralph는 순수 수동 진입 스킬이다. 의도된 후퇴인지 누락인지 사용자 결정 대기, 이번 draft 범위 밖.

## 5/15:43 spec-sync (implemented, evidence 있음) — producer-owned 품질 게이트
- **무엇/왜**: draft `2026-07-26_feature_draft_producer_owned_review_gates.md` Part 1의 5개 contract를 global spec에 반영. 체인 종료.
- **결과**: 5개 delta 전부 IMPLEMENTED로 무표식 승격(evidence: structural check 79 PASS/0 FAIL, version lockstep 8필드 실측, 하네스 5곳 체인 리터럴 1건 + 예외 1줄 각 1건, `git diff --check` clean). `main.md` 4.6.8→4.6.9 — §2에 **게이트 소유권 불변식** 신규 guardrail(Repo-wide Invariant Test 3항 통과: 스킬 4개 표면 + 하네스 5곳에 흩어져 한두 파일로 복구 불가 / feature-draft·implementation·autopilot 3 workflow 공통 / 틀리면 이중 호출·게이트 누락), `implementation` 마감 순서 sub-bullet, autopilot bullet v4.0.0 3스킬 체인, 분할 canonical 목록에서 `plan-review` 제외, **선행 drift 2건 정정**(`plan-review` optional 서술 / 결정 테이블 `plan quality gate` 행 → `품질 게이트 소유권` 행). `components.md` 4행(autopilot·implementation·plan-review·implementation-review) 갱신, `usage-guide.md` Scenario 2·2b 갱신, `decision_log.md`·`logs/changelog.md` 신규 entry.
- **planned 고정 2건**(사용자 판단 요청분): `docs/en/SDD_WORKFLOW.md` ko 대비 한 세대 drift(§2 누락 + full 어휘 5곳) → 별도 feature 크기라 `현재 운영 제약`에 `🚧 Planned`로 고정. `implementation-review` version 4필드 불일치(`2.1.0`/`3.0.0` vs `7.0.0`) → 기존 "version metadata는 편집 discipline 의존" 제약의 실례라 그 하위 `🚧 Planned`로 고정.
- **발견(별건)**: `logs/changelog.md`에 v4.6.7·v4.6.8 entry가 없다(두 sync 모두 main.md 본문 무변경이라 의도적으로 건너뜀 — 3/4번 항목 참조). 결과적으로 changelog는 "main.md 본문 변경분"만 기록하는 로그가 됐는데, 파일 서두 정의("버전별 변경 기록")와 어긋난다. 정의를 고칠지 backfill할지는 별도 판단.
- **포인터**: `_sdd/spec/main.md` §2·§3 · `_sdd/spec/decision_log.md` 2026-07-26 producer-owned 게이트 entry · `_sdd/spec/logs/changelog.md` v4.6.9 · `_sdd/drafts/_processed_2026-07-26_feature_draft_producer_owned_review_gates.md`

## 6/16:57 spec-sync (implemented, evidence 있음) — `docs/en` 미러 세대 drift 해소
- **무엇/왜**: 항목 5가 `main.md` `현재 운영 제약`에 `🚧 Planned`로 고정한 en drift를 draft 없는 inline task로 처리한 뒤 그 planned 항목을 종결. 원인 census 결과 단일 — 2026-06-12 하네스 레이어(`AGENTS.md`) 도입이 en 미러에 전파되지 않아 `SDD_WORKFLOW` §2 Harness와 `SDD_CONCEPT` §1 Harness 행이 동시에 빠졌다.
- **planned 종결 판정**: bullet이 명시한 관찰 조건 두 개를 HEAD `c4aabef` 대조로 fresh 확인 — en `SDD_WORKFLOW` 섹션 9→10(ko와 동수), full 레인 어휘 HEAD 5곳→0곳. evidence: `check_en_docs.py` 35 PASS/0 FAIL(섹션·줄 수 parity, ko/en 짝 대칭, 변경 6파일 한정 포함), 회귀 `check_gates.py` 79 PASS/0 FAIL, `git diff --check` 무출력. → IMPLEMENTED 승격, planned bullet 소거.
- **결과**: `main.md` 4.6.9→4.6.10. planned bullet을 **ko/en 미러 대칭 마감 운영 제약 1줄**로 대체 — 새 guardrail은 만들지 않았다(§3 결정 테이블 `canonical rollout 순서`에 이미 `english mirrors` 단계가 있어, 같은 규칙을 guardrail로 재선언하면 판정 주체가 둘로 갈린다 → anti-duplication). 레이어 모델·temporary spec 구성·등가 표기는 canonical 문서(`docs/SDD_CONCEPT.md`·`docs/SDD_WORKFLOW.md`·`docs/SDD_SPEC_DEFINITION.md`)가 단일 소스라 §1~§3 본문 무변경. `components.md`·`usage-guide.md` 무변경(Strategic Code Map은 ko canonical 경로만 싣는다). `decision_log.md`·`logs/changelog.md` 신규 entry.
- **보류 finding 2건은 planned 고정하지 않음**(사용자 판단 요청분): simplicity M1(`SDD_WORKFLOW` §2 둘째 문단 ↔ `SDD_CONCEPT` §1 소유권 규칙 중복)·M2(§3 "출발점" ↔ §2 "하네스 먼저" 긴장)는 내용 정합이 유지된 supporting doc 산문 품질이라 Repo-wide Invariant Test #2·#3 미통과. Low 3건(README:14 "예시 포함"이나 예시 0건, README:16 "두 단계 구조"이나 5레이어, `docs/agentic_coding_principle.md` en 미러 부재 — README:18이 부분 커버리지를 이미 명시) 동일 판정. 전부 `decision_log` 2026-07-26 entry #5와 이 항목에만 남긴다.
- **정정(보고 정확도)**: "`touchpoints` live 표면 전역 0건"은 과대 진술이다 — `spec-rewrite`/`spec-upgrade` references, `spec-review` SKILL, `spec-sync-agent` 쌍, `SDD_SPEC_DEFINITION` ko·en에 **의도적으로** 남아 있다(구형 full draft 형식을 읽을 때의 legacy fallback 기준). 이번에 0이 된 범위는 `SDD_WORKFLOW`·`SDD_CONCEPT` ko·en이다.
- **포인터**: `_sdd/spec/main.md` `현재 운영 제약` · `_sdd/spec/decision_log.md` 2026-07-26 `docs/en` 미러 entry · `_sdd/spec/logs/changelog.md` v4.6.10
