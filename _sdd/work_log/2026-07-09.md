# 2026-07-09

## 1/19:15 feature-draft 속도 개선 — 검증 단일 패스 + 리뷰 리포트 다이어트

- **무엇/왜**: feature-draft 파이프라인 속도 리뷰에서 채택한 2개 레버 적용 (병목=작성 실측과 일관). ① `feature-draft-agent`의 말미 검증 5중 패스(Step 6 HR12 self-check·계획 품질 self-check·HR11 Pass 1+2 → Step 8 12항목 checklist → Final Check)를 **Step 8 단일 검증 패스**(AC 목록 기준 1회)로 통합 — 유실 방지로 AC에 "Part 1↔Part 2 같은 delta 범위(HR10)" 항목 추가, 교정 가이드([C]→[M], task 분리, shared setup 추출, description 축소)는 Step 8에 보존. ② `plan-review-agent` 리포트 §4 Plan Surface Summary(plan 재진술)·§5 Recommendations(finding의 Recommended Plan Change 재진술)를 참조/특이점만 남기는 다이어트 — loop 안에서 두 섹션의 소비자 없음(fix mode는 findings만, orchestrator는 Blocker Status만 소비), re-review 회차마다 절감 곱해짐.
- **결과**: 4파일 수정(.claude/.codex 미러 짝), 순감 −41줄. `git diff --check` clean, codex TOML 파싱 OK. exit 조건 medium 제외(레버 1)는 사용자 기각으로 미적용.
- **포인터**: 브랜치 `perf/feature-draft-single-pass-and-review-diet`, 메모리 `sdd-orchestration-speed`(출력 다이어트 C+A의 연장). 후속 후보: `implementation-plan-agent`도 동일 다중 패스 패턴 보유(이번 범위 밖).
