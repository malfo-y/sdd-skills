# 2026-07-09

## 1/19:15 feature-draft 속도 개선 — 검증 단일 패스 + 리뷰 리포트 다이어트

- **무엇/왜**: feature-draft 파이프라인 속도 리뷰에서 채택한 2개 레버 적용 (병목=작성 실측과 일관). ① `feature-draft-agent`의 말미 검증 5중 패스(Step 6 HR12 self-check·계획 품질 self-check·HR11 Pass 1+2 → Step 8 12항목 checklist → Final Check)를 **Step 8 단일 검증 패스**(AC 목록 기준 1회)로 통합 — 유실 방지로 AC에 "Part 1↔Part 2 같은 delta 범위(HR10)" 항목 추가, 교정 가이드([C]→[M], task 분리, shared setup 추출, description 축소)는 Step 8에 보존. ② `plan-review-agent` 리포트 §4 Plan Surface Summary(plan 재진술)·§5 Recommendations(finding의 Recommended Plan Change 재진술)를 참조/특이점만 남기는 다이어트 — loop 안에서 두 섹션의 소비자 없음(fix mode는 findings만, orchestrator는 Blocker Status만 소비), re-review 회차마다 절감 곱해짐.
- **결과**: 4파일 수정(.claude/.codex 미러 짝), 순감 −41줄. `git diff --check` clean, codex TOML 파싱 OK. exit 조건 medium 제외(레버 1)는 사용자 기각으로 미적용.
- **포인터**: 브랜치 `perf/feature-draft-single-pass-and-review-diet`, 메모리 `sdd-orchestration-speed`(출력 다이어트 C+A의 연장). 후속 후보: `implementation-plan-agent`도 동일 다중 패스 패턴 보유(이번 범위 밖).

## 2/19:28 implementation-plan-agent 검증 단일 패스 통합 (feature-draft 패턴 확장)

- **무엇/왜**: feature-draft-agent에 적용한 단일 검증 패스 통합(항목 1/19:15)을 implementation-plan-agent에도 확장. Step 4 말미 2중 self-check(AC/HR12 + 계획 품질)·Step 7 저장 전 9항목 점검·codex Self-Containment Check·Final Check 재검증을 **Step 7 단일 AC 패스**로 통합, 교정 가이드는 Step 7에 보존. 그 직전에 [C]-근거없음 교정 문구도 4곳 교정(마커 교체로 오독되던 "[M]으로 바꾸거나" → 항목 교체로 명확화, d015f94).
- **결과**: 2파일 수정(.md/.toml 미러), 순감 −36줄. TOML 파싱 OK, 옛 문구 잔존 0건.
- **포인터**: 같은 브랜치 `perf/feature-draft-single-pass-and-review-diet`.

## 3/20:30 discussion — task ordering을 feature-draft에서 분리해 late-binding

- **무엇/왜**: implementation-plan이 안 쓰이는 문제에서 출발한 구조화 토론(7라운드)을 discussion 관례대로 정리. 합의: feature-draft Part 2의 task 정의는 유지하되 **task ordering(dependency 계산+순서+phase 분할+multi/single 판정)만 분리**해 신규 `task-ordering-agent`가 구현 직전 전체 조망으로 late-binding(코드베이스 실측 없음, review loop 없음, 항상 경유). `implementation-plan` skill+agent는 하위호환 deprecated feature-draft mirror로 은퇴, 새 역할엔 새 이름(옵션 B, legibility). plan-review-agent는 task 정의 리뷰어로 유지하되 dependency/parallelism 항목 제거. near-duplicate producer smell 자동 해소.
- **결과**: `_sdd/discussion/2026-07-09_discussion_task_ordering_late_binding.md` 생성. 결정 D1~D8, 기각 대안 3건, 미결 Q1(전체 조망만으로 dependency 복원 가능한지 — needs-data)·Q2(진행 방식)·Q3(deprecation 시점).
- **포인터**: 후속 핸드오프 섹션에 구현 목표/변경 금지 제약/검증(agent 등록 갭·rename census·§섹션 propagation·미러 parity·spec surface)/중단 조건 명시. 구현 미착수(진행 방식 Q2 미결).

## 4/22:40 feature-draft 도그푸딩 — task ordering late-binding 구현 계획 생성

- **무엇/왜**: discussion(항목 3) 합의를 도그푸딩(Q2 결정)으로 feature-draft 파이프라인에 태워 구현 계획 draft 생성. 내가 orchestrator(digest 정리 → `feature-draft-agent` 생성 → `plan-review-agent` review-fix loop → relay) 수행. draft = Part 1 spec delta + Part 2 task 정의(T1 feature-draft flat화, T2 신규 task-ordering-agent, T3 plan-review trim, T4 implementation 배선, T5 autopilot 재배선, T6 implementation-plan deprecate, T7 플러그인 등록), C1~C7/I1~I5/V1~V8 ID 연결, claude↔codex 미러 짝을 task 내부에 묶음.
- **결과**: `_sdd/drafts/2026-07-09_feature_draft_task_ordering_late_binding.md` + 리뷰 리포트 `_sdd/implementation/2026-07-09_plan_review_task_ordering_late_binding.md`. review-fix loop 3 iteration: ①BLOCKED(High3: 이중 배선·contract 표면 과소·plan-review 구 precedence + Med2) → fix(소유권 2분기 확정, phase 3→4 재편) → ②CLEAR(신규 Med1: Part1 문구 잔존) → fix → ③CLEAR(0/0/0/0). 최종 Blocker Status CLEAR, 잔존 advisory 0.
- **포인터**: High #1 해결 = task-ordering 소유권을 "상위 orchestrator 유무"로 2분기(autopilot이 step 소유+Phase Source hand-off / implementation은 Phase Source 없을 때만 self-dispatch, ordering 정확히 1회). 다음: 이 draft를 implementation에 태워 실제 구현(형님 지시 대기). 브랜치 아직 `perf/feature-draft-single-pass-and-review-diet`, 커밋 미실행.

## 5/09:55 사용자 draft 리뷰 반영 개정 (2 지적 + census 갭)

- **무엇/왜**: 형님이 draft 직접 리뷰 후 2건 지적. ① implementation-plan **agent**는 caller가 없어(skill은 feature-draft-agent 위임, autopilot은 task-ordering-agent 재배선) deprecated mirror 불필요 → **agent 삭제**(skill만 하위호환 트리거 위해 mirror 유지). ② autopilot은 implementation **SKILL을 호출 안 함**(implementation-dispatch-controller로 leaf 직접 dispatch) → implementation SKILL은 사용자 직접 실행 전용, "Phase Source 소비 분기"(High #1 fix) 과잉 → **항상 self-dispatch 단일 경로로 롤백/단순화**. High #1의 "같은 SKILL 양쪽 사용 이중실행" 전제가 틀렸음(형님 교정). grep census로 둘 다 검증(implementation-plan-agent caller=skill+autopilot 둘뿐; autopilot=dispatch-controller). 앞서 형님이 짚은 T1 phase 산출 제거 갭(`## Implementation Phases`)도 반영. census 미포착 표면 2개(sample-orchestrator.md·validate_orchestrator.py) T5로 흡수.
- **결과**: fix mode 2회 + re-review 2회(Iteration 4→5). C3/C4/C6/C7·T1/T4/T5/T6/T7·V4/V5/V6/V7/V8 개정, T5 6→10파일. Medium #8(삭제 census carve-out이 agent-유지 시절 잔재 → agent-token 전역 0 census로 정정) 해소. 최종 Blocker Status **CLEAR (0/0/0/0)**.
- **포인터**: draft·리포트 동일 경로. loop 누적 5 iteration. 다음: 구현 착수 or 커밋(형님 지시 대기).

## 6/10:45 task ordering late-binding 구현 (draft 7 task 실행, test-first 생략)

- **무엇/왜**: draft `2026-07-09_feature_draft_task_ordering_late_binding.md`의 7 task를 브랜치 `feat/task-ordering-late-binding`에서 구현(문서 repo라 test-first 생략, draft Validation Plan V1~V8 grep/census로 검증). **T1** feature-draft-agent(.md/.toml) Part 2에서 ordering 산출(Implementation Phases·Parallel Execution Summary·Dependencies·의미충돌 5패턴·relay 안내) 제거 → flat task-set. **T2** 신규 `task-ordering-agent`(.md/.toml) — flat task-set만 입력, 코드 실측 없이 dependency·phase·Checkpoint 파생, review loop 없이 self-check, 항상 경유, 복원 불가 시 BLOCKED. **T6** implementation-plan-agent 2파일 삭제 + skill 2파일 deprecated feature-draft mirror(트리거 보존). **T4** implementation SKILL(.md 양측) Step 1 → ordered task-set 확보 + 항상 task-ordering self-dispatch(사용자 직접 실행 전용, Phase Source 분기 없음). **T5** autopilot SKILL·orchestrator-contract·sdd-reasoning-reference·sample-orchestrator·validate_orchestrator(claude+codex 10파일) 재배선 — precedence 폐기, implementation-plan output→task-ordering output(§2·§4·§5·§6·§8), task-ordering gate 면제 명시, Example A에도 task-ordering step 추가. **T7** marketplace.json + codex README(Agent Set·Inline Writing) 등록 교체. **T3** plan-review-agent(.md/.toml) dependency/parallelism 추출·Task Boundary Drift 충돌 문구·Orchestrator rubric 구 precedence 제거(task 정의 리뷰 유지). T5 반복 재지정 + Example A는 subagent 3개 병렬 위임(파일 짝별), 나머지 직접 편집.
- **결과**: 21 코드파일 변경(+307/−373) + 신규 task-ordering-agent 2 + discussion/draft. V1~V8 전부 통과: implementation-plan-agent 토큰 repo 전역 0, 구 계약("multi-phase⇒implementation-plan 필수"·"Phase Source=implementation-plan output"·standalone) 0, 미러 짝 완비, `git diff --check` clean, codex toml/json/python 파싱 OK. codex는 개념/output/step underscore(`task_ordering`)·agent_type kebab 관례 정합.
- **포인터**: 브랜치 `feat/task-ordering-late-binding`(discussion/draft/work_log 딸려옴). 커밋 미실행(형님 지시 대기). 후속: 실제 파이프라인 e2e 검증은 Q1(전체 조망만으로 dependency 복원) 실측 후속 과제.

## 7/10:48 implementation-review (2-reviewer 병렬) + Medium fix

- **무엇/왜**: 구현분을 correctness(implementation-review-agent) ∥ simplicity(simplicity-review-agent) 병렬 리뷰. 문서 repo라 grep/census/미러 parity를 acceptance로 graceful degrade. 결과 **Blocker 없음(READY)**, Critical/High 0. Medium 2건은 내가 놓친 실제 결함이라 fix: **correctness M1** — sample-orchestrator(claude+codex) feature-draft 프롬프트가 Example A/B 양쪽에서 "Part 2에 dependency 산출/implementation이 직접 읽는" 구 소비 모델 지시 잔존 → flat task-set 계약(C1) 모순, seed template이라 비정합 orchestrator 파생 소지 → dependency 산출 지시 제거·task-ordering 파생으로 교정(6곳). **simplicity M1** — 의미충돌 5패턴 3번째 어휘가 canonical(task-ordering-agent "같은 config/env") vs implementation SKILL "동일 config" vs feature-draft "config 공유"로 3갈래 drift → "동일 config/env"로 통일(4곳).
- **결과**: 두 리포트 `_sdd/implementation/2026-07-10_implementation_review_*` / `_..._simplicity_review_*`. fix 10곳(미러 짝), 재검증 통과(구 지시 0·config 공유 0·git diff --check clean). Low 4건(Glob tools·codex README Inline Writing·도달불가 방어분기·phase 6필드 이중)은 reviewer 판단상 조치 불요(advisory).
- **포인터**: 구현+리뷰+fix 완결. 커밋 미실행(형님 지시 대기).

## 8/12:35 방어적 사족 정리 (사용자 지적 — legibility)

- **무엇/왜**: 형님이 feature-draft-agent의 "Part 2는 dependency·phase 산출하지 않는다 — task-ordering 소관 …" 문장을 두고 "방어적 사족 아니냐"고 지적. 전 변경 파일 census 결과 두 지대 확인: (1) 의미충돌 5패턴이 canonical(task-ordering-agent) 외 feature-draft·implementation SKILL에 복제(drift 소스 — simplicity M1의 근본 원인), (2) "dependency·phase 산출 안 함/task-ordering 소관"이 feature-draft 5곳 반복(L12 개요·L136·L159·L226·L315). 개요 1회 정의로 충분하고 나머지는 negative 재천명이라 걷어냄.
- **결과**: feature-draft-agent(claude+codex) 4곳 정리(L136/L159 괄호 축소·L226 5패턴+재천명 bullet 제거·L315 뒷문장 제거, L12 경계 정의 유지) + implementation SKILL(claude+codex) 5패턴 요약 제거. canonical 5패턴은 `task-ordering-agent` 1곳만(5개 온전), "산출하지 않는다" 4→1곳. 이전 config/env 통일(M1 fix)은 5패턴 복제 제거로 자동 해소. git diff --check clean, codex toml OK, 미러 대칭.
- **포인터**: 유지보수 결합 해소(5패턴 변경 시 1파일만). 커밋 미실행.

## 9/13:34 구현 후속 정리 (사용자 리뷰 반영, 구현 커밋에 amend)

- **무엇/왜**: 커밋 후 사용자 리뷰로 4건 반영. ① L12 개요를 flat task-set 용어 정의 1줄로 축소(negative·중복 포인터·late-binding 제거). ② `implementation-plan` skill을 축약 mirror → **feature-draft full mirror**(사용자가 claude 직접 편집)로 전환, codex 짝도 동기화(codex feature-draft 복제 + DEPRECATED, spawn_agent 규약), 양 feature-draft Role Pointer 꼬리("승격됨") 제거 + newline 복원. ③ **wave 파생을 task-ordering으로 이동**: task-ordering이 각 phase 내 wave(병렬 그룹)를 선언(Target Files) 기반 greedy 확정 파생(priority 정렬·크기 5 제한), implementation Step 3.2는 "그룹 파생 규칙"→"wave 소비(+구식 plan fallback)". orchestrator의 file-disjoint **실측** 가드레일·순차 강등은 유지(task-ordering은 코드 실측 안 함 → 실행 직전 실측 최종권은 orchestrator).
- **핵심 판단(설계 논의)**: task-ordering 존재 이유 재확인 — (a) dependency(의미적 순서: model→import 등 파일 비겹침)는 file-disjoint 실측으로 못 잡음 → 전체 조망 추론 필요, (b) autopilot이 implementation SKILL 미경유라 dependency/phase 계산을 공유 agent로 빼야 함. task-ordering에 코드 실측을 넣자는 안은 기각 — file-disjoint는 wave마다 파일 상태가 바뀌어 orchestrator가 매 dispatch 직전 상시 실측해야 하므로, task-ordering 실측은 그 검사를 대체 못 하고 중복만 추가(Q1 "전체 조망이 핵심" 결정과 정합).
- **결과**: feature-draft/implementation-plan/task-ordering/implementation/**sdd-autopilot**(SKILL·orchestrator-contract·sample-orchestrator; claude+codex) 편집, git diff --check clean, codex toml OK, 미러 대칭. 구현 커밋 amend.
- **포인터**: 관련 메모리 [[defensive-boilerplate-removal]]. **wave 파생=task-ordering(선언) / wave 실측 검증=orchestrator(실행)** 역할 분담이 핵심 계약 — orchestrator는 implementation(수동)·autopilot(dispatch-controller) 양쪽 모두 task-ordering wave를 소비하고 fan-out 직전 file-disjoint 실측으로 검증한다(autopilot도 wave 파생 서술을 소비로 정합).
