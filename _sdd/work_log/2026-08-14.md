# 2026-08-14

## 10:04 Codex 설치 원격 한 줄을 README 기본 경로로 (Opus 5)

- **무엇/왜**: "codex 스킬 설치를 uvx로 한 번에 못 하나?"라는 질문에서 출발. 조사 결과 **uvx는 불필요** — `tools/install-codex-skill-bundle.py`는 서드파티 의존성 0(표준 라이브러리만)이고 `__file__`·로컬 경로를 참조하지 않으며 항상 GitHub zipball을 받아 설치하므로, clone 없이 원격 실행해도 정상 동작한다. uvx를 쓰려면 pyproject + `[project.scripts]` 패키징이 필요한데 격리 venv의 이점이 없고(의존성 0) 버전 핀은 이미 `--ref`가 제공한다 — 순비용이라 기각.
- **결과**: README Option A를 원격 한 줄 우선으로 재구성 — `curl -fsSL <raw URL> | python3 -` 기본, `uv run <raw URL>` 대안, clone 시 로컬 경로 실행은 동등 경로로 후치. 예시 블록에 원격 실행 시 인자 전달법(`| python3 - --dry-run`) 1줄 주석 추가. 기존 문면은 clone 전제로 읽혔던 것이 실제 개선점이었다.
- **검증**: 4경로 전부 `--dry-run` 실행 확인(clone 없는 `/tmp`에서 1·2·3, repo에서 4) — curl 파이프 / `uv run` URL / 인자 전달(`--force` 부여 시 `unchanged` → `would replace`로 전환) / 로컬 경로. current spec(`main.md`·`components.md`)에는 이 스크립트 언급이 없어(`_sdd/spec/prev/` 스냅샷 한정) spec-sync 불요.
- **부수 관측**: 로컬 `~/.codex` 설치본이 최신 main보다 낡음 — `pr-review-agent.toml`·`simplicity-review-agent.toml` 2개가 `would replace`.

## 15:30 plan-review gather 병렬화 feature draft 작성 + gate 1 (Fable 5)

- **무엇/왜**: plan-review 게이트가 9~10분(직렬 read 22~25회) 걸리는 병목 개선. 사용자 논의로 task-shard(cross-task smell 훼손)·smell별/2-렌즈 판정 분할(컨텍스트 중복+merge 부담) 기각, "병렬 gatherer → digest 파일(`_sdd/pipeline/plan_review_gather/`) → 단일 전역 판정" 확정. 신규 `plan-context-gatherer` agent(Read/Glob/Grep/Write) 등록 포함 task 5개.
- **결과**: draft 작성 완료, plan-review gate 1 (fable-5): **H1 M2 L1** → fix 1 전량 반영(High: `.gitignore`에 `_sdd/pipeline/` 부재 실측 → Task 2에 `[M] .gitignore`+AC 추가; M: codex read 규칙 전파, census grep `_sdd` 제외; L: Source Pointer 문구 갱신). gate 2 임계 미달(C+H=1, M=2) — 미실행. 게이트 소요 91s/tool 7회 — 호출자가 컨텍스트를 미리 주면 빨라진다는 gather 가설의 방증.
- **포인터**: `_sdd/drafts/2026-08-14_feature_draft_plan_review_parallel_gather.md`
- **잔여**: digest 저장 위치(scratchpad→`_sdd/pipeline/`) 사용자 확인 대기. 게이트 2 임계 조정은 별도 feature.

## 15:45 plan-review gather 병렬화 구현 + implementation-review gate (Fable 5)

- **무엇/왜**: draft의 task 5개 구현 — plan-context-gatherer agent 신규(claude .md + codex .toml + marketplace + codex README 등재), 양측 plan-review SKILL.md gather phase(병렬 gatherer→digest 파일→경로만 중계, degrade), plan-review-agent digest 입력 모드(residual read 확정 의무·하위 호환), `.gitignore`에 `_sdd/pipeline/`, 명명 census. 전 task (b) structural-check로 RED→GREEN.
- **결과**: implementation-review gate 1 (fable-5, correctness 5 shard ∥ simplicity 2 묶음): C0 H0 **M2 L6** → M2(codex gather bullet 재서술+과잉압축) 단일 재작성 fix, correctness L3 fix, advisory 3 잔존. gate 2 임계 미달. AC 1등급 전부 재현 grep 통과, 2등급 전부 reviewer 인용 MET. 계획 이탈 1: codex README 실경로 `.codex/agents/README.md`(draft 표기 정정).
- **포인터**: ledger `_sdd/implementation/2026-08-14_implementation_ledger_plan_review_parallel_gather.md`, branch `refactor/plan-review-parallel-gather`

## 15:55 plan-review gather 병렬화 spec-sync (Fable 5)

- **무엇/왜**: 구현 완료분을 global spec에 승격 — main.md v4.16.0(gather phase 서술을 렌즈 재제안 금지 불릿에 공존 명시, 쓰기-서로소 작성자 분할 인스턴스 0→1, gatherer는 reviewer 아님, 🚧 Planned 2건: 벽시계 효과 판정·게이트 2 임계 비례화), components.md plan-review 행·Platform Notes 갱신, decision_log·changelog entry append.
- **검증**: 헤더 4.16.0 = changelog 최신 entry 일치, 기록 파일 삭제 줄 0(append-only), draft `_processed_` rename.
- **포인터**: `_sdd/spec/logs/changelog.md` v4.16.0, `_sdd/spec/decision_log.md` 2026-08-14 entry
