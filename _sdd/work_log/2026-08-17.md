# 2026-08-17

## 1/20:40 게이트 review-fix 루프 지속 규칙 추가 (fable-5)

- 무엇/왜: feature-draft·implementation의 품질 게이트가 리뷰 반환 후 fix로 복귀하지 않고 턴을 닫는 문제 진단·수정. 원인 = 직접 실행 전환(v4.20~4.21) 후 리뷰 스킬의 종결 형태("채팅 반환이 전부"·"finding 반영은 producer 소관")가 프레임을 지배하는데, 반환 직후 fix로 복귀하라는 명시 명령이 producer·reviewer 어느 쪽에도 없었음 — agent 시대 역할 분리 언어의 잔재. gate 2 조건 분기도 자연 중단 지점.
- 결과: producer 2벌(feature-draft·implementation)에 "게이트 반환은 중간 산출물, 사용자 입력 대기 지점 아님 — 반환 직후 fix 시작, gate 2 판정·실행도 묻지 않고 수행" 루프 지속 규칙 추가, reviewer 2벌(plan-review·implementation-review) Integration에 "게이트 호출 시 반환 직후 호출 스킬 fix 단계로 복귀" 짝 문장 추가. claude/codex 미러 각 8벌, 문면 동일(codex 적응 delta 없음).
- 포인터: 브랜치 fix/gate-loop-continuation, PR 예정. 게이트 정책 자체(임계값·fix 횟수)는 불변이라 spec 변경 없음.

## 2/21:25 ralph-loop-init 검증 구조화 — verify.sh 분리 + DONE 게이트 (fable-5)

- 무엇/왜: 사용자 요청 리뷰에서 무인 루프의 자가 성공 경로 2종(Success Criteria 자체 편집, 검증 없는 DONE 선언) 확인 → SDD 체인으로 구현. verify.sh 고정 분리, run.sh DONE 게이트(Final status + PASS 시 verify exit 0, reject 시 ADJUST_PHASE 복귀), PROMPT.md 편집 금지 구역·in-run 가드·_sdd 수정 금지, 표면(AC·CHECKS·Step 4/8·요약) 갱신. claude/codex 미러 2벌.
- 결과: plan-review gate 1 (fable-5): H1 M1 L1 → fix. impl-review gate 1 (fable-5): correctness L1 + simplicity M1 → fix(set_state_note·멤버십 가드). 게이트 기능 테스트 6/6 양 미러 + 변이 확인, anchor 20/20. 이번 세션에 넣은 게이트 루프 지속 규칙(PR #77) 발효 후 첫 실전 — 두 게이트 모두 반환 직후 fix로 끊김 없이 복귀함.
- 포인터: spec components.md ralph 행(7파일·DONE 게이트 계약), decision_log 2026-08-17 entry, draft _processed_2026-08-17_feature_draft_ralph_verify_hardening.md, PR 예정
