# 2026-07-27

## 1/spec-sync (implemented, evidence 있음) — 죽은 `skill.json` 삭제 + version 단일 소스 고정

- **무엇/왜**: draft `2026-07-27_feature_draft_drop_dead_skill_json.md` Part 1의 delta를 global spec에 반영. 체인 종료.
- **status 분류**: 4개 delta 전부 IMPLEMENTED로 무표식 승격. evidence — `find .claude/skills .codex/skills -name skill.json` 0건(삭제 전 37), `git status` D 37 + M 2로 정확히 일치, live 표면(`.claude/`·`.codex/`·`.claude-plugin/`·`tools/`·`docs/`·`AGENTS.md`·`CLAUDE.md`·`README.md`·`_sdd/env.md`) 전수 grep 0건, 미러 쌍 version 전수 대조 재실행, `git diff --check` 무출력. 구현 측 evidence는 structural check 14 PASS/0 FAIL + 회귀 79/0·39/0 + 40개 frontmatter PyYAML 전수 파싱 실패 0.
- **결과**: `main.md` 4.6.10→4.6.11. §3 결정 테이블 `Skill 정의 형식` 행을 확장(`SKILL.md` frontmatter가 메타데이터 단일 소스, 사이드카 파일 없음) — **새 guardrail도 새 결정 행도 만들지 않았다**. Repo-wide Invariant Test #1(한두 파일 읽기로 복구 — `SKILL.md` 하나 열면 frontmatter가 보이고 사이드카 부재는 `ls`로 확인) 미통과이고, "Markdown `SKILL.md`가 스킬 정의 형식"이라는 기존 결정의 실체 확정이라 별도 행을 만들면 판정 주체가 둘로 갈린다(v4.6.10 anti-duplication 논법 재사용). `decision_log.md`·`logs/changelog.md` 신규 entry. `components.md`·`usage-guide.md` 무변경(`skill.json` 리터럴 0건).
- **planned 치환 (사용자 명시 요청 항목)**: `main.md:150`의 기존 `🚧 Planned`("`implementation-review` version 4필드 불일치")를 **삭제가 아니라 치환**으로 닫았다. 지목 대상(`skill.json` `2.1.0`/`3.0.0`)은 물리적으로 소멸했고 남은 2필드는 이미 일치(`7.0.0`)라 항목은 종결이 맞지만, 상위 사실("version 갱신이 편집 discipline 의존")은 유효하고 실례가 남는다 — 미러 쌍 전수 대조 재확인 결과 live version 드리프트는 `guide-create`(`2.2.0`/`2.4.0`) **1건**. 상위 불릿을 version 단일 소스 서술로 재작성하고, 하위 planned를 `guide-create`·`spec-snapshot` **미러 본문 세대 격차**(176/159줄, 135/118줄 + codex `:28`에만 legacy uppercase 대응)로 교체했다. `spec-snapshot`은 version을 `1.2.0`으로 맞춘 탓에 version만으로는 격차가 안 보이므로 명시 기록이 특히 필요했다.
- **Open Question 3건은 planned 고정하지 않음**: (a) `version:` 필드 자체 폐지 — 소비자 census 0으로 `skill.json`과 같은 논법이 성립하나, 같은 sync에서 "frontmatter = version 단일 소스"를 고정하면서 그 필드 전폐를 planned로 박으면 spec이 자기모순이 된다. 사용자 판정 대기. (b) Codex 번들 검증기 허용 키(`version`·`argument-hint` 부재) — 큐레이션 패키징 검증기 추정이라 근거가 약하고 현재 정상 로드. (c) 미러 본문 세대 격차의 canonical 방향 — 격차 존재는 planned로 고정했으나 어느 쪽이 구세대인지는 본문 대조 선행 필요라 미정으로 뒀다.
- **포인터**: `_sdd/spec/main.md` §3 결정 테이블·`현재 운영 제약` · `_sdd/spec/decision_log.md` 2026-07-27 entry · `_sdd/spec/logs/changelog.md` v4.6.11 · `_sdd/drafts/_processed_2026-07-27_feature_draft_drop_dead_skill_json.md`
