# 2026-07-27

## 1/spec-sync (implemented, evidence 있음) — 죽은 `skill.json` 삭제 + version 단일 소스 고정

- **무엇/왜**: draft `2026-07-27_feature_draft_drop_dead_skill_json.md` Part 1의 delta를 global spec에 반영. 체인 종료.
- **status 분류**: 4개 delta 전부 IMPLEMENTED로 무표식 승격. evidence — `find .claude/skills .codex/skills -name skill.json` 0건(삭제 전 37), `git status` D 37 + M 2로 정확히 일치, live 표면(`.claude/`·`.codex/`·`.claude-plugin/`·`tools/`·`docs/`·`AGENTS.md`·`CLAUDE.md`·`README.md`·`_sdd/env.md`) 전수 grep 0건, 미러 쌍 version 전수 대조 재실행, `git diff --check` 무출력. 구현 측 evidence는 structural check 14 PASS/0 FAIL + 회귀 79/0·39/0 + 40개 frontmatter PyYAML 전수 파싱 실패 0.
- **결과**: `main.md` 4.6.10→4.6.11. §3 결정 테이블 `Skill 정의 형식` 행을 확장(`SKILL.md` frontmatter가 메타데이터 단일 소스, 사이드카 파일 없음) — **새 guardrail도 새 결정 행도 만들지 않았다**. Repo-wide Invariant Test #1(한두 파일 읽기로 복구 — `SKILL.md` 하나 열면 frontmatter가 보이고 사이드카 부재는 `ls`로 확인) 미통과이고, "Markdown `SKILL.md`가 스킬 정의 형식"이라는 기존 결정의 실체 확정이라 별도 행을 만들면 판정 주체가 둘로 갈린다(v4.6.10 anti-duplication 논법 재사용). `decision_log.md`·`logs/changelog.md` 신규 entry. `components.md`·`usage-guide.md` 무변경(`skill.json` 리터럴 0건).
- **planned 치환 (사용자 명시 요청 항목)**: `main.md:150`의 기존 `🚧 Planned`("`implementation-review` version 4필드 불일치")를 **삭제가 아니라 치환**으로 닫았다. 지목 대상(`skill.json` `2.1.0`/`3.0.0`)은 물리적으로 소멸했고 남은 2필드는 이미 일치(`7.0.0`)라 항목은 종결이 맞지만, 상위 사실("version 갱신이 편집 discipline 의존")은 유효하고 실례가 남는다 — 미러 쌍 전수 대조 재확인 결과 live version 드리프트는 `guide-create`(`2.2.0`/`2.4.0`) **1건**. 상위 불릿을 version 단일 소스 서술로 재작성하고, 하위 planned를 `guide-create`·`spec-snapshot` **미러 본문 세대 격차**(176/159줄, 135/118줄 + codex `:28`에만 legacy uppercase 대응)로 교체했다. `spec-snapshot`은 version을 `1.2.0`으로 맞춘 탓에 version만으로는 격차가 안 보이므로 명시 기록이 특히 필요했다.
- **Open Question 3건은 planned 고정하지 않음**: (a) `version:` 필드 자체 폐지 — 소비자 census 0으로 `skill.json`과 같은 논법이 성립하나, 같은 sync에서 "frontmatter = version 단일 소스"를 고정하면서 그 필드 전폐를 planned로 박으면 spec이 자기모순이 된다. 사용자 판정 대기. (b) Codex 번들 검증기 허용 키(`version`·`argument-hint` 부재) — 큐레이션 패키징 검증기 추정이라 근거가 약하고 현재 정상 로드. (c) 미러 본문 세대 격차의 canonical 방향 — 격차 존재는 planned로 고정했으나 어느 쪽이 구세대인지는 본문 대조 선행 필요라 미정으로 뒀다.
- **포인터**: `_sdd/spec/main.md` §3 결정 테이블·`현재 운영 제약` · `_sdd/spec/decision_log.md` 2026-07-27 entry · `_sdd/spec/logs/changelog.md` v4.6.11 · `_sdd/drafts/_processed_2026-07-27_feature_draft_drop_dead_skill_json.md`

## 2/11:22 draft + 구현 + 리뷰 게이트 — `SKILL.md` frontmatter `version:` 필드 삭제

- **무엇/왜**: 직전 단위(1/)의 Open Question (a)를 사용자가 "지우자"로 판정. `version`은 소비자가 0이다 — 두 런타임 바이너리 모두 frontmatter에서 `name`·`description`만 사용하고, `tools/install-codex-skill-bundle.py`는 `SKILL.md` 존재만 확인(`:284`,`:461` copytree 전면 교체), `marketplace.json`은 디렉토리 경로 등록, `AGENTS.md`·`README.md`·`docs/`·agent 파일 참조 0건. 유지 근거는 "사람이 읽는 앵커" 하나뿐이었고, 비용은 실측된 드리프트(`guide-create` 2.2.0/2.4.0)로 계상돼 있었다.
- **결과**: `.claude/skills/*/SKILL.md` 21 + `.codex/skills/*/SKILL.md` 19 = **40파일**에서 frontmatter `version:` 줄만 삭제(각 파일 diff `+0/-1`, YAML 재직렬화 없음). 남은 키는 `name`·`description` + 선택 키(`argument-hint`·`user_invocable`). "version lockstep" 개념이 4필드→2필드(PR #27)를 거쳐 **0필드**로 소멸.
- **검증**: structural check 10 PASS/0 FAIL, 회귀 3종 13/0·77/0·39/0, 40개 frontmatter PyYAML 전수 파싱 실패 0. plan-review 1회(High 1 + Medium 3 반영) → implementation-review(correctness ∥ simplicity) 1회 + fix 1회. correctness reviewer가 installer·marketplace·심볼릭 링크 경로를 독립 재검증해 깨지는 경로 0건 확인.
- **계약 폐기 선언 3건**: `check_gates.py` version 2필드 검사 2건 삭제(필드 소멸), `check_skill_json.py` version 검사 2건 삭제(이번 변경이 뒤집은 계약), 같은 스크립트의 상태 가드 2건을 머지 범위(`ee677ca..4e9b0c0`)로 고정. 이번 세션에서 "worktree 기준 범위 가드가 후속 작업에서 오탐"이 **네 번째**라, check를 repo에 영속화한다면 범위 가드는 항상 baseline/머지 범위 기준으로 쓰는 편이 낫다.
- **기각한 finding 1건**: simplicity reviewer가 2라운드 연속 `user_invocable: true`를 "소비자 0인 같은 부류"로 삭제 제안했으나, Claude Code 2.1.220 바이너리가 `user_invocable`·`user-invocable`을 실제로 읽는다(문자열 2건 실측). 재census 방지를 위해 근거를 draft Scope Out에 고정했다.
- **복구 앵커**: 삭제된 40개 version 값은 `git show 4e9b0c0:<path>`로 복구한다(소비자 0이라 별도 tag는 두지 않음).
- **포인터**: `_sdd/drafts/2026-07-27_feature_draft_drop_skill_version_field.md` · 삭제 직전 커밋 `4e9b0c0`

## 3/spec-sync (implemented, evidence 있음) — `version:` 필드 삭제를 global spec에 반영

- **무엇/왜**: draft `2026-07-27_feature_draft_drop_skill_version_field.md` Part 1의 delta를 반영. 체인 종료.
- **status 분류**: 4개 delta 전부 IMPLEMENTED로 무표식 승격, 기존 `🚧 Planned` 1건은 planned 유지(재서술). evidence — live 표면(`.claude/`·`.codex/`·`.claude-plugin/`·`tools/`·`docs/`·`AGENTS.md`·`CLAUDE.md`·`README.md`) 전수 `^version:` 0건, `git diff --stat 4e9b0c0` = 41파일 +10/-40(SKILL.md 40개 각 -1), 보존 대상 2건 잔존 확인, `user_invocable`·`argument-hint` 잔존 확인.
- **결과**: `main.md` 4.6.11→4.6.12. §3 결정 행 `Skill 정의 형식`을 **교체**(직전 sync가 이틀 전 고정한 같은 행) — "스킬 버전을 담는 필드·파일은 두지 않는다(스킬 변경 이력 = git history)". 유지 이유를 "사이드카(구 `skill.json`)든 frontmatter 필드(구 `version:`)든 소비자 없는 값은 두지 않는다"로 **일반화**해 다음 판정 기준이 파일 형태가 아니라 소비자 census가 되게 했다. 별도 결정 행·guardrail 신설 없음(v4.6.11 anti-duplication 논법 동일). `현재 운영 제약`의 version 단일 소스 불릿은 소멸.
- **planned 처리 — 이번엔 치환이 아니라 "재료 교체 후 유지"**: v4.6.11은 planned를 치환으로 닫았지만(대상 소멸 / 사실 잔존), 이번엔 상위 불릿이 대변하던 사실 자체가 소멸해 **삭제**가 맞았다. 반면 하위 planned(미러 본문 세대 격차)는 격차가 미해소라 유지하되 근거 재료만 version→본문 실측으로 갈아끼우고 최상위로 승격했다: `guide-create` 175/158줄 + codex에 `**Version**: X.Y.Z` 템플릿 블록 부재, `spec-snapshot` 134/117줄 + codex에만 legacy uppercase 대응. "버전 값 비교라는 우회 신호가 이제 없다"를 명시해 앞으로 세대 판정이 본문 대조뿐임을 고정했다. codex `spec-snapshot` 포인터는 줄 번호(`:28`→`:27` 밀림) 대신 규칙 내용으로 바꿔 재드리프트를 막았다.
- **산문 정리는 판정 규칙으로**: `v?\d+\.\d+\.\d+` 전수 대조 후 스킬 버전만 10줄 정리. (i) 현재 계약 표기는 괄호째 삭제, (ii) 역사 앵커는 feature 앵커로 치환. `components.md`의 `v3.0.0에서 마감이 강제 게이트 계약이 됐다`는 대응 feature ID가 없어 현재형 계약 서술로 바꿔 해소했다. `main.md`의 `sdd-autopilot` 불릿은 스킬 버전 `v4.0.0`과 문서 버전 `2.1.0` 공존이라 부분 편집.
- **포인터**: `_sdd/spec/main.md` §3 결정 테이블·`현재 운영 제약` · `_sdd/spec/components.md`(4줄) · `_sdd/spec/usage-guide.md`(1줄) · `_sdd/spec/decision_log.md` 2026-07-27 상단 entry · `_sdd/spec/logs/changelog.md` v4.6.12 · `_sdd/drafts/_processed_2026-07-27_feature_draft_drop_skill_version_field.md`
