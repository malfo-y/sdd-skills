## 12:32 Codex implementation fix dispatch guard

### 무엇/왜
- Codex `implementation` 스킬의 review-fix loop에서 orchestrator가 fix를 직접 수행하지 않고 내부 agent dispatch를 사용해야 한다는 런타임 어댑터 문장을 강화했다.
- 실제 Codex 런타임의 sub-agent 명시 허가 정책과 스킬 계약 사이에서 오케스트레이터가 직접 수정으로 빠지는 해석 여지를 줄이기 위함이다.

### 결과
- `.codex/skills/implementation/SKILL.md` Runtime Adapter에 직접 호출이 내부 SDD agent spawn에 대한 명시적 사용자 허가이며, fix 중 orchestrator 직접 코드 편집은 금지되고 dispatch 불가 시 `BLOCKED`로 보고해야 한다는 문장을 추가했다.

### 포인터
- `.codex/skills/implementation/SKILL.md`
