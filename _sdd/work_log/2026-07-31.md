# 2026-07-31

## 1/11:35 feature-draft — agent tool call 배칭 규칙

- **무엇/왜**: 사용자 질문("리뷰가 느린데 subagent라서 그런가")에서 출발. 2026-07-30 게이트의 subagent transcript를 계수해 답을 냈다 — correctness 430s/17콜/31턴, simplicity 177s/3콜/7턴, **user 메시지당 tool_result가 정확히 1개**(= 배칭 0회, 완전 직렬). correctness 17콜 중 앞 9개는 호출자 digest만 보고 대상이 정해지는 것들이라 서로 의존하지 않았다.
- **가설 판정**: nesting 제한(agent가 sub-agent를 못 띄움)은 원인이 아니다. 병렬이 안 되는 건 사실이나 원인은 **배칭 지시 부재**이고, nesting이 허용돼도 파일 5개 읽자고 자식을 띄우면 컨텍스트 적재가 배칭보다 비싸다. 병목은 dispatch 왕복도 읽기량도 아니라 **턴 수만큼 반복되는 추론**.
- **레버 상태 정리**: 출력 다이어트(1% 미만, 측정 종료)·입력 상한(읽기 범위 계단이 이미 `Read`를 5회로 억제)·모델 강등(재제안 금지)은 모두 소진. 남은 건 턴 접기.
- **결과**: `_sdd/drafts/2026-07-31_feature_draft_reviewer_tool_batching.md` (적격, task 3개 — claude 5종 Hard Rule / codex TOML 5종 3-way merge / 행동 계측). 브랜치 `feat/reviewer-tool-batching`.
- **plan-review High 2 + Medium 4 + Low 2 전량 반영**:
  - **H1 (내 사실오인)**: "모든 agent가 Hard Rule 8을 공유한다"고 전제했는데 실측은 **8/8/10/8/13**이었다(`pr-review` 10, `spec-sync` 13). AC4의 "1~8 불변"이 그 두 파일에서 9~13번 훼손을 통과시키는 공허한 검사였다. → 파일별 실측 번호를 draft에 박고 AC를 `git diff -U0` 전량 인용으로 교체.
  - **H2**: Task 3에 계측 주체·transcript 획득 경로·계수 규칙·기록 위치가 전부 없어 **실행 불가**였다. → 별도 재dispatch를 없애고 **마감 게이트 실행을 그대로 관측 대상**으로 재정의(중복 7분 제거), JSONL 1줄=1메시지 계수 규칙과 기록 위치(마감 보고) 명시.
  - **M1 (가장 값진 지적)**: repo의 기존 배칭 문장이 전부 **허가형**("배칭해 병렬로 실행해도 된다")이다. 새 규칙도 허가형으로 렌더링되면 AC2는 통과하는데 행동은 안 바뀐다. → AC에 "지시형일 것"을 판정 조건으로 추가.
  - **M2**: 계단 예외를 Hard Rule에 두면 계단 판정 주체가 두 곳으로 갈린다 → 제약 문장을 `plan-review-agent` Step 3 안으로 내리고 Hard Rule은 5종 동일 문면 유지.
  - **M3**: 규칙 주문장을 `multi_tool_use.parallel`에 걸면 런타임이 그 이름을 안 내놓을 때 **실패하는 호출 지시**가 된다 → 주문장은 tool 이름 비의존, codex 관용은 예시로만. codex 미러 5파일 실측 검증 0을 Open Question에 명시.
  - **M4**: 배칭 관측의 인과 귀속 불가(baseline과 repo 상태·대상이 다름) → 양성=정합 관측 / 음성=반증이라는 **비대칭**을 AC에 못박음.
  - **L1** grep 범위를 `## Hard Rules` 절로 한정. **L2** 어휘를 repo 선례 "한 메시지에서"로 정렬.
  - 리뷰어가 추가로 짚은 부작용(배칭 지시가 "미리 다 읽어두자"로 번져 입력 상한을 잠식) → 필수 요소에 "배칭은 읽을 대상을 늘리지 않는다" 추가(3요소 → 4요소).
- **포인터**: `_sdd/drafts/2026-07-31_feature_draft_reviewer_tool_batching.md`

## 2/11:50 implementation — agent tool call 배칭 규칙 (Task 1·2 완료, Task 3 계측 대기)

- **결과**: agent 10파일에 `tool call 배칭` Hard Rule 1개씩 추가(번호 8/8/8/10/13 → 9/9/9/11/14, 파일별로 다름). `plan-review-agent` 짝에만 Step 3 도구 계단 절에 "배칭은 같은 단 안에서만" 1문장. codex TOML 5종은 `multi_tool_use.parallel` 예시 문구를 덧붙인 3-way merge. 삭제 라인 0(순수 추가).
- **검증**: structural check RED 23 FAIL → GREEN 64 PASS / 0 FAIL. 변이 8건 전량 검출 후 복구·재실행 통과.
- **스크립트 함정 2건 (이 repo가 반복해 밟는 계열, 둘 다 이번에 실측)**:
  - **멀티바이트가 변수명을 삼킴**: `"$head_n→$work_n"`처럼 변수 뒤에 공백 없이 `→`를 붙이면 bash 3.2가 `head_n→...`를 식별자로 읽어 `unbound variable`로 죽는다. 하필 PASS 분기에만 있어 **RED 때는 드러나지 않고 GREEN 직전에 터졌다**. → `${head_n} → ${work_n}`.
  - **파이프가 exit code를 가림**: `bash check.sh | tail -30`으로 돌려 `$?`가 tail 것이 됐고, 스크립트가 중간에 죽었는데 `exit=0`으로 보였다. → `out=$(bash chk); rc=$?` 로 캡처.
- **커버리지 델타 3건 (신설 §4 절차의 첫 실사용, 전부 (b)로 닫음)**: AC가 "필수 요소가 문장으로 있는가"만 봐서 아래가 전부 미커버였다.
  - **D1**: 계단 제약 문장이 계단 4항목 **뒤**에 오는가 — 앞에 오면 아직 정의되지 않은 '단'을 참조하게 된다. AC3는 "Step 3 절 안"까지만 봤다.
  - **D2**: codex 전용 예시 문구(`multi_tool_use.parallel`)가 claude 파일로 누출되지 않는가 — PAIR check은 4요소 앵커만 대조해 이 문구를 안 본다.
  - **D3**: 진영 내 5파일의 규칙 본문이 바이트 동일한가 — 한 파일만 오타 나도 AC2/PAIR가 전부 통과한다.
  - 3건 다 변이로 판별력 증명(위치 이동/누출 삽입/1파일 오타 → 각각 검출).
- **PAIR check의 공허 통과 관찰**: RED 시점엔 claude·codex 양쪽 다 앵커가 없어 "대응함"으로 PASS했다(파리티 검사의 구조적 성질). AC2가 존재를 따로 잡아 실해는 없지만, **파리티 검사는 양쪽 공백에서 무조건 통과한다**는 걸 기록해둔다.
- **Task 3(행동 계측)**: 별도 재dispatch 없이 이 마감 게이트 실행을 관측 대상으로 삼는다(plan-review H2 반영 — 중복 7분 제거). 결과는 마감 보고에.
- **포인터**: draft `_sdd/drafts/2026-07-31_feature_draft_reviewer_tool_batching.md` · check `scratchpad/check_batching.sh`

## 3/11:56 implementation 마감 — 게이트 fix + L3 범위 확장 + Task 3 UNTESTED

- **게이트 결과**: correctness High 1 + Medium 1, simplicity Medium 1 → **fix 3건**. Blocker는 H1(Task 3 계측 무효).
- **계측 규칙이 틀렸다 (내 오류, 실측으로 교정)**: transcript JSONL은 **한 메시지의 content 블록을 줄 단위로 쪼개** 기록한다(같은 assistant 메시지의 `text`/`tool_use`가 별도 줄). 그래서 "메시지당 tool_use 수"는 **항상 1**이고, 배칭을 원리적으로 탐지하지 못한다 — 15개 transcript 전량 max=1이 그 증거였다. 유효 지표는 **연속 실행 길이**(user tool_result가 끼지 않고 이어지는 assistant tool_use 줄 수)다.
  - **양성 대조로 지표 검증**: 같은 날 `plan-review` 회차가 최대 연속 **2**(구간 1개)로 잡힌다. correctness·simplicity는 4회차(7/30·7/31 각 2종) 전부 **1**.
  - **결론: 전제("배칭 0")는 지표를 바꿔도 살아남았다.**
- **리뷰어 자기보고 1건 기각**: correctness가 "이번 세션에 한 메시지에 2·2·4·2·2개를 냈다(최대 4)"고 주장했으나 자기 transcript 실측은 최대 1이다. 그 위에 세운 "규칙 없이도 배칭이 나온다"는 논거는 성립하지 않는다. **리뷰어의 자기 행동 보고는 transcript로 교차검증해야 한다.**
- **H1 본체는 맞았다 — 이번 세션 최대 수확**: dispatch되는 agent/skill은 작업트리가 아니라 **plugin 설치본**(`~/.claude/plugins/cache/sdd-skills/`, 푸시된 커밋 SHA)에서 로드된다. 검증: 현 설치본 `fc8f1c9`에 배칭 규칙 **0/5**, 반면 어제 머지한 커버리지 델타는 **1건** 존재. 즉 **오늘 편집한 agent 본문은 커밋·푸시·플러그인 갱신 전까지 발효되지 않으며, 마감 게이트는 구 본문으로 돌았다.**
  - → Task 3을 **UNTESTED**로 정직하게 닫고, draft에 계측 전제조건(푸시 후 갱신 + 배칭 논의 없는 새 세션)을 명문화.
- **fix 3건**: (a) simplicity M1 — 계단 문장 중간 절이 Hard Rule 9 (i)을 재천명해 삭제. (b) correctness M1 — codex tool 이름을 지시형→조건형(`제공하면 그것으로 표현한다`), 없는 도구 호출로 턴 낭비하는 역효과 차단. (c) H1 — draft 계수 규칙·AC1·표 헤더 교정 + 전제조건 추가.
- **L3 범위 확장 (사용자 지시)**: draft Scope Out이었던 `implementation` SKILL `## 입력` 불릿을 처리. 한 문장이 **재량(task 병렬)과 항상-이득(tool call 배칭)을 섞고 있던 것**이 문제였다 → 둘로 쪼개 배칭만 지시형으로 올리고 task 병렬은 허가형 유지(강제하면 안 되는 판단), RED→GREEN 불변식 보존. 미러 2벌.
- **커버리지 델타 (L3 diff)**: 1건 — 새 불릿이 `## 입력` 절 안에 있는가(어느 절에 있든 grep은 통과). L3e로 닫고 `## 마감` 이동 변이로 검출 확인.
- **최종 회귀**: check **73 PASS / 0 FAIL**, 변이 누적 12건 전량 검출, `git diff --check` clean.
- **잔존 advisory**: L1(`plan-review` 1↔2단은 서로 독립이라 Hard Rule만 보면 묶일 여지 — 비용 Glob+Grep 1회, 대체안이 계단 소유권·D3 바이트 동일성을 깨서 인지만) · L2(쓰기 없는 2종에서 요소(iii) 도달 불가 — 10파일 동일 문면 계약상 유지).
- **포인터**: draft `_sdd/drafts/2026-07-31_feature_draft_reviewer_tool_batching.md` · check `scratchpad/check_batching.sh`

## 4/12:05 spec-sync — agent tool call 배칭 규칙 (implemented, evidence 있음 / 효과는 미검증)

- **결과**: spec 4파일 — `main.md` 4.6.17→**4.6.18**(§2 nesting 불릿 하위에 "실행 경제 — 해소 수단은 fan-out이 아니라 턴 접기다" 1개, §3 현재 운영 제약에 plugin 캐시 지연 + transcript 계측 지표 2개, 그리고 행동 효과 `🚧 Planned` 1개), `components.md` `plan-review` 행(배칭×계단 제약), `decision_log.md` 신규 entry(33/0), `logs/changelog.md` v4.6.18(8/0).
- **agent의 배치 판단 (동의)**: 배칭 규칙을 **nesting 1단계 제한 불릿의 하위**에 넣었다 — "nesting 때문에 느리다"는 오인이 정확히 그 불릿을 읽는 사람에게서 나오기 때문. 계단×배칭 상호작용은 단일 agent 계약이라 `Repo-wide Invariant Test` 미통과 → `components.md`로. 반면 plugin 캐시 지연·transcript 지표는 agent·skill 변경 **일반**에 걸리므로 §3 본문 승격.
- **정직성 게이트 통과 확인**: 절감 수치가 spec 어디에도 사실로 적히지 않았다. 매칭된 2줄은 오히려 "단일 표본으로 주장하지 않는다"·"어디에도 기록하지 않으며"라는 미승격 선언이다. 행동 효과는 `🚧 Planned`로 남고 전제조건(푸시 후 갱신 + 배칭 개념 없는 새 세션 + 연속 실행 길이 ≥ 2)이 함께 기록됐다.
- **검증**: append-only 삭제 0줄, `git diff --check` clean, structural check 재실행 73 PASS / 0 FAIL, draft `_processed_` rename 완료.
- **Follow-up (decision_log에 기록)**: codex 미러 5파일 검증 경로 0 → 다음 codex 실행 시 사후 확인 · `plan-review` 1↔2단 배칭 여지 · 쓰기 없는 2종의 요소(iii) 도달 불가.
- **포인터**: `_sdd/spec/decision_log.md` 2026-07-31 entry · 위 `## 1/11:35`~`## 3/11:56`

## 5/12:25 계측 — 배칭 Hard Rule 첫 처치군 관측 (효과 없음 + 전제 정정)

- **무엇/왜**: `f1c2fbd` 머지 후 플러그인이 갱신돼(설치본 배칭 규칙 5/5 확인) 규칙이 발효됐다. 배칭과 무관한 대상(커버리지 델타 PR#32 사후 감사)으로 게이트 2종을 돌려 첫 처치군 데이터를 얻었다. digest에는 배칭을 언급하지 않았다.
- **A/B가 아니라 before/after 누적으로 확정**: 활성 플러그인 SHA가 하나뿐이라 동시 대조 불가 + 대조군 자체에 변동(`plan-review`는 규칙 없이 2)이 있어 n=1 arm 비교는 무의미하다. 사용자가 누적 관측(옵션 1) 선택.
- **지표 재검증(양성 대조)**: `plan-review` 회차의 "연속 2"가 아티팩트가 아님을 직접 확인 — 같은 assistant 메시지에서 `Glob` 2개가 나가고 두 결과가 다음 assistant 턴 전에 함께 돌아온다. 지표 유효.
- **결과 — 규칙 ON에서도 최대 연속 1**:

  | 회차 | 규칙 | 턴 | tool_use | 최대 연속 |
  |---|---|---|---|---|
  | 7/30 correctness | OFF | 31 | 17 | 1 |
  | 7/30 simplicity | OFF | 7 | 3 | 1 |
  | 7/31 correctness | OFF | 28 | 17 | 1 |
  | 7/31 simplicity | OFF | 21 | 13 | 1 |
  | 7/31 plan-review | OFF | 23 | 12 | **2** |
  | 7/31 correctness | **ON** | 35 | 21 | **1** |
  | 7/31 simplicity | **ON** | 8 | 4 | **1** |

  draft AC의 비대칭대로 음성은 n=1에서도 반증력이 있다 — 규칙이 문면으로만 남았을 가능성이 크다.
- **전제 정정 (중요)**: correctness의 Bash 호출은 OFF·ON 양쪽 **100% 복합 명령**(`&&`·`;`·`|`)이었다(12/12, 16/16). 첫 호출부터 `git status --short && git log --oneline -3 && git diff --stat` 식이다. 즉 **agent는 이미 셸 체이닝으로 배칭하고 있었고**, multi-tool 배칭의 실제 여지는 셸로 못 엮는 `Read` 5회뿐 — 28~35턴 중 4턴 남짓이다. draft/decision_log에 적힌 "앞 9개가 서로 의존하지 않는데 한 개씩 냈다"와 "17콜 → 5~6라운드" 추정은 **호출 수만 세고 호출 안의 내용을 안 본 판단**이었다.
- **판단**: 되돌리지 않고 누적한다. 지금 되돌리면 n=1 음성으로 결론내는 셈이고, 지금 문면을 강화하면 효과 미확인 상태에서 규칙만 키운다. 무관한 과제 2~3건 더 쌓아 1이 고정인지 확인한 뒤 판정.
- **미채택 후보**: 현 문면이 추상 원칙("서로 의존하지 않는 read-only 호출")이라 매번 독립성 판정이 필요하다. 여지가 `Read`에 몰려 있으니 "읽기 범위 계단 ①의 파일 `Read`는 한 메시지에 모은다"처럼 상황을 지목하는 형태가 후보 — 효과 확인 전에는 착수하지 않는다.
- **사후 감사 부산물 (별건)**: correctness Medium 2 — (M1) 마감 §3에 §4 델타로 되돌아갈 포인터가 없다(규칙 소유자는 §4, 사용 지점 포인터 누락), (M2) "삭제가 1순위" 경로에 삭제 후 재검증 의무가 없어 증거 테이블의 GREEN 증거가 최종 코드와 어긋날 수 있다. simplicity Medium 2 — (M1) §4 첫 문단이 6개 의무를 산문에 압축, (M2) `(c) task` 예외절이 정의되지 않은 척도를 도입하고 일반 규칙과 같은 결과로 합류. **이번 계측 단위에서는 미처리** — 커버리지 델타 feature의 후속으로 따로 다룬다.
- **포인터**: 위 `## 1/11:35`~`## 4/12:05` · 메모리 `subagent-transcript-metrics`

## 6/14:35 feature-draft + implementation — 커버리지 델타 후속 정정 4건

- **무엇/왜**: 머지된 커버리지 델타 절의 사후 감사에서 나온 정합 갭 4건 처리. 사용자가 "별건 처리하며 ON 회차 누적"을 택해 이 체인이 계측 vehicle을 겸한다.
- **plan-review Medium 5 전량 반영 (Blocker 0)**:
  - **F2 (설계 정정)**: 마감 §3에 포인터를 **추가**하려던 게 문제였다. §4 말미가 이미 순서를 서술하고 spec `main.md`도 마감 순서를 열거해, 포인터를 더하면 같은 순서 명제가 **세 곳**에 남는다. → 소유권을 표면 역할로 갈라 §4는 **적용 대상**만, 순서는 집행 주체인 **마감 §3**이 소유하도록 **이동**(추가 아님). spec 마감 순서 항도 전파 대상으로 Part 1에 명시.
  - **F1**: AC1이 "불릿 2개 이상"이라 전형적 델타 예시만 떼어내도 통과했다 → 앵커별로 `^- ` 줄에 있는지 판정으로 교체.
  - **F3**: 새 삭제-재검증 의무에 범위가 없어 (c)까지 걸렸다 — 이 draft가 고치려는 "(c) 미정의 적용" 결함을 새 조항에 다시 심는 셈 → (a)/(b) 한정 명시.
  - **F4**: 새로 쓰는 문장 3개의 AC가 "취지의 문장" 독해 판정이었다 → 리터럴 앵커 고정.
  - **F5**: (c) 대체 문장이 오해 부정만 담으면 열거 기준이 사라져 순손실 → "도달 테스트 0개 → diff 동작 전량"을 AC에 포함.
- **검증**: RED 11 FAIL → GREEN 19 PASS / 0 FAIL. 변이 7건 전량 검출 후 복구·재실행 통과.
- **RED 단계에서 위장 통과 성분 2건 적발**: 앵커 `다시 실행해 통과를 재확인`이 **변이 확인 문장에 이미 존재**했고, `(a)/(b)`도 기존 Triage 문장에 있었다. 둘 다 "새 문장의 앵커가 기존 문장과 겹치는지"를 안 본 탓 — 새 check의 앵커는 기존 본문 grep으로 유일성부터 확인해야 한다.
- **커버리지 델타가 실제 결함 1건을 잡았다 (§4의 두 번째 실사용)**: Task 3에서 마감 §3 불릿을 고치며 `fix가 있었으면 회귀를 1회 재실행` → `fix로 구현이 바뀌었으면 … 회귀를 1회 재실행`으로 **조건을 옮겨, 문서·테스트만 고친 fix가 회귀를 건너뛰게** 됐다. AC는 포인터 리터럴 존재만 봐서 이 조건 이동을 못 본다. → 회귀 의무는 fix 전체에 두고 델타 적용만 조건부로 분리. D2 check으로 고정.
- **델타 2건 모두 (b)로 닫음**: D1 삭제 후 증거 시점 규정 · D2 회귀 의무 범위.
- **배칭 누적 관측**: 이 체인의 plan-review 회차(ON)가 **호출 12 / 여지 12 / 실현 2** — 규칙 발효 후 첫 배칭 관측이다. 단 `plan-review`는 규칙 OFF에서도 2를 냈던 agent라 단독 결정력은 없다.
- **포인터**: draft `_sdd/drafts/2026-07-31_feature_draft_coverage_delta_followups.md` · check `scratchpad/check_followups.sh`

## 7/14:38 마감 + spec-sync — 커버리지 델타 후속 정정 4건

- **게이트 Must 4 → fix 4**: correctness M1 + simplicity M1이 같은 줄을 지목했다 — 순서 중복은 없앴는데 **적용 명제가 §4와 마감 §3에 복제**됐고, `재실행하고 … 그 전에`라는 **역참조**라 순차 집행자가 회귀를 먼저 돌 여지가 있었다(회귀 1회 규약상 옛 상태 측정). 선형 한 문장으로 합침. correctness M2는 draft 전파 대상에서 `components.md`가 빠져 **폐기된 척도가 spec에 canonical로 남을 뻔한 것** → 보강. simplicity M2는 삭제 문단에 문장을 끼우며 바로 위에서 푼 압축을 아래에 재생산한 것 → 불릿화.
- **계약 오류 선언 1회 (§5)**: check 2개가 "한 문단 = 한 줄"을 전제로 앵커를 잡아, 게이트 fix가 줄을 쪼개자 깨졌다. 문면 리터럴이 아니라 **의도**(회귀가 구현-변경 조건에 갇히지 않는다 / 재확인이 (a)/(b) 한정)를 검사하도록 교정하고 RED 재관찰(`델타가 회귀보다 뒤`) 후 구현 복귀. 같은 task에서 반복 없음.
- **fix 후 회귀**: 19 PASS / 0 FAIL, 변이 누적 10건 전량 검출, `git diff --check` clean, 미러 identical.
- **spec-sync**: 4.6.19→**4.6.20**. `main.md` §2 두 항목(커버리지 델타 하위·마감 순서 항 — 순서 명제 단일 소유자 명시), `components.md` `implementation` 행 두 문구, `decision_log` 신규 entry(30/0), changelog v4.6.20(6/0). **폐기 척도 live 잔존 0건**, 배칭 `🚧 Planned` 항목 무변경.
- **잔존 advisory**: simplicity Low(§4의 diff 근거 3중 서술 — Task 1 계약이 "문장 삭제 없이 배치만"이라 범위 밖) · 증거 테이블 델타 행 `AC` 열 표기 관례 미정(존치).
- **배칭 누적 (ON 3회 추가)**: plan-review 여지 12→실현 2, correctness 여지 2→1, simplicity 여지 4→1. 누적 OFF 8회 중 2회 / ON 7회 중 1회.
- **포인터**: `_sdd/spec/decision_log.md` 2026-07-31 세 번째 entry · 위 `## 6/14:35`

## 8/14:50 feature-draft + implementation — 배칭 규칙에 발화 시점 1문장

- **무엇/왜**: 배칭 Hard Rule이 추상 원칙만 서술해 매 호출마다 독립성을 판정해야 했고, 발효 후에도 배칭이 안 늘었다. 구체적 발화 시점 1문장을 덧붙인다. 사용자가 "효과 미확인이지만 일단 넣고 확인"을 명시 선택.
- **plan-review High 1 (내 사실오인) + Medium 4 전량 반영**:
  - **H1**: "`기준 문서`·`변경 파일`은 5종 공통"이라고 **단언했는데 실측이 반증**했다 — `기준 문서`는 `implementation-review`에만(9건), `변경 파일`은 impl 1·simplicity 1·pr 2건이고 `plan-review`(구현 전 리뷰)·`spec-sync`엔 0건. 리뷰어가 제안한 대안 `호출자 지정`도 pr-review·spec-sync에 0건이었다. → **5종이 공유하는 건 어휘가 아니라 구조**(모두 범위/입력을 정하는 Step 1을 갖는다). 리터럴을 빌리지 않고 그 구조를 가리키는 문장으로 재설계: `범위를 정하는 첫 단계가 끝나면, 그때 확정된 파일들을 첫 메시지에서 한꺼번에 읽는다.`
  - **M1**: 초안 앵커 `이미 읽기로 정해진 파일`이 기존 규칙 마지막 절 `이미 읽기로 결정한 호출`을 4줄 안에서 재서술 → 앵커를 시점 정보만으로 교체.
  - **M2**: claude 쪽에 위치 AC가 없어 새 문장을 4요소 **중간**에 끼워도 전부 통과했다(5파일이 함께 틀리면 바이트 동일성도 통과) → codex AC3와 대칭인 위치 AC 추가.
  - **M3/M4**: 미래 판정이 falsifiable하지 않았다 → 종료 조건을 명문화(관측 대상 = 여지 ≥ 3인 simplicity 시작 구간 / 지지 = 무관 과제 2회 실현 ≥ 2 / **반증 = 여지 ≥ 3인 회차 3회 연속 실현 1이면 문장 제거** / 여지 < 3 회차는 표본 제외).
- **계약 오류 선언 1회 (§5)**: AC6이 "삭제 라인 0"을 요구했으나 배칭 규칙은 번호 항목 **한 줄**이라 문장을 붙이면 `-1/+1`이 필연이다. 의도("기존 규칙 훼손 0")대로 교정 — 처음엔 "삭제 줄이 추가 줄의 접두사"로 했다가 **codex는 설계상 예시 문장 앞에 끼우므로 접두사 검사가 성립하지 않아** 다시 "새 문장을 제거하면 원문과 일치"로 재교정. 양 진영 공통 판정이 됐다.
- **검증**: RED 26 FAIL → GREEN 77 PASS / 0 FAIL. 변이 7건 전량 검출(허가형 어미·agent 구조 이름 사용·4요소 중간 삽입·codex 1파일 누락·codex 예시 뒤 배치·진영 간 대응 깨짐·문장 중복).
- **커버리지 델타 2건**: D1 **진영 간** 대응(codex = claude + 예시) — 진영 **내** 바이트 동일성만으로는 claude만 한 단어 늘어도 못 잡는다. D2 새 문장이 파일당 정확히 1회(다른 규칙으로 누출 방지). 둘 다 변이로 판별력 증명.
- **판정 부담 가설 관측 설계**: 이번 게이트의 `simplicity-review` digest에 읽을 파일 3개를 명시했다. 문면 변경 0 상태(설치본은 구 본문)라 규칙과 독립적으로 "판정 부담이 최소면 묶는가"만 본다.
- **포인터**: draft `_sdd/drafts/2026-07-31_feature_draft_batching_concrete_moment.md` · check `scratchpad/check_moment.sh`

## 9/15:27 배칭 발화 시점 feature 폐기 (revert)

- **무엇/왜**: 위 `## 8`의 구현(agent 10파일 2안 문장)을 미커밋 상태에서 전량 revert하고 draft를 삭제했다. 근거 둘: (1) plan-review BLOCKED — 트리거 문장의 관찰 가능 절이 기존 요소 (i)의 재서술이고 신규분(`먼저 확인하고`)은 관찰 불가한 심의 단계라 세 번째 시도도 같은 실패 형태(H3), (2) 사용자 재정의 — 병목은 파일 읽기(I/O)가 아니라 **분석(추론)의 직렬성**이므로 tool call 배칭은 비용의 소수만 겨냥한다. 배칭 누적 관측(OFF 1.8% / ON 2.9%, 총 6건)도 판정 불가 수준.
- **결과**: working tree clean(agent 10파일 원복, draft 삭제), 빈 브랜치 `feat/batching-concrete-moment` 삭제. 기존 배칭 Hard Rule(4.6.18 발효분)은 무변경 존치, spec `🚧 Planned` 누적 관측 항목도 존치.
- **후속**: 다음 레버는 correctness reviewer의 task 단위 분할(1+N 병렬 dispatch, 게이트 벽시계 max(shard, simplicity 177s) 상한) — 다음 2-task feature에서 파일럿 1회 실측 예정.
- **포인터**: 위 `## 8` · `_sdd/spec/main.md` 배칭 Planned 항목

## 10/16:15 feature-draft + implementation — implementation-review correctness task-shard 분할 (1+N)

- **무엇/왜**: 게이트 벽시계 = correctness 단독(214~430s 실측, simplicity는 병렬 그늘). correctness 시간 분해(검증 루프 65~80% + 리포트 19~35%)가 둘 다 task 경계로 나뉘므로, orchestrator SKILL에 task별 shard 분할 dispatch(1+N) 계약을 넣었다. simplicity는 통짜 유지(중복 렌즈는 두 지점 동시 관찰 필요). 미러는 3-way(codex는 spawn_agent 어댑터 보존), docs 가이드 ko/en 각 1줄 갱신.
- **plan-review High 1 반영**: census가 `docs/AUTOPILOT_GUIDE.md` ko/en의 "2-reviewer" 서술을 놓침(내 grep이 docs/ 미포함) → docs를 Task 3으로 편입, census를 repo-wide로 확장. Low: 파일럿 중간 밴드 판정 명시.
- **계약 오류 선언 1회 (§5)**: Task 4 census가 "grep 가시 = repo 표면"을 전제 → `_sdd/implementation/`은 gitignore 로컬 전용이라 git 추적 파일로 한정. check 결함 교정 1회: markdown 불릿 삭제 줄을 diff header 패턴이 삼킴 → header 리터럴만 제외.
- **작업 사고 1회**: 변이 복원에 `git checkout --`을 사용해 미커밋 구현이 HEAD로 원복됨 → 컨텍스트에서 동일 재적용. 이후 변이 복원은 cp 백업 방식(교훈: 미커밋 작업의 변이 테스트는 사본 복원).
- **검증**: RED(17 FAIL 관찰) → GREEN 17 PASS → fix 후 18 PASS(델타 check D1 포함). 변이 7종 전량 검출(고정 표기 재도입·연접 제거·고정 2-id 복원·docs 원복·분할 조건 약화·docs 초과 수정·call 문법 재기재).
- **게이트 = 파일럿 실측 (핵심 결과)**: correctness shard A(T1+T3) 70s ∥ shard B(T2+T4) 143s ∥ simplicity 120s, 한 메시지 동시 dispatch. **게이트 벽시계 143s, shard 합 213s ≈ 동급 단독 correctness 214s** — 일의 총량 보존 + 벽시계 단축(비례 분배 지지, 220s 밴드 통과). finding: simplicity Medium 1(codex call 문법 이중 기재+따옴표 drift 실증) → fix로 Runtime Adapter 블록 단일 소스화, correctness Low 1(따옴표 불일치)은 fix에 흡수. shard A/B finding 0·전 AC MET.
- **잔존 advisory (Low, 미반영)**: 인트로 shard 예고 문장은 실행 1 요약 재기재(취향) · codex step 2 경로-불명확 문장 분리 제안은 fix로 이미 별도 불릿화됨.
- **포인터**: draft `_sdd/drafts/2026-07-31_feature_draft_correctness_task_shard.md` · check `scratchpad/check_shard.sh` · spec-sync는 후속 항목

## 11/16:18 spec-sync — correctness task-shard 반영 (v4.6.21)

- **무엇/왜**: 위 `## 10` 구현의 spec 동기화. 직교 2-렌즈 적용 지점을 PR review + implementation 게이트 2곳으로 정정(stale이던 "현재 적용 지점은 PR review" 제거), implementation-review의 1+N 계약 승격, components 행 orchestrator 재분류.
- **결과**: `main.md` 4.6.20→**4.6.21**(+5/-4), `components.md`(+2/-2), decision_log 신규 entry(24/0)·changelog v4.6.21(6/0) append-only. 파일럿 실측(shard 70s∥143s∥120s, 벽시계 143s, 합 213s≈단독 214s)은 decision_log Rationale에 보존. draft는 `_processed_` rename 완료. plan-review 2-렌즈 분할은 Planned 승격 없이 Follow-up 기록만(Scope Out 준수).
- **포인터**: `_sdd/spec/decision_log.md` 2026-07-31 entry · changelog v4.6.21 · 위 `## 10`

## 12/16:45 feature-draft + implementation — plan-review 2-렌즈 분할 (실측 ∥ 판단)

- **무엇/왜**: plan-review는 유일한 무그늘 단독 리뷰(5회차 257~347s, 리포트 54~68%). task 축은 성립 안 함(finding 9건 중 task 내부 닫힘 0건 실측) → 렌즈 축: agent에 `호출자 렌즈 한정` 절(실측=Step 3 계단+VW+사실 대조, 판단=나머지 5 smell+규모 판정+Step 4), SKILL은 2-dispatch 병합 orchestrator화. 새 agent 없음(동일 agent 렌즈 한정 2회 — 등록 census 리스크 회피).
- **plan-review High 1 반영**: AC2(문구 일반화)↔AC3(삭제 0줄)이 정상 구현 통과 불가한 모순 쌍 → AC3를 "삭제 줄이 일반화 대상 계열 한정"으로 교정. Medium(판단 렌즈 UNKNOWN seam) → 소유 경계 닫힘 계약(실측이 사실 전제 소유, 판단은 가정 판정·UNKNOWN 금지). Low 3건 반영(Contracts 참조화·Model override 앵커·grep -F).
- **check 결함 교정 1회**: `grep -c`(매치 0→exit 1) × pipefail이 정상 0건을 실패로 오판 → 계수 캡처로 교정.
- **검증**: RED(11 FAIL 관찰) → GREEN 18 PASS → fix 후 21 PASS(델타 D1 교차 포인터·D2 AC 조건화·D3 payload 단일 소스). 변이 9종 전량 검출.
- **게이트 = 1+N 첫 정식 런 (핵심 실측)**: correctness shard 122s ∥ 81s ∥ 101s + simplicity 107s, 한 메시지 4-agent — **게이트 벽시계 122s** (shard 합 304s). 파일럿(143s) 재현+개선. finding: shard1 Medium(AC2/AC3 무조건 문구 ↔ 렌즈 절 내부 모순 — Final Check가 실측 렌즈에 판단 일 수행 유인) → 소유 렌즈 적용 문장 fix. simplicity Medium(codex payload 스펙 이중 기재+Mode/review mode drift — 지난 문법 단일화의 payload 판) → 어댑터 블록 상세형 단일 소스화. shard2·3 finding 0.
- **spec 모순 발견**: `main.md:62` "해소 수단은 fan-out이 아니라 턴 접기다" guardrail이 fan-out 계열 결정들과 모순 후보 — spec-sync에 정정/supersede 인계. `usage-guide.md` 표면(41·47·52)도 shard 3 census가 신규 발견.
- **잔존 advisory (Low)**: 판단 렌즈 "나머지 5 smell" 비열거(유일 해석) · AC1 괄호의 렌즈 절 재서술(자기완결성 취향) · 실측 렌즈의 타 smell 사실 반증 finding은 VW 행으로 낸다(기존 H1 관례) — simplicity 전달 관측에 대한 해석.
- **포인터**: draft `_sdd/drafts/2026-07-31_feature_draft_plan_review_two_lens.md` · check `scratchpad/check_twolens.sh` · spec-sync 후속 항목

## 13/16:49 spec-sync — plan-review 2-렌즈 반영 (v4.6.22)

- **무엇/왜**: 위 `## 12` 구현의 spec 동기화 + **fan-out guardrail supersede** — `main.md:62` "해소 수단은 fan-out이 아니라 턴 접기다"를 "턴 접기(배칭) + read-only reviewer fan-out"으로 정정(구 문면 인용 보존, 중첩 fan-out 비대상 유지).
- **결과**: main.md 4.6.21→**4.6.22**(2-렌즈 게이트 불릿 신규·단일 패스 단서·결정 행 3곳·1+N 첫 정식 런 122s evidence), components.md(plan-review 행 orchestrator화, wrapper-backed 잔존 = spec-sync만), usage-guide.md Scenario 2, decision_log·changelog append-only entry. 🚧 Planned: 2-렌즈 벽시계 효과(판정 밴드 ≤~220s 지지 / ~300s+ 재고) — 첫 관측은 다음 draft의 plan-review 게이트.
- **계측**: 이 spec-sync 회차 445s(도구 32회) — 세션 최장. spec-sync 시간 분해 실측(4회차): 분석 ~35% / 집필 ~50% / 리포트 ~12%, 쓰기 파일 4개는 서로소 → F3(spec-sync 분할) 후보 근거.
- **포인터**: `_sdd/spec/decision_log.md` 2026-07-31 entry · changelog v4.6.22 · 위 `## 12`

## 14/17:15 feature-draft + implementation — simplicity 차원 묶음 분할 (참조 ∥ 국소)

- **무엇/왜**: 게이트의 다른 구간이 내려간 뒤 simplicity(107~252s, 리포트 56~96%)가 임계 경로 후보. 분할 축은 차원 — **한정은 차원이지 범위가 아니라** 각 shard가 전체 변경을 봐서 중복 렌즈 보존(4.6.21 task 축 반대 논거와 공존). agent에 `호출자 차원 한정` 절(참조=중복·죽은 코드·단일 사용처 추상화 / 국소=도달 불가 에러 처리·과잉압축), impl-review SKILL은 묶음마다 1회 dispatch(N+2). pr-review는 후방 호환 경로로 무변경.
- **plan-review 게이트 = 2-렌즈 첫 실측**: 실측 렌즈 320s(도구 34회) ∥ 판단 렌즈 86s(도구 1회) — **max 320s, 재고 밴드(~300s+) 진입**. 렌즈 불균형 리스크 실현(적을 양이 실측 렌즈에 집중). 단 품질 상승: High 1(무조건 "5개" 문면 3곳+Integration stale — 내 draft가 재발 방지를 표방한 바로 그 실수) + Medium 2(wait 단수 실측 불일치·위장/불가 앵커) 검출. n=1, 판정은 사용자 보고 후.
- **draft 반영**: High(무조건 문면 전수 일반화 편입) + M2(wait 복수화) + M3(위장 앵커 `합집합` 금지·codex `한 메시지` 부재 앵커 교체) + Low(출처 병기) + 자체 발견 1건(AC3 삭제 필터가 Integration 삭제와 모순 — F1과 같은 클래스).
- **check 도구 결함 교정 2회**: 섹션 추출 awk가 동일 앵커 포함 AC1 줄에서 조기 시작·종료(메모리의 추출기 함정 재현) → `^##` 헤더 앵커로 / wait 단수 잔존 패턴이 실재하지 않는 인용형(위장 앵커) → 무인용형으로.
- **검증**: RED 관찰 → GREEN 18 PASS → fix 후 20 PASS(델타 D1 교차 포인터·D2 근거/payload 재기재 0). 변이 8종 전량 검출.
- **게이트 = N+2 완전체 첫 런 (핵심 실측)**: correctness 103/85/110 + simplicity 참조 164 ∥ 국소 91 — **게이트 벽시계 164s**. simplicity 합 255s ≈ 통짜 252s(총량 보존 3번째 재현). **적을 양 불균형 실측**: finding이 중복 차원 집중 → 참조 shard가 시간·리포트 우세(사용자 관찰 "적어야 할 양이 시간을 결정" 그대로). finding: 참조 Medium 2(근거 문장 4벌 복제 — draft 계약 "근거 계승"에서 의도 이탈로 fix, 기록 / codex payload bullet 이중 기재 재발) → 단일 소스화 fix. 국소·shard 2·3 finding 0, Low 4건 advisory.
- **잔존 advisory (Low)**: Severity Rules의 4번째 무조건 "5개"(변형형 — 후속 일반화 후보) · 도입부 요약 재서술 · "이 절은 호출 형태를 넓힐 뿐" 방어 재천명 · (fix로 흡수됨: codex 수거 괄호 표기).
- **포인터**: draft `_sdd/drafts/2026-07-31_feature_draft_simplicity_dimension_shard.md` · check `scratchpad/check_dimshard.sh` · spec-sync 후속 항목

## 15/17:16 spec-sync — simplicity 차원 묶음 분할 반영 (v4.6.23)

- **무엇/왜**: 위 `## 14` 구현의 spec 동기화. "simplicity 통짜 1회" 서술 3곳을 묶음 2회(impl-review) / 통짜 1회(pr-review 후방 호환) 구분으로 갱신, N+2 게이트·합집합 relay 승격, 첫 런 실측(164s, 합 255≈252 총량 보존) evidence 기록.
- **plan-review 2-렌즈 첫 관측 기록**: 4.6.22 Planned 항목에 max 320s(재고 밴드, n=1) 관측 추가 — 판정은 확정하지 않고 사용자 결정 대기로 명시.
- **결과**: main.md 4.6.22→**4.6.23**, components.md 2행, decision_log·changelog append-only entry(묶음 재배분은 Follow-up만).
- **포인터**: `_sdd/spec/decision_log.md` 2026-07-31 entry · changelog v4.6.23 · 위 `## 14`
