# Work Log — 2026-08-05

## 1/12:12 feature-draft·implementation 반복 finding 원인 진단

- **무엇/왜**: Claude Code Opus 5 사용 시 `feature-draft`·`implementation` 산출물에서 리뷰 finding이 많이 발생하는 원인을 diagnose-only로 점검했다. 현재 Claude 스킬·reviewer agent 계약과 최근 work log의 실제 finding 사례를 대조했다.
- **결과**: 모델 고유 회귀는 모델명이 붙은 충분한 표본이 없어 `UNCONFIRMED`. 구조적 원인은 확인됨 — producer↔reviewer 요구 비대칭(Open Questions 산출 계약보다 reviewer의 decision/assumption 요구가 넓음), producer보다 넓은 reviewer 탐색 범위, implementation의 다단계 자연어 상태 관리, 단일 패스 후 fix 1회·무자동 재리뷰, correctness shard 중복 finding 무dedup 합산. 최근 로그의 실제 결함(전파 surface 누락, 약한 census, live 값 미실측, 계약 어절 유실)과 일치한다.
- **포인터**: `.claude/skills/feature-draft/SKILL.md`, `.claude/skills/implementation/SKILL.md`, `.claude/skills/implementation-review/SKILL.md`, `.claude/agents/plan-review-agent.md`, `.claude/agents/implementation-review-agent.md`, `_sdd/work_log/2026-07-29.md`, `_sdd/work_log/2026-08-01.md`

## 2/12:25 producer-review 계약 정렬·implementation ledger 토론

- **무엇/왜**: 앞선 진단의 개선안 1~3 적용 여부와 최소 설계를 `discussion` 스킬로 11라운드 논의했다. producer-review 정렬, propagation surface, compact-safe implementation 상태 외부화의 범위와 부작용을 비교했다.
- **결과**: 2단계 도입 확정 — Feature A는 중요 결정만 producer↔reviewer 계약을 동일화하고 동일 변경 요소가 둘 이상의 동기화 표면에 걸릴 때만 전역 propagation 표를 둔다. Feature B는 모든 implementation 실행에 `_sdd/implementation/` resume-only 단일 ledger를 만들고 `READY → RED_CONFIRMED → GREEN_CONFIRMED → DELTA_CLOSED` 네 상태와 최소 포인터만 기록한다. 완료 기준은 구조 검증만이며 실제 finding 감소 관찰은 범위 밖이다. 인스코프 미결 0건.
- **포인터**: `_sdd/discussion/2026-08-05_discussion_producer_review_alignment_and_ledger.md`

## 3/12:37 Feature A draft + plan-review 게이트

- **무엇/왜**: 브랜치 `feat/producer-review-contract-alignment`에서 Feature A의 producer-review 중요 결정 계약 정렬과 조건부 propagation 표를 Claude/Codex 양쪽 및 한·영 정의 문서에 적용할 draft를 작성했다. 전파형 신규 계약이라 full SDD 경로를 채택했다.
- **결과**: 3 tasks·6 implementation files·2 propagation rows로 단일-context 적격. plan-review 2-렌즈 단일 패스 결과 High 2·Medium 2·Low 0. High는 광범위 `rg`가 전수성을 증명하지 못한 census oracle과 runtime 차이를 포함한 `semantic parity`의 비반증성, Medium은 중요 결정 3건 누락과 read-only task의 mutation 경계 모호성이었다. fix 1회로 exact live-root 기대 집합, `PROD1~5`·`DOC1~3`·`REV1~4` 유한 assertion matrix, runtime 제외 경계, 저장소 밖 임시 복사본 mutation, D3~D5 5필드를 반영했다. fix 전 합산 C+H=2·M=2라 추가 plan-review 권고 임계값 미만이다.
- **포인터**: `_sdd/drafts/2026-08-05_feature_draft_producer_review_contract_alignment.md`

## 4/12:59 Feature A implementation — producer·reviewer 계약 정렬

- **무엇/왜**: draft 3 tasks를 RED→GREEN으로 구현했다. Task 1은 Claude/Codex `feature-draft`와 한·영 `SDD_SPEC_DEFINITION`, Task 2는 Claude/Codex `plan-review-agent`, Task 3은 exact live-root census와 구조 assertion/mutation 검증이다.
- **결과**: Task 1 RED 14누락·Task 2 RED 8누락을 exit 1로 선관찰. producer에 조건부 `Decisions and Assumptions` 5필드와 `Propagation Surfaces` 5열·단일 owner/Task 연접을 추가하고, reviewer가 동일 조건을 기존 `Verification Weakness`와 Glob→Grep→Read 계단에서 검사하도록 정렬했다. docs 한·영 미러 갱신. GREEN은 producer byte parity·TOML parse·`PROD1~5`·`DOC1~3`·`REV1~4`·exact census 3종·AUTOPILOT_GUIDE 무변경을 통과했다. task diff 재독 결과 AC 밖 동작 delta 없음. check 구문 오류 2건(outer `done` 누락, 백틱 command substitution)과 macOS temp prefix 가정 오류 1건은 assertion을 바꾸지 않고 명령만 교정했다.
- **포인터**: `.claude/skills/feature-draft/SKILL.md`, `.codex/skills/feature-draft/SKILL.md`, `.claude/agents/plan-review-agent.md`, `.codex/agents/plan-review-agent.toml`, `docs/SDD_SPEC_DEFINITION.md`, `docs/en/SDD_SPEC_DEFINITION.md`

## 5/12:59 Feature A implementation-review + fix 1회

- **무엇/왜**: runtime 4-slot 상한 때문에 correctness 3-shard → simplicity 2묶음의 두 read-only wave로 동일 단일 패스를 수행했다(판정 범위·reviewer 수 무축소).
- **결과**: correctness Task 1·2 전 AC MET/finding 0, Task 3 High 1 — 최초 mutation은 anchor 소멸만 확인해 canonical checker의 mutant non-zero를 증명하지 못했다. simplicity 참조 3차원 finding 0, 국소 과잉압축 Medium 2. fix 1회로 canonical checker를 함수화해 정본 12/12 PASS + 각 mutant에서 동일 assertion checker 12/12 non-zero를 직접 확인했고, producer·docs·reviewer의 propagation 규칙을 판정 단위 하위 목록으로 펼쳤다. DOC3 checker의 영어 문장 첫글자 대문자 가정 오류 1건은 case-insensitive 의미 검사로 선언·교정. fix diff coverage delta는 계약 의미 불변 형태 변경뿐이며, post-fix 회귀(TOML·byte parity·exact census·AUTOPILOT 무변경·diff check) PASS. fix 전 합산 C+H=1·M=2라 추가 implementation-review 권고 임계값 미만이다.
- **포인터**: 위 draft Task 1~3 AC ledger, implementation-review correctness/simplicity 경량 반환(세션), 이 로그 `## 4`

## 6/13:05 Feature A spec-sync v4.6.33

- **무엇/왜**: 구현 evidence를 기준으로 Feature A의 live truth를 canonical spec에 동기화했다. 충돌을 피하도록 main/components 본문과 append-only decision/changelog 기록을 writer-disjoint로 나눠 반영했다.
- **결과**: `_sdd/spec/main.md`를 v4.6.33(2026-08-05)으로 올리고 producer-review 중요 결정 5필드 정렬과 조건부 propagation 5열·단일 owner 계약을 guardrail에 추가했다. `components.md`의 feature-draft·plan-review 역할을 갱신하고, decision log와 changelog는 삭제 없이 각각 24줄·5줄 append했다. 처리 완료 draft는 `_processed_` 접두로 rename했으며 usage guide는 변경하지 않았다. version 일치·append-only·draft 상태·`git diff --check` 검증을 통과했다.
- **포인터**: `_sdd/spec/main.md`, `_sdd/spec/components.md`, `_sdd/spec/decision_log.md`, `_sdd/spec/logs/changelog.md`, `_sdd/drafts/_processed_2026-08-05_feature_draft_producer_review_contract_alignment.md`

## 7/14:50 D&A 5필드 계약 제거 — 풀 체인 (draft→plan-review→구현→review→spec-sync v4.6.34)

- **무엇/왜**: 오전 v4.6.33이 도입한 두 계약 중 `Decisions and Assumptions` 조건부 5필드만 사용자 결정으로 제거하고 producer·reviewer·정의 문서를 99a6bd5^ 산문 수준으로 복귀했다(`Propagation Surfaces`는 실측 실패 이력 기반이라 유지). D&A는 실패 이력 없는 형식 추가였고 알맹이(사용자 확인 필요 결정 노출)는 기존 `Open Questions`가 담당하며, 5필드는 형식 준수 검사로 퇴화하기 쉽다는 판단. 계약 제거라 경량 경로 부적격 → 풀 체인.
- **결과**: draft 4 tasks(6파일 + census)·propagation 3행 — 이번에 만든 `Propagation Surfaces` 표가 첫 실전 발동. plan-review 2-렌즈 C+H 0·M 2("smell 7/8" 오라벨→Hard Rule 7/8 정정, census `중요 결정` 범위 한정)·L 1, fix 1회 반영. 구현은 structural check RED 45 FAIL 선관찰 → GREEN 70/70(exit 0), 커버리지 델타 1건(en 재번호 check)은 변이 확인 1/1 kill로 닫음. producer 미러 byte parity·TOML parse·census 계약 고유 리터럴 0건(허용 예외: reviewer Step 4 헤딩 2건, 99a6bd5^ 원문). implementation-review 6 reviewer(correctness 4 shard + simplicity 2묶음) finding 0. spec-sync 본문∥기록 분할로 main.md v4.6.34 bump + guardrail을 propagation 단독 계약으로 재서술, components.md 2행 D&A 문구 제거, decision_log·changelog append-only entry, draft `_processed_` rename. 사후 정합(버전 일치·기록 삭제 줄 0) 통과.
- **포인터**: `_sdd/drafts/_processed_2026-08-05_feature_draft_decisions_assumptions_contract_removal.md`, `_sdd/spec/main.md`, `_sdd/spec/components.md`, `_sdd/spec/decision_log.md`, `_sdd/spec/logs/changelog.md`

## 8/15:30 Feature B: resume-only implementation ledger — 풀 체인 (spec v4.6.35)

- **무엇/왜**: 토론 결정 6~9의 Feature B를 사용자 조정 2건(① ledger = 마감 AC→증거 테이블의 점진 작성본·기록처 통합 ② 재개 시 미완료 task 무조건 fresh 재판정)과 "재실행으로 복원 불가한 사실만 기록" 기준으로 구현했다. compact로 인한 implementation task 상태 유실 통로를 닫는 resume pointer이며, 감사 로그가 아니다. 브랜치 feat/implementation-ledger.
- **결과**: draft 3 tasks·propagation 3행(SKILL 2벌·SPEC_DEFINITION 한영·AUTOPILOT_GUIDE 한영), plan-review C+H 0·M 1(AC4 절-범위 마스킹→문장-범위 앵커)·L 2 fix 반영. 구현 RED 24 FAIL → GREEN, 델타 check 4건(en 재번호류 아님 — slug·갱신 시점·Review fix delta·이어쓰기) 변이 3회 kill. implementation-review 6 reviewer M 3(이어쓰기 규칙 부재·마감 통합 이중 정의·기록 기준 과잉압축) fix 1회 반영, post-fix 회귀 35/35 exit 0, Low 5 advisory 잔존. 계약 오류 선언 1회(check selector 앵커 — assertion 불변 교정). spec-sync 본문∥기록: main.md v4.6.35 + guardrail ledger 계약 승격 + 도입 관측 exit 조건 Planned 항목(실사용 전무 시 회수 재검토 — D&A 회수와 동일 기준), components.md·usage-guide.md 갱신, decision_log·changelog entry, draft `_processed_` rename. 사후 정합(버전 일치·삭제 줄 0) 통과.
- **포인터**: `_sdd/drafts/_processed_2026-08-05_feature_draft_implementation_ledger.md`, `.claude/skills/implementation/SKILL.md`, `.codex/skills/implementation/SKILL.md`, `docs/SDD_SPEC_DEFINITION.md`, `docs/en/SDD_SPEC_DEFINITION.md`, `docs/AUTOPILOT_GUIDE.md`, `docs/en/AUTOPILOT_GUIDE.md`, `_sdd/spec/main.md`

## 9/19:58 work log 게이트 수치에 실행 모델명 병기 (경량 경로, fable-5)

- **무엇/왜**: 모델 회귀 가설("Opus 5 잔실수가 많다")이 8/5 진단에서 UNCONFIRMED로 남은 원인이 게이트 기록에 실행 모델 표본이 없어서였다 → 하네스 §5에 "리뷰 게이트 결과 수치 기록 시 실행 모델명 병기(예: `plan-review (opus-5): H2 M2`)" bullet 1줄 추가. 표면 5곳(AGENTS.md + spec-create/spec-upgrade 하네스 템플릿 claude·codex 4미러) 동일 문면 — 소비 repo에도 적용.
- **경량 경로 판정**: 새 contract/invariant 없음(기존 §5 항목 필드 확장) · 신규 파일 없음 · 전파 표면 5곳 전수 열거 + §5 블록 md5 일치/diff 검증.
- **결과**: 5표면 각 +1줄, §5 md5 재일치, `git diff --check` 통과. spec-sync 본문∥기록 분할 dispatch — main.md v4.6.35→v4.6.36(본문 서술 무변경 판단: §5 항목 필드를 재서술하는 spec 표면 없음), decision_log·changelog append-only entry, 사후 정합(버전 일치·삭제 줄 0) 통과.
- **포인터**: 브랜치 `chore/worklog-model-tag` · decision_log 2026-08-05 "work log 게이트 수치에 실행 모델명 병기"

## 10/20:06 work log 모델명 병기를 전 항목으로 일반화 (fable-5)

- **무엇/왜**: 직전 항목(9)의 게이트 한정 병기로는 draft·구현 등 producer 단계의 모델 귀속이 유실된다(사용자 제안) → §5 항목 형식을 `## <순번/HH:MM> <제목> (<실행 모델명>)`로 확장하고, 게이트 한정 bullet을 "모든 항목 제목 병기 + 다른 모델로 돌린 게이트 수치 옆 병기"로 대체. 표면 5곳(AGENTS.md + 하네스 템플릿 4미러) 동일 문면, 파일당 +2/-2.
- **경량 경로 판정**: 새 contract/invariant 없음(기존 규칙 범위 일반화) · 신규 파일 없음 · 전파 표면 5곳 전수 열거 + §5 블록 md5 일치/`git diff --check` 검증.
- **결과**: spec-sync 본문∥기록 분할 — main.md v4.6.36→v4.6.37(본문 무변경, 재서술 표면 0 재확인), decision_log·changelog append-only entry, 사후 정합(버전 일치·삭제 줄 0) 통과.
- **포인터**: 브랜치 `chore/worklog-model-tag` · decision_log 2026-08-05 "work log 모델명 병기를 전 항목으로 일반화"

## 11/20:45 review agent 장기실행 watchdog 훅 실험 1 (fable-5)

- **무엇/왜**: review agent가 5분+ 돌 때 시간 도둑(예: `uv run --with torch` 반복 재설치)을 스스로 점검하게 하는 watchdog 훅의 성립 조건 검증. 실험 1 = 훅이 subagent tool call에 발동하는지 + agent 식별 필드가 오는지 — worklog-gate.sh(PreToolUse Bash 전체 matcher)에 stdin 로깅을 임시 삽입해 메인 루프 vs haiku subagent 각 1회 발동시켜 payload 대조 후 원복.
- **결과 (확정)**: subagent tool call에도 훅 발동 ○. subagent payload에만 `agent_id`(Agent 반환 agentId와 일치)·`agent_type` 존재 → per-agent 경과시간 추적 키 확보. `transcript_path`는 subagent에서도 메인 세션 jsonl(agent 전용 아님). 실험 2(PostToolUse `decision:block` reason의 subagent 전달) 자산 준비 완료 — `agent-watchdog-exp.sh`(오프라인 4케이스 PASS) + settings.local.json PostToolUse 등록(로컬 전용, 커밋 안 함). 훅 등록 스냅샷 특성상 CLI 재시작 후 검증.
- **포인터**: memory `agent-watchdog-experiment.md`(검증 절차·정리 규칙) · 확정 시 하네스 훅 자산 feature-draft 풀 체인 예정

## 12/20:48 review agent 장기실행 watchdog 훅 실험 2 — nudge 전달 성공 (fable-5)

- **무엇/왜**: 실험 1(항목 11)의 잔여 미검증 — PostToolUse `decision:block` reason이 subagent 모델에게 실제 전달되는가. CLI 재시작으로 등록 발효 후, haiku probe agent에 `sleep 20` 3회를 별도 tool call로 실행시켜 검증.
- **결과 (확정)**: 성공 — 46초 경과 시점(3번째 call 직후) nudge가 subagent에 전문 그대로 전달됐고, 모델이 지시(자평·캐시 점검)를 수행해 최종 응답에 포함했다. 계측 로그 발동 3회(0/+24/+46s)·start/nudge 상태 파일 정상. watchdog 훅의 성립 조건 전부 검증 완료 → 하네스 훅 자산화 feature-draft 풀 체인 준비됨. 정리: settings.local.json 등록 해제(스크립트 파일은 이 세션 스냅샷 에러 방지로 다음 세션 삭제), 실험 자산 커밋 안 함.
- **포인터**: memory `agent-watchdog-experiment.md`(확정 사실·설계 한계·잔여 정리)

## 13/21:05 Agent Watchdog 훅 feature-draft + plan-review 게이트 (fable-5)

- **무엇/왜**: 실험 1·2(항목 11·12)로 검증된 watchdog 훅의 하네스 자산화 draft 작성. 5 tasks(스크립트 5경로·SKILL 계약 4파일·self-host 등록·census)·Propagation Surfaces 4행, 규모 적격 판정.
- **결과**: plan-review 2-렌즈 C+H 0·M 1·L 3. Medium(census 패턴이 수사 변형 "셋 다"·"두 항목" 미커버 — rename-census 변형형 전례와 동일 패턴) → fix 1회로 패턴 확장 + P3 실측 count 6·6→7·7 정정. Low 3(관측용 nudge 문구·패턴 3곳 중복·T4 타 task 참조)은 advisory 수용. 재리뷰 권고 임계 미만.
- **포인터**: `_sdd/drafts/2026-08-05_feature_draft_agent_watchdog_hook.md` · 브랜치 `feat/agent-watchdog-hook`

## 14/21:40 Agent Watchdog 훅 implementation + implementation-review 게이트 (fable-5)

- **무엇/왜**: draft(항목 13) Task 1~5 실행 — agent-watchdog.sh 정본+5경로(THRESHOLD/COOLDOWN 300s·jq→python3 fallback·fail-open), spec-create/spec-upgrade SKILL 4파일 설치 계약 3→4개 확장, settings.json PostToolUse self-host 등록, census.
- **결과**: structural check RED 12 FAIL 선관찰 → GREEN 28/28 PASS(exit 0), 변이 7종 전량 kill(md5·census·settings·cooldown·임계 299s·start/last 오염 가드 — ⑧은 stderr assert 보강 후 kill). 커버리지 델타 3건을 check ⑦⑧⑨로 닫음. 게이트(correctness 5 shard + simplicity 2묶음, fix 전): C/H 0·M 1(도달 불가 줄바꿈 이스케이프 no-op)·L 5 → fix 1회로 치환 제거+주석 통합, 회귀 28/28 재PASS. Low 5건은 ledger advisory. 재리뷰 권고 임계 미만. 사고 1건: 변이 원복에 git checkout을 썼다가 구현 편집 소실 → count-assert 치환 스크립트로 재적용(이후 cp 백업 방식 전환).
- **포인터**: ledger `_sdd/implementation/2026-08-05_implementation_ledger_agent_watchdog_hook.md`(AC→증거 테이블 포함) · check `scratchpad/check_watchdog.sh`

## 15/21:52 Agent Watchdog 훅 spec-sync — v4.6.38 (fable-5)

- **무엇/왜**: 풀 체인 마지막 단계 — draft Part 1을 입력으로 global spec 동기화(본문∥기록 분할 dispatch).
- **결과**: main.md v4.6.37→v4.6.38 — 66행 guardrail 훅 자산 4개 확장 + 경고 의무의 강제/advisory 구분, 124행 harness layer에 advisory 자산 단서, components.md 52행 "훅 4개"·78행 정본 나열에 watchdog 추가, usage-guide.md 32행 설치 목록 갱신(본문 묶음이 targeted 확인에서 발견한 어긋남 포함). decision_log·changelog append-only entry, draft `_processed_` rename. 사후 정합(버전 일치·삭제 줄 0·rename) 통과.
- **포인터**: decision_log 2026-08-05 "Agent Watchdog 훅 — 하네스 훅 자산 4호 (advisory)" · changelog v4.6.38

## 16/21:18 watchdog nudge 문구 일반화 (fable-5)

- **무엇/왜**: nudge (2)항이 venv 예시 하나에 치우쳐 시야를 좁힌다(사용자 지적) → 시간 도둑 범주 3개(반복 준비/설치→재사용, 잦은 전체 재실행→범위 축소, 재읽기/재계산→이전 결과 재활용)로 일반화. 5경로 재동기화.
- **결과**: check 28/28 PASS 유지(③의 자평·재사용·언급 anchor 불변), md5 5-way 재일치. 계약·spec 불변(문구는 feature-level detail) — PR #48 브랜치에 커밋 추가.
- **요약**: 인라인로 충분.
