# 2026-07-30

## 1/10:30 spec-sync — reviewer 출력 다이어트 + plan-review 읽기 계단 (implemented, evidence 있음)

- **무엇/왜**: 전날 구현(`_sdd/work_log/2026-07-29.md` §8~§10)을 global spec에 반영. draft Part 1 마커가 입력이나, 구현 중 계약 조정(불변식 소유자를 AC1 1곳으로)이 있어 **draft 문면이 아니라 실제 구현 상태**를 반영하도록 지시했다.
- **결과**: spec 4파일 — `main.md` 4.6.14→4.6.15(§2 reviewer guardrail 불릿에 하위 항목 2개), `components.md` `plan-review` 행 Notes에 읽기 계단, `decision_log.md` 신규 entry(결정 11건), `logs/changelog.md` v4.6.15.
- **착지 판단 2건**: (a) 새 guardrail 불릿을 세우지 않고 기존 reviewer 불릿의 **하위 항목**으로 넣었다 — 새 행을 세우면 reviewer 반환 계약의 판정 주체가 둘로 갈린다. (b) 읽기 계단은 `main.md`가 아니라 `components.md`에 착지했다 — agent 한 곳에만 적용돼 "2개 이상 표면 공통"이라는 repo-wide invariant 기준을 통과하지 못한다.
- **과거 결정 대체**: 2026-07-10 결정 #6의 "`Progress Overview`를 task/AC 상태로 **제약**" 조항이 이번 단위의 **삭제**로 대체됨을 신규 entry #2에 명시. append-only 이력은 무수정(삭제 0줄 실측).
- **기각 대안 기록**: lite agent 신설 / model·effort 티어 강등(**재제안 금지** 명기) / Medium 블록 강등 / `Read` 전면 금지. 특히 "`plan-review` 자신이 `Read` 금지의 반례였다"를 근거로 남겼다.
- **검증**: live spec 3파일에 stale 리터럴 0건(`Progress Overview`·`smell 6행`·`차원 5행`·`필요한 범위만 읽는다`), append-only 삭제 0줄, draft `_processed_` rename 완료.
- **속도 효과에 대한 정직한 계측 (중요)**: 이 세션 실측 — plan-review 242s/19 tool/62k, correctness 363s/17 tool/79k, simplicity 261s/13 tool/64k. **최종 리포트는 각 2~3k 토큰으로 전체 소비의 4% 안팎**이고 나머지는 파일 읽기 + 중간 추론(correctness는 변이 테스트 12건 자발 수행)이다. 따라서 출력 다이어트(T1/T2/T4)의 실제 절감은 전체의 **1% 미만**이며 체감은 거의 없다. 일의 양을 실제로 깎는 것은 입력 상한(T3)뿐이다. 초기 진단 "병목은 작성"은 반쯤 틀렸다 — 정확히는 **"보이지 않는 추론"이 병목이고 "보이는 리포트"는 아니다**. 다음 레버는 검증 노력의 상한이며 판별력과의 맞교환이다(이번에 잡힌 위장 PASS 3건이 깊이 판 대가였다).
- **포인터**: `_sdd/spec/decision_log.md` 2026-07-30 entry · `_sdd/drafts/_processed_2026-07-29_feature_draft_review_agent_output_diet.md`

## 2/11:05 feature-draft — implementation-review 읽기 범위 계단

- **무엇/왜**: 직전 feature의 정직한 계측(리포트는 전체 토큰의 4% → 출력 다이어트 절감 1% 미만)에 따라, 일의 양을 실제로 깎는 입력 상한을 `implementation-review-agent`로 확장. 사용자가 대상에서 `pr-review`를 명시적으로 제외했다("PR 리뷰는 충분히 시간을 들이는 게 좋다").
- **형태 결정 — 도구 계단이 아니라 범위 계단**: `plan-review`의 Glob→Grep→Read→UNKNOWN을 그대로 이식하면 안 된다. correctness 렌즈는 코드 본문 Read가 핵심 수단이고, 실제로 이 agent가 직전 feature에서 변이 테스트 12건으로 검증 스크립트의 위장 PASS 3건을 잡았다. 그래서 도구 순서 대신 **무엇을 읽을지의 범위**를 제한한다 — ①변경 집합+기준 문서는 전문 Read 보장(상한 없음), ②인접 표면은 Grep 우선, ③그 밖은 탐색적 읽기 금지.
- **결과**: `_sdd/drafts/2026-07-30_feature_draft_impl_review_scope_ladder.md` (규모 판정 적격, task 2개 — Step 3 계단 신설 + Error Handling 행 처리 / read-only 미러·잔존 검증). 브랜치 `refactor/impl-review-scope-ladder`.
- **plan-review High 3 전량 반영 — 셋 다 실질적 설계 결함이었다**:
  - **H1**: 3분할이 **기준 문서(draft/spec 문서 자체)를 어느 단에도 넣지 않아** spec 정합 렌즈(`ALIGNED/DRIFT/MISSING` 마커, 기준 문서 적응 2단)가 문면상 ③으로 떨어졌다. 덤으로 ②의 술어 "호출"은 콜그래프 전제라 이 repo(마크다운 자산)에서 ②가 공집합이 된다. → ①에 기준 문서 포함, ②를 "의존 또는 짝 관계(호출·import·미러 짝·wrapper↔agent 포인터·spec surface)"로 일반화.
  - **H2**: ③"읽지 않음"이 **AC가 요구하는 census 증거까지 막아** `UNTESTED`로 새는 통로를 열었다 — 이 draft 자신의 Task 2 AC1("전수 grep 0건")이 반례다. 이 repo가 반복해 앓은 census 잔존·미러 누락이 정확히 그 표면에서 났다. → ③에 "AC가 명시적으로 요구하는 증거는 범위 밖이라도 Grep/Bash로 확보, ③이 막는 것은 AC가 요구하지 않는 탐색" 예외 신설.
  - **H3**: Error Handling `대규모 코드베이스` 행을 포인터로 축약하면 **유일한 degradation 규칙이 삭제**되는데 포인터 대상 ①은 "상한 없음"이라 그 상황에 답이 없다. 근인은 Part 1이 "무엇을 읽는가(범위)"와 "담기지 않을 때 무엇을 포기하는가(초과 대응)"를 같은 규칙으로 본 것. → 초과 대응을 ①로 **이전**(삭제 아님)하고, 이것이 사용자가 배제한 검증 예산 제한이 아니라 기존 능력 이전임을 Scope에 명시.
- **Medium 3 · Low 2 반영**: `UNTESTED` 토큰이 HR5(실행 불가)와 ③(범위 밖) 두 의미로 갈리는 문제 → 사유 병기(`UNTESTED(범위 밖)`, ledger 형식 불변) · AC6/7/8의 맞물림 오류(AC7이 무변경 AC6에 "변경 반영"을 요구, AC8 화이트리스트에 agent AC 절·Hard Rules 누락) → **diff hunk positive 1개**(AC9)로 접어 열거 누락 위험 제거 · 절감 미측정 가정을 Open Questions로 표면화 · Contracts 3중 서술 제거 · `components.md` `implementation-review` 행 Primary Source에 codex toml 누락(plan-review 행은 짝 2개 보유) delta 추가.
- **포인터**: draft 파일 · 직전 feature 커밋 4d8dbc1 (plan-review 도구 계단)

## 3/11:40 implementation — implementation-review 읽기 범위 계단 (2렌즈 게이트 + fix)

- **결과**: `implementation-review-agent` Step 3에 3단 범위 계단 신설 + Error Handling `대규모 코드베이스` 행을 Step 3 ① 포인터로 축약. claude md + codex toml 2파일, payload 동일.
- **검증**: structural-check 47 PASS / 0 FAIL (RED 34건 → GREEN). 변이 테스트 12건(단일 미러 6 + 양쪽 미러 동시 5 + 재확인)으로 각 check이 자기 변이를 잡는지 증명.
- **implementation-review(correctness) Medium 4 → fix 4** — 자기참조 리뷰였고 자기 계약의 경계 결함 3건을 잡았다:
  - **M1 모드 커버리지**: ①의 기준 문서 열거가 "호출자 지정 draft/plan"에 앵커돼 `기준 문서 적응` mode 2(`spec만 있음`)·mode 3에서 기준 문서가 ①에 안 들어가고 ②로 떨어진다 — plan-review H1이 막으려던 "spec 정합 렌즈 강등"이 mode 1에서만 닫혔다.
  - **M2 목적 역행 (가장 값진 지적)**: ①의 "그것이 참조한 spec 범위"에 한정 술어가 없어, 실측 `decision_log.md` 1752줄까지 문면상 무상한 ①에 포함된다. 변경 전 Step 3에는 spec 읽기 지시가 아예 없었으므로 이 조항은 상한이 아니라 **신규 읽기 의무**로 작동할 수 있었다 — 읽기량을 줄이려는 feature가 팽창 여지를 만든 것. → `AC·정합 판정에 필요한 절로 한정`.
  - **M3 커밋 후 공집합**: `git diff --name-only` 단일 수단이라 구현이 커밋된 뒤 호출되면 ①이 공집합이 되고 변경 파일이 ③ "탐색적 읽기 금지"에 걸린다(mode 3에서 특히). 같은 파일 `기준 문서 적응` mode 3은 `git log`를 명시하는데 ①은 안 해 수단이 어긋났다. → `<base>..HEAD`/`git log` fallback 추가.
  - **M4 위장 PASS 4건**: 리뷰어가 사본에 **양쪽 미러 동시 변이**를 넣어 T1-AC11(미러 diff) 마스킹까지 제거하고 실증했다 — (a) AC9의 라인 패턴 제외 `^[1-3]\. \*\*`가 Scope-Out으로 보호된 `기준 문서 적응` 3항목까지 면제, (b) AC3c가 리터럴 2종만 봐 `Read는 마지막 수단` 삽입(= 이 feature의 핵심 non-goal인 도구 계단 누출)을 놓침, (c) AC10 hedge 목록 3개 리터럴이 `여력이 되는 범위에서`로 우회됨, (d) AC4b가 `limitation` 단어 존재만 봐 `필요하면 적는다`로 축소돼도 통과. AC1b는 번호 자리수만 봐 판별력 0. → AC9를 **보호 섹션 sha256 대조**로, AC3c/AC10을 문장 단위 + positive 앵커로, AC4b/c를 실질 문구로, AC1b를 tier 키워드 순서로 교체.
- **내 변이 하네스에서도 같은 위장 PASS가 났다**: 첫 시도는 `python -c` 인라인 치환 코드의 IndentationError로 죽어 **변이가 적용되지 않은 47 PASS**를 보고했다. 별도 `mutate.py`로 분리하고 치환 실패 시 non-zero로 죽게 만든 뒤 재측정 — 5건 중 4건 검출, M18b만 미검출이었고 원인은 AC3c가 예외 문구를 세그먼트 단위로 제외해 같은 줄의 강등 표현까지 버린 것. 예외 '문구'만 sed로 지우고 검사하도록 고쳐 5/5 검출.
- **simplicity Medium 5 → fix 3, 반려 2**:
  - 반영: (M2) ① 한 문장에 5개 지시 + 2중 괄호 → 하위 불릿 5개로 분할 · (M3) ① 안의 기준 문서 모드 재열거를 Step 1/`기준 문서 적응`에 위임(판정 주체 1곳) · (M4 부분) `UNTESTED(범위 밖)` 뒤 HR5 괄호 재서술 제거.
  - **반려 (M1) correctness 지시 3중 서술**: 그 문장은 신설이 아니라 **변경 전 Step 3에 있던 문장을 ① 안으로 이동**한 것이다(리뷰어도 Assumptions에서 확인 불가로 남겼다). AGENTS.md §0 Surgical Changes("clean up only your own mess")상 선재 중복은 이번 범위가 아니고, 삭제 시 draft AC3/AC10과 충돌한다. **별건 후속 대상**.
  - **반려 (M5) Error Handling 행 삭제**: plan-review H3이 "포인터 vs 짧은 행 유지" 두 안을 이미 평가하고 draft AC8이 포인터를 택했다. 또 그 표는 *상황→규칙* 라우팅 표면이라 행을 지우면 `대규모 코드베이스` 상황이 표에서 사라진다.
  - 유지 판단: ③의 `이 단이 막는 것은 AC가 요구하지 않는 탐색이다`는 재서술로 지적됐으나 plan-review H2가 잡은 실제 실패 모드(census AC가 UNTESTED로 새는 것)를 막는 negative라 남겼다.
- **fix 후 회귀**: 47 PASS / 0 FAIL, 변이 5건 전량 재검출, `git diff --check` clean, codex toml `tomllib` 파싱 + `Codex Agent Boundary` delta 보존.
- **잔존 advisory**: correctness Low 3(Step 6 Assumptions 정의가 "기준 문서 없이 리뷰한 경우"로 한정돼 범위 밖 가정의 귀속이 문면 모순 — 반환 형식이 Scope Out이라 이번엔 못 고침 / UNTESTED 사유 병기가 편면적 — 무표기 UNTESTED는 여전히 모호 / `components.md` spec surface는 spec-sync 후속). simplicity Low 1(① 라벨 `상한을 걸지 않는다`가 말미 초과 대응과 표현상 긴장).
- **포인터**: draft `_sdd/drafts/2026-07-30_feature_draft_impl_review_scope_ladder.md` · check `scratchpad/check_scope_ladder.sh` · 변이 하네스 `scratchpad/mutate.py`

## 4/12:25 spec-sync — implementation-review 읽기 범위 계단 (implemented, evidence 있음)

- **결과**: spec 4파일 — `main.md` 4.6.15→**4.6.16**(§2 reviewer 불릿에 하위 항목 1개), `components.md` `implementation-review` 행(Notes에 계단 전문 + Primary Source에 codex 짝 2개), `decision_log.md` 신규 entry(결정 9 + Rationale 5 + Follow-up, 31/0), `logs/changelog.md` v4.6.16(7/0). append-only 삭제 0줄.
- **draft delta와 다른 판단 2건 (agent가 명시하고 내가 실측 확인)**:
  - draft Part 1은 "`main.md` 본문 승격 안 함"이라 적었으나, agent는 **계단 상세는 `components.md`에 두고 "입력 상한의 형태는 렌즈에 맞춘다"는 일반화만** §2에 올렸다. 이 일반화는 reviewer 2종(plan-review 도구 계단 / implementation-review 범위 계단)에 걸쳐 `Repo-wide Invariant Test` "2개 이상 표면 공통"을 통과한다. 근거: 이게 없으면 두 agent 파일을 읽어도 왜 형태가 다른지·왜 `pr-review`가 빠졌는지 복구되지 않고, 다음 feature가 도구 계단을 correctness 리뷰어에 그대로 복사한다. **동의.**
  - Primary Source에 `.codex/agents/...toml` 외 `.codex/skills/implementation-review/SKILL.md`도 추가(draft는 toml만 지목). 인용된 비대칭이 "`plan-review`는 codex 짝 2개"였고 그 파일도 실존(실측 확인)해 비대칭을 온전히 닫는 쪽을 택했다. **동의.**
- **decision log에 남은 재사용 가능 결정**: 같은 목적(입력 상한)이 agent의 **렌즈에 따라 다른 형태**를 갖는다 — draft를 보는 리뷰는 도구 계단, 코드 correctness를 보는 리뷰는 범위 계단. 그리고 ③은 "읽지 않음"이 아니라 "**탐색하지 않음**"이며 AC 요구 증거는 예외다.
- **정직한 효과 기록**: 확정 효과는 절감 수치가 아니라 "무제한 재량 → 상한". ②③ 억제의 절감 규모는 미측정이고 다음 `implementation-review` 1회의 tool call 구성으로 사후 실측 대상(decision_log #9).
- **검증**: live spec 3파일에 stale 리터럴 0건(`핵심 컴포넌트 중심으로`, `필요한 범위만 읽는다`), 자산·코드 무수정, 신규 파일 없음, draft `_processed_` rename 완료.
- **후속 surface 2건 (이번 범위 아님)**: (a) 선재 smell — correctness 능동 검토 지시가 `implementation-review-agent` 헤더·AC1·Step 3 3곳에 서술(판정 주체 1곳 규범 위반). (b) `components.md`의 다른 행(`spec-sync`·`spec-summary` 등)도 실재하는 codex 짝을 Primary Source에 열거하지 않는 **동일 누락 패턴** — sweep이 필요하면 별도 feature.
- **포인터**: `_sdd/spec/decision_log.md` 2026-07-30 두 번째 entry · 위 `## 2/11:05`·`## 3/11:40`
