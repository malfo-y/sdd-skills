# 2026-08-01

## 1/10:45 feature-draft + implementation — plan-review 실측 렌즈 묶음 분할 (검증 ∥ 전제) ⟨되돌림 — `## 3` 참조⟩

- **무엇/왜**: plan-review 2-렌즈(v4.6.22) 후에도 벽시계 = 실측 렌즈 단독(320s/178s ∥ 판단 86~122s). "적을 양"이 실측 렌즈에 쏠려 있어 그 안을 다시 나눴다 — 상위 렌즈 이름 2개는 유지하고 실측 렌즈를 **검증 묶음**(`Verification Weakness` 전담, 행 소유) ∥ **전제 묶음**(판단 렌즈 소유 smell의 사실 전제 repo 대조, 행 없음)으로 대체 정의. SKILL은 2-dispatch → **3-dispatch** + 병합 규칙(합집합 + PASS 접기 보정).
- **베이스 정리**: 시작 시 HEAD가 로컬 전용 `codex/fix-codex-runtime-adapter`(PR 없음)였고 그 브랜치가 `.codex/skills/plan-review/SKILL.md` 어댑터를 2-contract(Mailbox / Target-close) 구조로 바꿔 이번 변경과 충돌 예정이었다 → 사용자가 먼저 머지(PR #39, `58405fb`), 갱신된 main에서 분기.
- **plan-review 게이트 = 3-dispatch 파일럿 (핵심 실측)**: **검증 225s ∥ 전제 353s ∥ 판단 160s — 벽시계 353s, 재고 밴드**(사전 기준 ≤130 지지 / 200+ 재고). 기존 2-렌즈 표본(320/178)보다 나쁘고 합 738s로 **총량 보존이 처음으로 깨졌다**. 원인: 전제 묶음 반환의 큰 몫이 *반증되지 않은(지지된) 전제의 열거*였다 — 계약에 대조 대상 상한이 없었다. 판단 렌즈가 160s로 뛴 것은 이 draft 자체가 이전보다 무겁다는 신호라 절대 밴드 비교는 오염(정규화 max÷판단 = 2.2 / 표본1 3.7 / 표본2 1.46). 사용자 결정으로 **전제 묶음 계약을 "반증된 전제만 반환, 지지된 전제 비열거"로 축소**해 반영, 효과 검증은 다음 draft의 게이트.
- **게이트 품질은 상승**: High 2 — ① 전제 묶음이 `main.md:164`와 대조해 내 draft의 사실오인("판단 렌즈 86s가 두 표본 동일한 고정 바닥" ← 표본 2는 122s)을 잡음. 파일럿 판정 밴드가 그 위에 얹혀 있었다. ② 검증 묶음이 Task 3 census 리터럴 목록의 변형형 누락(`두 경량 반환`·`두 spawn`·`두 task/agent`)을 잡음 → 정규식 전수 매치로 교체. Medium 6·Low 3 전량 반영(병합 규칙 개수 복제 제거, 호출자 없는 후방 호환 분기 삭제, Task 3 AC4 제거 = spec 인계는 spec-sync 소관, 판정 기준 상대값화).
- **검증**: RED 9 FAIL 선관찰 → GREEN 18 PASS, 변이 9종 전량 검출. check 결함 3건 선언·교정 — 삭제 계열 필터가 절 도입 문장 오판 / `지지된 전제\|반증된 전제만` **택일이 만든 위장 PASS**(약한 변이 함정 재현) / `Severity` 앵커가 표 헤더 행에도 매치돼 섹션 삭제를 못 봄(`^## ` 앵커로 교정).
- **구현 게이트(N+2) = 벽시계 279s**: correctness 240/190/212 + simplicity 참조 279 ∥ 국소 147. Critical/High 0, Medium 4 전량 fix.
  - **M1(핵심)**: 분할이 구 실측 렌즈의 상위 조항 "**draft 사실 주장의 repo 대조**"를 소리 없이 삭제했다 — 두 묶음은 그 부분집합만 나눠 가져, 어느 smell에도 매달리지 않은 사실 주장이 무주공산이 됐다(렌즈 한정 없는 전체 호출에도 적용). → 전제 묶음 대조 대상을 상위집합으로 복원.
  - **M2**: "행은 내지 않는다"가 agent 자신의 어휘(개별 행 vs `PASS` 접기 줄)를 덮지 못해 5 smell 접기 줄이 되살아날 수 있었다 → "`Smell 판정` 섹션 자체를 생략"으로 좁힘.
  - **M3**(shard1·shard2 교차 검출): PASS 접기 보정이 "제외"만 하고 대체 표기를 안 정해, 반증된 smell이 행 없이 사라졌다 → 보정이 finding의 권고 status로 행을 만들도록 + agent가 finding에 권고 status를 적도록.
  - **simplicity**: AC1 3중 기재·소유 정의 복제 → 조건형 1문장으로 축약(참조 M1) / 병합 규칙 과잉압축 → 불릿 6줄로 전개(국소 M1, 형제 스킬 `implementation-review` 선례 형태).
  - fix diff 커버리지 델타로 check 3종 추가(상위집합 대조·섹션 생략·보정 행 생성) + 변이 4종 검출, fix 후 회귀 18 PASS.
- **잔존 advisory (Low)**: `:47`의 "실측 렌즈 dispatch" 표현이 더 이상 단일 dispatch를 안 가리킴(해석은 상위/하위 정의로 닫힘) · 같은 smell에 판단/전제 finding이 겹칠 때 dedupe 미규정 · check의 "별도 문장" 조건은 육안 증거 · check 변경집합 수집이 staged를 누락(scratchpad 도구) · 상위 이름 `실측 렌즈`의 조작적 사용처가 1곳뿐(평평화 후보, 2-렌즈 명칭 유지 결정으로 보류).
- **포인터**: draft `_sdd/drafts/2026-08-01_feature_draft_measurement_lens_shard.md` · check `scratchpad/check_lensshard.sh` · spec-sync 후속 항목

## 2/11:35 spec-sync — plan-review 묶음 분할 반영 (v4.6.28) ⟨되돌림 — `## 3` 참조⟩

- **무엇/왜**: 위 `## 1` 구현의 spec 동기화. 플러그인 설치본이 아직 구 wrapper(1-dispatch)라 v4.6.24 계약대로 **본문 ∥ 기록 2-shard를 수동 dispatch**했다(선고정 + 사후 정합 grep 2종).
- **핵심 실측**: 본문 221s ∥ 기록 153s — **벽시계 221s**(합 374s). 사후 정합 ① main 헤더 == changelog 최신 == decision_log entry = **4.6.28 일치**, ② 기록 파일 삭제 줄 **0**(decision_log +34/−0, changelog +7/−0). 쓰기 서로소 준수(본문 = main·components·usage-guide / 기록 = decision_log·changelog + draft rename), 충돌 0.
- **선고정 오류 1건 (내 실수) — 두 shard가 독립 검출**: 내가 신규 버전을 `4.6.24 → 4.6.25`로 고정했으나 live `main.md`는 이미 **4.6.27**이었다(직전 PR #39의 codex 계열 3커밋이 4.6.25~27 선점). 이전 세션 기억을 "대화에서 아는 값"으로 취급해 계약이 허용한 **grep 1회 확정을 생략**한 것이 원인이다. 두 묶음이 각자 Hard Rule 12(비파괴적 충돌 해소)로 **4.6.28**에 수렴해 정합이 유지됐다 — 쓰기-서로소 분할의 부수 이점(독립 교차 검증)이 처음 관측된 사례. **교훈: 선고정 값은 기억이 아니라 실측으로 확정한다.**
- **결과**: `main.md` 4.6.27→**4.6.28**(게이트 분할 축·도구 계단 소유·품질 게이트 행·실행 분리 행·2-렌즈 결정 행 관측 누적·🚧 Planned 갱신), `components.md`(plan-review 행 + Claude split 행), `usage-guide.md` Scenario 2, `decision_log.md`·`logs/changelog.md` append-only entry. draft `_processed_` rename 완료.
- **🚧 Planned로 남긴 것**: ① 렌즈 분할의 벽시계 효과 — 표본 3(3-dispatch 353s 재고, 총량 보존 첫 붕괴)까지 반영하되 **미확정**, 계약 축소(반증만 반환)의 효과는 다음 draft 게이트가 첫 관측. ② 검증/전제 재배분은 관측 누적 후 별건. ③ 표본 2 판단 렌즈 값 상충(main.md 122s vs work_log 86s)은 live truth 기준으로 두고 상충 사실을 명시(work_log는 append-only라 무수정).
- **포인터**: `_sdd/spec/decision_log.md` 2026-08-01 entry · changelog v4.6.28 · 위 `## 1`

## 3/11:12 결정 — 묶음 분할 전량 되돌림, 계약 축소만 남긴다

- **무엇/왜**: `## 1`·`## 2`의 산출물(스킬/agent 4파일 + spec 5파일 + draft rename)을 **커밋 전에 전량 되돌렸다**(HEAD 복원, 이력 흔적 0, supersede entry 불요). 백업은 `scratchpad/revert_bak/` 11파일.
- **판단 근거 (사용자 제안 채택)**: 분할의 장부가 손해였다 — 속도는 재고 밴드(353s, 총량 보존 첫 붕괴), 검출 품질은 개선 근거 없음(잡힌 High 2가 전부 구 실측 렌즈도 소유하던 종류, n=1), 대신 계약 표면이 늘었다(비대칭 반환·PASS 접기 보정 두 불변식 신설, 그 자리에서 구멍 3개 발생).
- **핵심 관찰 — 축소는 분할과 독립이다**: 지지된 전제를 열거하는 습성은 전제 묶음 전용이 아니었다. 같은 게이트에서 **검증 묶음 반환에도 `## 참고: 실측으로 확인된 앵커 (finding 아님)` 섹션이 나왔다** — 실측 계열 dispatch 전반의 반환 습성이다. 따라서 "반증/finding이 아닌 대조 결과는 열거하지 않는다"는 통짜 실측 렌즈에도 그대로, 더 넓게 먹는다. **나눠서 줄이는 것보다 애초에 덜 쓰게 하는 편이 계약 표면을 안 늘린다.**
- **다음 범위**: 렌즈 개념을 건드리지 않고 agent `Step 6: Return`에 반환 다이어트 1문장만 넣는다(미러 2벌). SKILL 4벌 무변경. 이 feature의 plan-review 게이트가 2-렌즈 대조 표본 4가 되고, 다이어트 효과의 첫 관측은 다음 feature 게이트다(플러그인 발효 지연 구조).
- **보존할 음성 결과**: 3-dispatch 353s / 합 738s(총량 보존 4회 재현 후 첫 붕괴) / 분할이 상위 조항을 흘린 M1 함정 — 되돌렸어도 `## 1`에 그대로 남긴다.

## 4/11:22 feature-draft + implementation — plan-review 반환 다이어트

- **무엇/왜**: `## 3` 결정에 따른 축소 범위 구현. `plan-review-agent` 미러 2벌의 `Step 6: Return`에 규칙 1문장 추가(확인했으나 finding이 아닌 대조 결과는 열거하지 않는다 / 반환은 위 항목이 전부다 / 렌즈 한정 여부와 무관 / 줄이는 것은 출력이지 Step 3 대조 범위가 아니다) + 자체 검증 `AC5`에 준수 흡수. 새 AC·Hard Rule·섹션 없음, 렌즈 구조·rubric·SKILL 4벌 무변경. diff = 파일당 +4/-1.
- **plan-review 게이트 = 2-렌즈 대조 표본 4** (다이어트 미발효): 실측 131s ∥ 판단 93s — **벽시계 131s**, Blocker CLEAR. 표본 1·2(320/178)보다 크게 낮지만 draft 규모가 훨씬 작다(정규화 max÷판단 = 1.41로 표본 2의 1.46과 유사).
- **게이트 finding 반영**: 실측 M(Contracts의 "무조건성"이 어떤 AC로도 검증 안 됨 → AC1에 (c) 추가) · 판단 M1(Contracts가 반환 4항목 중 2개만 열거해 AC3와 긴장 → 문면 교정 + AC1에 (d) 추가) · 판단 M2(AC5 "바이트 동일" 전제 미검증 → 실측 렌즈가 확인해준 근거 병기 + 어긋날 때 하향 판정 경로 명시) · Low 2(근거 문장 정정, Change Summary 축약).
- **검증**: RED 4 FAIL → GREEN 9 PASS, 변이 7종 전량 검출. check 결함 1건 선언·교정(`AC4` 필터가 **빈 줄 추가**를 구간 밖 변경으로 오판).
- **구현 게이트 = 벽시계 221s**(correctness 221 ∥ simplicity 참조 126 ∥ 국소 79). Critical/High 0, **Medium 1 fix**: 자체 검증 `AC5`에 범위 한정어가 빠져("Step 6 항목 밖에") 무한정으로 읽히면 agent가 자체 검증 때 `PASS` 접기 줄과 규모 판정 결과까지 지우는 쪽으로 수렴할 수 있었다 — draft Contracts엔 있던 어절을 AC로 옮기며 흘린 것. fix 델타 check 추가 + 변이 검출, fix 후 회귀 9 PASS.
- **잔존 advisory (Low)**: `Step 2`의 "반환에 전사하지 않는다"가 새 규칙의 특수 사례가 됨(통합 후보, 국소 가드라 존치) · "렌즈 한정 여부와 무관" 절이 `:48`의 통짜 선언과 겹침(근본 해소는 `:46` 조건화 목록을 닫는 것 — 별건) · 실측 렌즈의 사실 전제가 확인도 반증도 안 될 때의 귀속처 미명시(2-렌즈 도입 시점의 잔여 공백).
- **포인터**: draft `_sdd/drafts/_processed_2026-08-01_feature_draft_plan_review_return_diet.md` · check `scratchpad/check_diet.sh` · 위 `## 3`

## 5/11:27 spec-sync — 반환 다이어트 반영 (v4.6.28)

- **무엇/왜**: 위 `## 4` 구현의 spec 동기화. v4.6.24 계약대로 본문 ∥ 기록 2-shard 수동 dispatch. **선고정 버전을 grep 1회로 실측 확정**(`## 2`의 교훈 적용) — 이번엔 충돌 0.
- **핵심 실측**: 본문 194s ∥ 기록 95s — **벽시계 194s**. 사후 정합 ① main 헤더 == changelog == decision_log = **4.6.28 일치**, ② 기록 파일 삭제 줄 **0**(decision_log +30/−0, changelog +6/−0).
- **결과**: `main.md` 4.6.27→**4.6.28** — §2에 반환 다이어트 sub-bullet 신설, `:77`에 **되돌린 3-dispatch의 음성 결과 + 재제안 차단 문장** 고정("부풀림의 원인은 분할이 아니라 반환 습성 / 새 근거 없이는 재제안 대상 아님"), §3 🚧 Planned에 2-렌즈 표본 4(131s) 추가 + 다이어트 효과 미관측 신규 항목. `components.md`·`usage-guide.md`는 **의도적 무변경**(위임할 detail 없음·사용자 관측면 불변). `decision_log`·`changelog` append-only entry, draft `_processed_` rename.
- **효과는 미관측으로 명시**: 첫 관측은 발효 후 다음 feature의 plan-review 게이트이며 판정은 `실측 렌즈 시간` + `반환에 확인 목록이 실렸는지` 둘 다 본다. 절감 폭은 주장하지 않았다.
- **포인터**: `_sdd/spec/decision_log.md` 2026-08-01 entry(v4.6.28) · changelog v4.6.28 · 위 `## 4`

## 6/18:20 simplicity-review-agent 반환 다이어트 — draft·구현·게이트

무엇/왜: plan-review 반환 다이어트(v4.6.28)의 울타리 규칙을 simplicity-review-agent 미러 2벌에 적응 이식 (사용자 결정: 세 reviewer 중 simplicity만 — impl-review·pr-review는 ledger 예외 문장이 필요한 별도 feature로 제외). Step 4 규칙 1문장 + 자체 검증 AC3 흡수(범위 한정어 "Step 4 항목 밖에" 처음부터 포함). 각 미러 +3/-1.

결과:
- plan-review 게이트: CLEAR, Low 2(관측 계획이 spec-sync 마커 안 + Open Questions 중복 — 반영: Change Summary에서 해당 bullet 제거). **plan-review 다이어트 첫 관측(발효 후 첫 게이트)**: 실측 114s ∥ 판단 72s — 실측 절대값 역대 최저(320/178/131 대비), 정규화(실측÷판단) 1.58로 표본2(1.46)·표본4(1.41)와 유사 밴드. 반환 형태: 열거 섹션(`## 참고: …앵커`류) 소멸, 다만 "Findings 없음" 단락 안 검증 전제 요약 1문장 + 참고 1건 잔존 — 열거→압축 요약. 판정: 긍정 신호, n=1이라 확정하지 않고 누적.
- 구현: structural check(scratchpad/check_simpdiet.sh) RED 4 FAIL → GREEN 9 PASS, 변이 7종 전량 검출·원복. 커버리지 델타 없음(diff 전량 AC 유래).
- implementation-review 게이트: correctness 97s ∥ simplicity 참조 55s ∥ 국소 57s — Critical/High/Medium/Low 전부 0, fix 없음. 부수 관측: simplicity reviewer들(구계약 플러그인 캐시)이 "스캔 요지"·"폐기 후보 상술" 단락을 반환에 실음 — 이번 규칙이 막을 습성의 실재 증거.

포인터: _sdd/drafts/2026-08-01_feature_draft_simplicity_return_diet.md, .claude/agents/simplicity-review-agent.md:18,:81 (+toml 미러)

## 7/18:35 spec-sync — simplicity 반환 다이어트 반영 (v4.6.29)

무엇/왜: simplicity_return_diet 구현분을 global spec에 동기화. 본문 ∥ 기록 2-shard 병렬 dispatch (선고정 버전은 grep 실측 4.6.28 → 4.6.29 — 세션 기억 선고정 금지 규칙 준수).

결과: main.md v4.6.29(§2 guardrail에 simplicity 이식 승격 + ledger 계약 2종 제외 결정, §3 plan-review 다이어트 첫 관측 반영·2-렌즈 표본 5 추가·simplicity 다이어트 미관측 신규 Planned), decision_log +31/changelog +5 append-only(삭제 0), components/usage-guide 무변경(반환 형식 재기재 없음 실측). 사후 정합 grep: 버전 3표면 일치. shard 벽시계 253s ∥ 81s. draft `_processed_` rename 완료.

포인터: _sdd/spec/main.md, _sdd/spec/decision_log.md, _sdd/spec/logs/changelog.md, _sdd/drafts/_processed_2026-08-01_feature_draft_simplicity_return_diet.md
