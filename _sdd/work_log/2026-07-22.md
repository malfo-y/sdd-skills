# 2026-07-22

## 1/12:40 sdd-autopilot lite fast-path 신설 — lite 레인을 기본 레인으로 승격
- **무엇/왜**: 개밥 결과(draft ~1분, 경량 반환 게이트) 만족으로 사용자가 lite 레인 기본화를 요청. 설계 토론 결정 — **A안(lite fast-path)**: lite 적격이면 orchestrator 생성·구조 검증·plan-review orchestrator gate·Step 6 checkpoint 전부 생략(lite 규모에선 파이프라인 계획서 작성이 실행보다 비쌈). **무승인**: checkpoint의 존재 이유(비싼 무중단 Phase 2 진입 전 방향 확인)가 lite에선 둘 다 소멸 — 잘못 가도 싸고(draft 1분) plan-review 자동 게이트가 이미 있음. 승인은 승격 시 full 레인 Step 6으로 자연 복귀. 사용자 보강 — 각 게이트 뒤 **fix 1회**(re-review loop 없음, C/H/M만, 잔존은 보고+승격 신호; impl fix 후 회귀 1회로 마감).
- **결과**: autopilot SKILL 2.5.0→2.6.0. 체인 = `feature-draft-lite → plan-review(Tier 2-lite 자가 식별·경량 반환) → fix 1회 → implementation-lite → implementation-review(경량 반환 기본) → fix 1회 → spec-sync(조건부) → 최종 응답 보고(report 파일 없음)`. 구조: Step 2에 Lane 판정(기본 lite, 명백한 신호만 full 직행, 애매하면 lite — draft가 곧 판정 비용) + Step L 신설 + Step 1(reference 로딩 ~44K)을 full 전용으로 스코프(lite 런은 미로딩) + Step 0 분기 Step 2로 + Hard Rules 레인 스코프 preamble(2·3·5·6·8·10·15 full 전용) + AC-L1~L4 신설. 승격 canonical은 lite 표면 소유(autopilot은 신호 소비만), 승격 시 lite 산출물을 full 입력으로 재사용. reasoning-reference에 Lite-first 원칙·lite 그래프·카탈로그 4엔트리(feature-draft-lite/implementation-lite 신규, plan-review/implementation-review에 lite note). codex 미러 SKILL+reference 3-way 적용(메인 컨텍스트 어휘·prefix 무·Codex Runtime Adapter 유지), parity diff 확인. skill.json 2.4.0(기존 desync)→2.6.0 양쪽 정합. AUTOPILOT_GUIDE.md 상단에 2.6.0 레인 변경 노트(본문은 full 레인 기준 명시).
- **포인터**: `.claude/skills/sdd-autopilot/SKILL.md` Step L · `references/sdd-reasoning-reference.md` §1.7-2/§2.1 · `docs/AUTOPILOT_GUIDE.md`
- **발견(별건)**: AUTOPILOT_GUIDE.md 본문이 이미 폐기된 `implementation-plan` expanded path 등 구형 흐름을 서술(2.5.0 이전부터 drift). 전면 개정은 별도 작업으로 보류.

## 2/13:40 full 레인 삭제 결정 + 슬라이스 1: 승격→분할 의미 교체 (autopilot 2.7.0)
- **무엇/왜**: 사용자 결정 — full 레인 삭제 & lite 유일화. 근거 ① lite 품질이 full 대비 안 떨어지는데 훨씬 빠름 ② full이 필요한 복잡도면 여러 feature로 쪼개는 게 더 안전(실행단에서 분할 안내 + spec-sync todo) ③ 분기 단순화로 LLM 혼선 감소. 토론 정리: 승격 규칙 3종이 전부 분할로 환원(눈검산 불가→분할 따름정리, 컨텍스트 초과→분할, census→검증 task 흡수) + 죽은 레인 유지 비용 = 모든 하네스 변경 × 전파 표면. 정직한 트레이드: test-author 존치 논거(tail을 승격이 거른다)가 끊김 → 새 논거 "분할이 tail을 median화 + 테스트 불변 규칙·Fresh Verification 이중 안전장치", 개밥 관찰 항목으로 유지. 실행 순서: 슬라이스 1(의미 교체, 본 항목) → 슬라이스 2+(실체 삭제, 새 철학대로 분할 진행, 삭제 직전 `full-lane-final` tag). `-lite` 접미사 개명은 삭제 완료 후 별도 판단.
- **결과 (lite 체인 자가 실행으로 구현)**: draft `_sdd/drafts/2026-07-22_feature_draft_lite_escalation_to_split.md` (분할 draft 형태 = 롤링: Part 1 마커에 분할 목록→spec todo, Part 2는 첫 feature만 — 사용자 확인됨). plan-review 게이트 H2가 "이 draft 자체가 census형인데 검증 task 부재"를 잡음 — 신규 규칙의 자기 적용 첫 사례, Task 5(승격 어휘 grep census) 추가 + skill.json 4개 Target Files 편입(H1, registration-gap 패턴 재확인). 구현: feature-draft-lite **분할 규칙**(승격 규칙 대체, 롤링·census 검증 task·`> Lite 적격:` 리터럴 유지) / implementation-lite **중단·분할 규칙**(세션 초과→잔여 분할 마감, 계약 오류 반복→draft 복귀) / plan-review Tier 2-lite **분할 권고**(승격 권고 대체) / autopilot Lane 판정 full 직행 = 사용자 명시 요청만 + Step L 분할 규칙. 리뷰 게이트(경량 반환 2-reviewer) fix 1회: correctness M1→spec-sync-agent에 "분할 feature 목록은 feature별 개별 Planned todo" 1줄(대상 파일 밖 수정 — 롤링 계약의 소비자 규칙 부재 해소); simplicity M1→롤링 mechanics 괄호 복제 2곳 제거(canonical 포인터만 유지), M2→reasoning-reference의 lite 블록 전부 제거+상단 스코프 1줄(full 전용 문서라 lite 서술은 로드 시점상 소비자 없음 — 오전 2.6.0에 넣은 것 자체가 과잉이었음을 리뷰가 잡음). census GREEN(잔존 = 동음이의·명시 요청 서술뿐), 기존 suite 12 PASS/8 FAIL 유지(8건은 test-free triage 확대의 RED baseline — full 표면 대상이라 full 삭제 시 무의미해짐, 슬라이스 2에서 폐기 검토). 버전: lite 스킬 1.1.0, autopilot 2.7.0.
- **포인터**: draft 상기 경로 · advisory 잔존(L급): fdl description "full ceremony" 표현·autopilot :144 중복·fdl 마커 사실 3회 서술 — 삭제 슬라이스에서 sweep

## 3/13:55 슬라이스 2 개시 — 첫 롤링 분할 draft + F1(autopilot full 파트 제거, 3.0.0)
- **무엇/왜**: full 레인 실체 삭제. 규모가 리트머스를 넘어 **첫 롤링 분할 draft 실전** — Part 1 마커에 F1(autopilot full 파트)·F2(agent 4종·스킬 3종+등록)·F3(reviewer 트림)·F4(잔재·spec 정리) 목록, Part 2는 F1만. plan-review 게이트가 롤링 특유 오류를 잡음(H1: 후행 feature 표면 잔존을 선행 AC가 0으로 요구 — "후행 표면의 참조는 그 feature의 몫" 예외로 수정; M1 spec-summary example dangling 미배정→F1 편입; M2 census 변형 누락 task-ordering kebab·Phase 1.5). spec-sync(planned)가 F1~F4를 **feature별 개별 todo**로 고정(v4.6.1) — slice 1에서 넣은 롤링 소비 규칙 첫 실측 통과. draft는 `_processed_` 이동 안 함(F2~F4 원본).
- **결과 (F1)**: ① tag `full-lane-final`=407e08e ② autopilot SKILL 쌍 **v3.0.0** 전면 재작성 — Step 0(상태 확인, pipeline log 분기 삭제)→Step 1(요청 분석)→Step L(체인)만; Lane 판정·full 레인·orchestrator·AC1~8·Hard Rules full 전용 삭제, AC-L1~L6(구 AC5·AC7 흡수); codex는 Adapter 섹션 삭제(dispatch는 체인 스킬 소유), skill.json 3.0.0 완전 동일화(한 단어 delta 정리) ③ 부속 6디렉토리 삭제 + spec-summary example 쌍 dangling 정리 ④ AUTOPILOT_GUIDE **2.0.0** lite 기준 전면 재작성(implementation-plan drift 동반 해소, tag 복구 FAQ) ⑤ census GREEN — 잔존 = GUIDE tag 안내 1건(예외)·F2/F3 예정 표면 8파일. 리뷰 게이트: correctness C/H/M 0 전 AC MET(HEAD worktree로 회귀 기준선 독립 재검증), simplicity M1(canonical 포인터 재복제)·M2(Step 0 죽은 전방 참조) fix 반영. 자기 census 적발 사례: 재작성 중 넣은 "no approval checkpoints"가 walked into own census → 문구 교체. 회귀 suite 12/8→7/13 — 신규 실패 5건 전부 삭제된 orchestrator-contract를 grep하는 미구현 test-free triage RED baseline(F4 폐기 예정), 기존 8건 무변동.
- **F4 이월 항목**: `.sdd-workbench/exports/` 동결 기록물 3건을 census 예외 목록에 추가(correctness L2) · advisory: 다이어그램 주석 복제·AC-L3/L5 부분 중복
- **포인터**: draft `_sdd/drafts/2026-07-22_feature_draft_lite_full_lane_removal.md` (F2~F4 분할 계획 원본으로 존치)

## 4/14:45 F2 — full 전용 agent 4종·스킬 3종 삭제 + 등록·문서 정리
- **무엇/왜**: 분할 계획의 F2. 사용자 확정: "implement the plan"·"구현해줘" 계열 트리거를 implementation-lite로 흡수(v1.2.0), "병렬 구현" 폐기. plan-review 게이트가 **F1의 escape 2건**을 적발 — docs/en/AUTOPILOT_GUIDE.md 영어 미러(구세계 33건)와 하네스 템플릿 4미러 SDD 흐름 라인([[harness-section-propagation]] 재발 지점) + 미열거 live 참조 4곳·README가 Override 목록이라 lite 치환하면 거짓 문서가 된다는 지적(M3)까지. 전부 draft에 반영 후 구현.
- **결과**: 삭제 — 스킬 feature-draft·implementation·implementation-plan 쌍 6디렉토리, agent feature-draft·task-ordering·test-author·implementation-agent 쌍 8파일. 등록 — marketplace skills 21·agents 7, codex agents README 목록·orchestrator 서술 정리. 문서 — AGENTS.md+템플릿 4미러 lite 체인 교체, en 가이드 2.0.0 재작성, README Override 3항목만 잔존(lite 치환 안 함). census 중 **BSD grep --pcre2 미지원 + 2>/dev/null이 가짜 0을 만든 함정** 적발 → python lookahead 재실행으로 진짜 잔존 4건 추가 수정(discussion 표 라벨, codex boundary 예문 2, implementation-review Review-only 경계의 죽은 `implementation` gate 참조). 리뷰 게이트 fix 1회: correctness M1(pr-review claude 정합 근거)·M2(impl-review-agent 쌍 bare `implementation` Integration 줄 제거, :194는 F3 존치) / simplicity M1("autopilot review-fix loop" 죽은 규칙→"호출자의 fix 대상")·M2(spec-sync·spec-review 쌍의 Large/Step-6 죽은 포지션 테이블 삭제). 최종 census 잔존 22건 = 전부 F3 소관 reviewer 쌍 + SDD_SPEC_DEFINITION(F4 예외). 회귀 suite 7/13→0/20 — 검증 대상 파일 삭제로 인한 file-not-found 확인, 20개 전부 F4 폐기 대상.
- **F3 이월**: plan-review-agent 쌍 full Tier·Orchestrator Mode·planning precedence, impl-review-agent 쌍 :194·re-review/파일 mode 소비자 재검토, reviewer들 `*_implementation_plan_*` glob legacy 강등(simplicity L1·L2), codex implementation-review SKILL :50 tools 사실 불일치(스코프 밖 관찰). **F4 이월**: `_sdd/tests/` 20 스크립트+test-free triage draft 폐기, SDD_SPEC_DEFINITION lite 재서술, `.sdd-workbench/exports/` census 예외 명시, AGENTS §3 문장 과잉압축(L4, 5곳 동기), implementation-lite 트리거 중복 2쌍(L3).
- **포인터**: draft `_sdd/drafts/2026-07-22_feature_draft_lite_full_surfaces_removal.md`

## 5/15:20 F3 — reviewer full 기계장치 트림: 경량 반환 유일화 + read-only leaf
- **무엇/왜**: 분할 계획의 F3. 사용자 확정 3건 — ① plan-review full rubric 전부 삭제(Tier 2-lite가 유일 기준 승격) ② reviewer 파일 mode + re-review 삭제(단일 패스 유일) ③ **pr-review = agent 경량 반환 + 스킬(메인 루프)이 통합 리포트 1파일 직접 작성**(3→1; "작성은 호출자" 불변식의 reviewer 적용). plan-review 게이트 H1이 또 소비자 escape를 사전 적발("Tier 2-lite" 명칭의 autopilot·GUIDE 소비자가 스코프·census 양쪽 무주공산 — F1 escape와 동일 패턴) + M3(반환이 유일 소스가 되므로 payload 계약 이전 필요).
- **결과**: agent 4종 쌍 재작성(plan-review 264→~140줄; correctness는 "Tier"를 "기준 문서 적응" 산문으로 대체 — draft 내부 모순(입력 적응 유지 vs Tier 잔존 0) 계약 오류 선언 1회로 해소), tools에서 **Write 제거**(Bash는 correctness·pr 테스트용 유지). SKILL 쌍: plan-review 3.0.0 재작성+skill.json, implementation-review 7.0.0(분기·예외 제거, 병렬 근거=실제 tools — codex :50 사실 불일치 자연 해소), pr-review 4.0.0(반환 payload 계약 이전, Review version 필드 폐기, 합류 규칙 Step 4 canonical화). autopilot 쌍·GUIDE ko/en의 Tier 2-lite 명칭·"기본" 한정어 정리. 죽은 기계장치 negative 사족 8곳 제거(단, Fix 1회 positive 규칙이 커버함을 확인 후). 리뷰 게이트(재작성된 reviewer들의 첫 라이브 실행): correctness C/H 0·M1 + simplicity M6 — 합집합 fix 1회 완료(죽은 detail-리포트 참조 6곳, 3중 서술 참조화, 표-본문 severity split 합치기). census(확장 패턴 "correctness 리포트|detail 리포트|Review version" 포함) 잔존 0. suite 0/20 무변동.
- **F4 이월**: implementation-review SKILL description "implementation plan" space-variant(L2), codex pr-review sample 구세대 흐름 drift(L3 — 별도 이슈 권고), impl-review-agent Quick Review 다이어트 후보(simplicity L1), 기존 이월분(tests 폐기·SDD_SPEC_DEFINITION·advisory sweep).
- **포인터**: draft `_sdd/drafts/2026-07-22_feature_draft_lite_reviewer_trim.md`

## 6/15:45 F4 — 잔재 정리 + full 어휘 census / F5(개명) 분할 계획 추가
- **무엇/왜**: 분할 계획의 F4. 사용자 제안으로 **F5 신설** — `-lite` 개명(이름+개념 전부: feature-draft-lite→feature-draft, implementation-lite→implementation, "lite 체인"→"SDD 체인" 등; F2에서 이름 충돌 해소로 가능해짐). SDD_SPEC_DEFINITION·`_sdd/spec/` 트림은 F5로 이동(개명 후 참조가 저절로 유효해지는 부분이 커서 — correctness M3로 유실 방지 명시). plan-review 게이트 H1이 **spec-sync·spec-review agent의 full draft 구조 참조**(예외 밖 무주공산)를 적발해 T2 편입 + census 발견 잔재 처분 경로(즉시 fix / 이월) 신설 + M1의 census 2계층 분리(고유 식별자 엄격 0 / 동음이의 125건 판정 기준) 채택.
- **결과**: T1 폐기 — `_sdd/tests/` 20 스크립트·test-free triage draft(무의미화된 미구현 계획). T2 sweep — fdl 쌍 v1.2.0 description 자립화·Integration 재서술 문장 제거, impl-review description "draft/plan"화, AGENTS+템플릿 4미러 spec-sync 문장 과잉압축 해소(5곳 verbatim 동일), Quick Review 섹션 삭제, spec-sync·spec-review 쌍 lite 기준 재서술. T3 — codex pr-review sample 재작성(spawn 흐름·Codex 화자). T4 census 실전 성과: **엄격 계층이 AGENTS.md:17 "fix/re-review" 잔재 적발→즉시 fix**, 판정 계층 35파일 spot 판정(호출자 orchestrator·ralph checkpoint 등 통과, spec-format Touchpoints류 F5 이월). 게이트 fix 1회: correctness H1("Part 2 coverage" 변형 잔존 — census 변형형 함정 재발 실증)+M3(codex sample Claude 화자 회귀, spec 트림 소유 부재), simplicity M3(legacy fallback 4회 재천명→canonical 1곳, spec-review rubric 축 lite화). 잔존 = legacy fallback canonical 서술 1곳(정당)뿐.
- **교훈**: census 판정 계층 패턴에 소문자·"index 탈락"류 변형형 기본 포함할 것 (H1·M2 전부 이 갭 — correctness reviewer 신규 권고).
- **포인터**: draft `_sdd/drafts/2026-07-22_feature_draft_lite_residue_cleanup.md` · F5 계획은 분할 원본 Part 1
