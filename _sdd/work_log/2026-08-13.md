# 2026-08-13

## 00:15 Propagation Surfaces 계약 폐기 미러·전파 (Opus 5)

- **무엇/왜**: 사용자가 `.claude` producer(feature-draft)·reviewer(plan-review-agent)를 직접 편집해 조건부 `Propagation Surfaces` 계약과 status 어휘(`PASS`/`WARN`/`FAIL`/`UNKNOWN`)·`Smell 판정` 반환 행을 제거. codex 미러 반영 요청.
- **결과**: 미러는 3-way가 아니라 **hunk 이식**으로 처리(편집 hunk 6개를 codex에서 유일 매칭 확인 후 치환 — 매칭 1회가 아니면 중단). census 결과 계약이 7개 표면에 더 생존 중이어서 전부 제거: 정의서 한·영(구조 항목 3 삭제 + 재번호 + 스켈레톤), spec-rewrite 템플릿 짝, components.md 2행, main.md §3. 편집이 남긴 dangling `UNKNOWN`(Step 3 "기록하라" vs Step 5 슬롯 부재 + AC3 열거 금지 = 자기모순)은 사용자 확정으로 "근거 부족 시 finding 없음"으로 닫음 → 반환 2항목. main.md v4.12.0 → v4.13.0.
- **검증**: live census 잔존 0, 미러 3쌍 대조 통과(feature-draft byte-identical / plan-review·template 본문 동일, 적응 delta만), 사용자 편집 줄 trailing whitespace 제거로 `git diff --check` 무출력.
- **경로 판정**: 전파류(census 필요)라 원칙상 풀 체인이나, 삭제 방향 + census 전수 열거·grep 검증 완료 + 신규 계약 0이라 경량 경로로 닫음.
- **포인터**: [`../spec/decision_log.md`](../spec/decision_log.md), [`../spec/logs/changelog.md`](../spec/logs/changelog.md)

## 14:47 Verification Weakness 술어 자립 (Opus 5)

- **무엇/왜**: 사용자 질문 — rubric의 "current `feature-draft` producer 계약을 충족하는가"에서 그 계약이 뭔지 문서만으로 알 수 없음. 원인: `current`를 실행시키던 Step 3 `Producer 계약` Read 불릿이 오늘 새벽 Propagation 제거 회차에 함께 삭제되면서, rubric은 계약 충족을 묻는데 읽기 지시는 없고 HR5는 재서술을 금지 → 판정 기준이 모델 기억이 됨.
- **결과**: 술어 자립으로 닫음 — "각 AC가 평가방법과 기대 evidence를 갖고 이진 판정으로 닫히는가? 그 evidence가 재현 가능한 출력 또는 인용 가능한 위치로 적혔는가?"(producer 공통 기준의 판정 축만, 등급 구분·형식은 복제 안 함 — 사용자 후속 지적으로 falsifiability 정의문을 draft에서 직접 확인되는 형태로 내림). HR5 후단("Step 3에서 원본을 확인한다")도 dangling이라 "rubric은 판정 축만 보유, 상세는 producer 단독 소유"로 교체. Step 3 Read 복원안은 기각(방금 줄인 읽기를 되돌림). dangling census 잔존 0, 미러 짝 동일, main.md 몸통 무변경 → bump 없음.
- **포인터**: [`../spec/decision_log.md`](../spec/decision_log.md)

## 14:53 Verification Weakness 술어 정리 (Opus 5)

- **무엇/왜**: 사용자 지적 — `검증이 구체적이고 content anchor를 사용하는가?`가 중복 아닌가. 확인 결과 앞 절만 중복(새 술어의 "재현 가능한 출력"이 흡수)이고, 뒤 절은 새 술어가 잘못 흡수한 축이었다("인용 가능한 위치"가 줄 번호를 연상시켜 anchor 취지와 역행). 더 큰 문제: `content anchor`가 repo 어디에도 정의 없음(producer·정의서에 부재, 이 rubric과 반환 규칙 문장에서만 사용) — 오늘 고친 "current producer 계약"과 같은 부류.
- **결과**: 삭제가 아니라 흡수+정의. 첫 술어에 `재현 가능한 출력 또는 content anchor(줄 번호가 아니라 파일·인용 문자열처럼 변경에 흔들리지 않는 앵커)`로 통합하고 별도 불릿 삭제 → 3불릿 2불릿. 용어를 쓰는 자리에서 정의해 반환 규칙 문장·spec 서술의 명칭 정합 유지. 미러 짝 동일, `git diff --check` 무출력.
- **포인터**: [`../spec/decision_log.md`](../spec/decision_log.md)

## 15:15 plan-review 2-렌즈 철회 미러·전파 (Opus 5)

- **무엇/왜**: 사용자가 `.claude` agent·SKILL에서 실측 ∥ 판단 2-렌즈 분해를 직접 제거(근거: 실측 렌즈가 항상 훨씬 오래 걸려 분할 이득 없음). codex 미러 + 기타 문서 반영 요청.
- **결과**: agent는 hunk 이식 7블록(유일 매칭 확인), codex SKILL은 Runtime Adapter가 codex 고유라 3-way로 6블록 적응(mailbox·target/close 2-spawn 예시 → 1-spawn). 편집이 남긴 dangling 2건(AC1 `호출자 렌즈 한정 시`, 반환 규칙 `렌즈 한정 여부와 무관`) 정리. census로 spec 4표면 추가 발견 — main.md 6곳(§3 분할 축·읽기 상한·Claim Manifest 기록·실행 분리표·게이트 소유권표 + 🚧 Planned 항목 해소), components.md 2행, usage-guide.md 1곳. main.md 읽기 상한 서술엔 어제 제거한 `UNKNOWN`+limitation 잔재도 남아 있어 함께 정정. v4.13.0 → v4.14.0.
- **판정 근거 보존**: 도입 시 밴드(≤220s 지지 / 300s+ 재고)는 표본 1·2가 상반돼 미결이었으나, 이번 철회 근거는 밴드가 아니라 구조적 관측 — 어느 표본에서도 판단 렌즈가 벽시계를 결정한 적 없음(320∥86 / 178∥122 / 114∥72). 재제안 금지로 기록.
- **경로 판정**: 전파류지만 삭제 방향 + census 전수 열거·grep 검증 + 신규 계약 0 → 경량 경로.
- **포인터**: [`../spec/decision_log.md`](../spec/decision_log.md), [`../spec/logs/changelog.md`](../spec/logs/changelog.md)

## 19:37 Principle Link 필드 폐지 · Step 참조 정정 (Opus 5)

- **무엇/왜**: 사용자 질문 "`Affected Plan Surface`·`Principle Link`가 뭐야?"에서 출발. 조사 결과 `Principle Link`는 **정의를 잃은 유령 필드** — `bc26e5c`(rubric 축 재배분)가 rubric 표를 중첩 불릿으로 바꾸며 `Principle Link` 열(smell별 KISS·YAGNI·DRY 등 값)을 삭제했는데 반환 필드 이름만 남아, reviewer가 값 목록 없이 매번 원칙명을 지어내고 있었다. 곁다리로 AC2·AC3의 `Step 5` 참조가 dangling(2-렌즈 철회로 Return이 Step 4로 밀림, claude·codex 양쪽). 사용자 지시로 SDD 풀 체인 없이 직접 수정.
- **결과**: agent 짝 2파일 × 3줄 — 반환 필드에서 `Principle Link` 삭제, `Step 5` → `Step 4` 2곳. `Affected Plan Surface`는 정의 없이도 이름으로 닫히고 Low finding 규정과 맞물려 존치. spec은 `main.md` 본문에 해당 서술이 없어 decision_log supersede 1건 + `Last Updated`만 갱신(본문 무변경이라 version bump·changelog entry 없음 — changelog 규칙).
- **검증**: live 표면 `Principle Link` 잔존 0(변형형 4종), agent 짝 `Step 5` 잔존 0, Step 헤딩 실제 범위 1~4로 AC 참조 일치, 미러 diff는 알려진 적응 delta 2건만, `git diff --check` 무출력. 기록물(`implementation/`·`drafts/`·`work_log/`·changelog) 언급은 append-only라 보존.
- **부수 관측(미수정, 범위 밖)**: `components.md` `plan-review` 행에 2-렌즈 철회 잔재 `(렌즈 한정 시 실측 렌즈 소유)`가 남아 있다.
- **경로 판정**: 신규 계약 0·신규 파일 0·전파 표면 2곳 전수 열거 + grep 검증 → 경량 경로(사용자가 풀 체인 생략을 명시 지시).
- **포인터**: [`../spec/decision_log.md`](../spec/decision_log.md)

## 20:03 하네스 §3 축 전환 spec 전파 (Opus 5)

- **무엇/왜**: 사용자가 하네스 5벌 §3을 직접 재작성 — SDD 적용 판정을 "스펙 영향 예측"(v4.12.0 적용 범위 게이트)에서 **사용자 직간접 요청**으로 되돌리고, 크거나 복잡하거나 스펙 영향이 큰 작업은 **사용자에게 질문**, 그 외 비대상. 경량 경로 문단은 통째 삭제, 단계 순서는 불릿화, "비적용 경로에서도 §2 준수 + 스펙 변경 시 spec-sync 호출 여부 확인" 신설. 편집분 확인 요청을 받아 미러 정합 검사 + spec 전파 수행.
- **확인 결과**: 하네스 5벌 전파 정확 — §3 문단 동일, 잔여 diff는 placeholder 적응 delta 4종뿐, 템플릿 4벌 바이트 동일(`744321c371f6`). 반면 spec 2줄이 stale이었다(`main.md` 107 = 구 게이트 문면, 108 = 삭제된 경량 경로를 current truth로 서술 + 107이 이를 "깊이 선택 축"으로 참조).
- **결과**: `main.md` 불릿 2개 → 1개 + 폐지 하위 불릿로 교체(폐지 규율 손실 표면별 확인 — 브랜치·Execute→Verify·work log는 §2·§5 무조건 소유라 손실 0, spec-sync만 무조건 실행 → 사용자 확인 후 실행). v4.14.0 → v4.15.0, decision_log·changelog entry. 같은 회차의 Principle Link 폐지도 v4.15.0에 합류(단독으론 본문 무변경이었음 — 앞 항목의 "version bump 없음" 서술을 정정).
- **census**: `경량 경로`·`light path`·`적용 범위 게이트` live 잔존 0. 소비 SKILL.md 4벌은 §3 본문 미인용이라 변경 불요, §번호 불변이라 `§0~§N` 리터럴 무영향. `git diff --check` 무출력.
- **포인터**: [`../spec/decision_log.md`](../spec/decision_log.md), [`../spec/logs/changelog.md`](../spec/logs/changelog.md)
