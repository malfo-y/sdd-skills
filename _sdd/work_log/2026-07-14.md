# 2026-07-14 Work Log

## 1/00:34 test-author 코멘트 반영 및 test-free 계약 정렬

- **무엇/왜**: LLM comment bundle이 `.claude/agents/test-author-agent.md`의 "AC당 테스트 1개", "`V*` 1:1", 프레임워크 부재 자산의 무조건 acceptance check 작성 문구를 지적. 이미 진행 중인 RED 게이트 3-way triage(test/structural-check/test-free) 변경과 맞춰 test-author가 테스트 개수를 강제하지 않고 최소 충분한 RED artifact coverage를 반환하도록 조정.
- **결과**: Claude/Codex `test-author-agent` 미러에서 AC당 1개·1:1 강제를 제거하고 AC/`V*` coverage mapping으로 전환. non-falsifiable content는 억지 동어반복 check 대신 `(c) test-free 후보`와 사유를 반환하도록 명시. `implementation` RED 게이트의 남은 1:1 검사 문구도 coverage mapping으로 정렬. 형님이 병행 반영한 `implementation`/`implementation-agent`/`sdd-autopilot` test-free 변경은 보존하고, 겹치는 표면만 최소 수정.
- **포인터**: `.claude/agents/test-author-agent.md`, `.codex/agents/test-author-agent.toml`, `.claude/skills/implementation/SKILL.md`, `.codex/skills/implementation/SKILL.md`
- **요약**: 관련 표면에서 `테스트 1개`·`1:1 대응` 잔여 grep 0건, `git diff --check` 통과, Codex agent TOML parse 통과.

## 2/01:30 agent 파일 방어적 사족 제거 + 구조 재편 (9개 agent × claude/codex 쌍)

- **무엇/왜**: 반복 수정으로 누적된 agent 지시문의 중복("뭐 하면 안된다" 재천명)을 다듬음. 같은 규칙이 AC ↔ Hard Rules ↔ Process step ↔ Final Check 4겹에 재진술되던 구조(예: pr-review "verdict 미판정" 5곳, "단일 작성자" 4곳; feature-draft "Strategic Code Map 재확인" 4곳; implementation-agent test-free 예외 4곳)를 단일 홈으로 재편. 원칙: Hard Rules=규범의 단일 홈(참조는 번호 대신 이름), AC=산출물 형태 체크 + "Hard Rules 준수" 포인터 1개, Final Check=1줄, 실재 유혹을 막는 negative(증거 없는 MET 금지·verdict 미판정·테스트 수정 금지 등)는 각 1회 유지.
- **결과**: `.claude/agents/` 9개 재편(feature-draft·pr-review·plan-review·spec-sync·implementation-review·simplicity-review·test-author·implementation·spec-review; task-ordering·ralph-loop-init는 깨끗해서 제외), 평균 ~12% 단어 감량(감량폭은 작지만 출력 템플릿·rubric은 보존 대상이라 상한이 낮음 — 핵심은 검증 4겹→1겹 구조 정리). plan-review의 Principle Mapping 섹션은 6-smell 표 Principle Link와 중복이라 삭제. `.codex/agents/` TOML 미러 9개도 3-way merge로 전파: old-claude↔codex diff로 codex 적응 카탈로그(boundary 섹션·spawn_agent·`.codex/skills` 경로·도구명 일반화·codex 전용 규칙 삭제/renumbering)를 추출해 새 본문에 재적용.
- **포인터**: `.claude/agents/*.md` 9개, `.codex/agents/*.toml` 9개
- **요약**: 검증 — 11개 TOML parse 통과, codex 본문에 `.claude/skills`·`Pass 1`·`AC1:` 잔존 0건, 내부 "Hard Rule N" 참조 renumbering 정합, `git diff --check` 통과. 외부 결합(스킬은 fix/re-review mode 개념과 `Current Status`/`Iteration History` 출력 섹션만 참조)은 사전 census로 확인 후 보존.

## 3/12:40 토론: feature-draft 산출물 다이어트 (global 3섹션 → task 병합)

- **무엇/왜**: feature-draft Part 2의 `Contract/Invariant Delta and Coverage`·`Touchpoints`·`Validation Plan`을 global로 유지할지 per-task 병합할지 구조화 토론(6라운드). 동기 = 작성 비용 + 읽기 동선. exemplar(2026-07-09 task_ordering draft) 실측: V1~V7↔T1~T7 1:1, 관계 양방향 이중 저장, global 섹션 존재가 재진술을 못 막음.
- **결과**: 방향 B 확정 — 상세의 단일 홈은 task(신설 `Contracts` 필드 + AC 아래 `Validation` 블록), global은 thin index(ID+1줄+Covered By, orphan delta 감사·spec-sync 입력 보존)만. Touchpoints는 공유 census 전용으로 축소, sweep형 cross-cutting 검증은 마지막 Type:Test task로 승격(D1~D6). 실행은 후속 feature-draft로.
- **포인터**: `_sdd/discussion/2026-07-14_discussion_feature_draft_output_diet.md`

## 4/12:57 구현: feature-draft Part 2 "상세는 task, 전역은 index" 재배치 (D1~D6 직접 반영)

- **무엇/왜**: 항목3 토론 결정을 사용자 지시로 직접 구현. repo 규칙(canonical model 변경은 definition 문서 먼저)대로 정의→spec→producer→consumers→codex 미러 순서로 전파.
- **결과**: `docs/SDD_SPEC_DEFINITION.md` §6에 검증 정의 지점의 실행 단위 병치 + task 단위 배치 규칙 선언. `_sdd/spec/main.md` 단일 홈 배치 guardrail 갱신 + decision_log v4.5.9 entry. producer `feature-draft-agent`(claude·codex): 전역 `Validation Plan` 섹션 삭제, task 템플릿에 `Contracts`·`Validation` 신설, coverage 표→thin index(`ID|Type|1줄|Covered By`), Technical Notes 역방향 기록 금지, Touchpoints 공유 census 전용, sweep 검증 task(`Target Files: 없음 (read-only 검증)`) 승격. consumers: `implementation` SKILL(dispatch 슬라이스 주석·Step 1b·3.1), `spec-sync-agent`(Input Sources·Routing), `plan-review-agent`(Verification Weakness 고아 delta·sweep 누락 감사 + DRY Risk 역방향 중복), `task-ordering-agent`(Rule 11 sweep task 마지막 배치), `spec-review-agent`(temporary rubric 이원화), autopilot 표면 3종(orchestrator-contract·sdd-reasoning-reference·sample-orchestrator) — 각 claude·codex 짝. feature-draft codex TOML은 3-way merge로 전파(코드덱스 적응 14건 보존).
- **포인터**: decision_log 2026-07-14 v4.5.9 entry, 커밋 예정 diff (24 files)
- **요약**: 검증 — TOML 11개 파싱 OK, 신규 토큰 5종(thin index·Contracts·없음(read-only 검증)·공유 census·고아 delta) claude/codex hit 동수, 구 구조 잔존(`Validated By`·`Covers C#`·전역 `## Validation Plan` heading) 0, `git diff --check` 통과. `spec-rewrite`/`spec-upgrade`의 temporary spec 포맷 참조는 standalone 장르 정의라 의도적 미변경(§6이 두 형태 구분). 미검증 가정(Open Q1): thin index만으로 orphan delta 감사 동등성은 다음 draft 도그푸딩에서 실측.
