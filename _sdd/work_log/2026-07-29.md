# 2026-07-29

## 1/11:35 work log 작성 강제 — Claude Code 훅(커밋 게이트 + 세션 컨텍스트) 도입

- **무엇/왜**: AGENTS.md §5의 "작업 단위 종료 시 예외 없이 기록"이 산문 규약뿐이라 실제로 누락된다. 훅은 하네스가 실행하는 셸 명령이라 모델 재량으로 건너뛸 수 없다는 점이 유일한 실질 차이 — 이를 이용해 강제 장치를 붙였다. 사용자 선택은 **A+C**(커밋 게이트 + 세션 시작 알림)이고, B(Stop 훅으로 턴 종료 차단)는 "커밋 없는 단계에서 로그가 실제로 샌다"는 실측이 나올 때까지 보류.
- **결과**: 3파일 신규.
  - `.claude/hooks/worklog-gate.sh` — PreToolUse(matcher `Bash`). `git commit` 호출을 판별해 오늘 로그에 미커밋 변경이 없으면 `permissionDecision: "deny"` + 한국어 사유(항목 형식 + 우회법)를 반환한다.
  - `.claude/hooks/worklog-context.sh` — SessionStart. 오늘 로그 경로·존재 여부·마지막 `## ` 헤더를 stdout으로 주입(SessionStart는 exit 0 stdout이 그대로 컨텍스트에 들어간다). 차단 없음.
  - `.claude/settings.json` — 두 훅 등록. **커밋되는 프로젝트 설정**이라 repo 규약으로 공유된다(수신자는 최초 1회 훅 신뢰 승인 필요).
- **핵심 설계 판단 — 판정 기준을 "스테이징 여부"가 아니라 "미커밋 변경 + 세션 마커"로**: 최초 안은 "커밋마다 오늘 로그가 스테이징됐는지" 확인이었으나, 이 repo는 `/git` 스킬이 변경을 **여러 커밋으로 분할**하므로 2번째 커밋부터 전부 오탐으로 막힌다. 실제 채택안은 (1) `git status --porcelain -- _sdd/work_log/<today>.md` 가 비어있지 않으면 통과, (2) 통과 시 `.git/sdd-worklog-ok/<session_id>` 마커를 남겨 같은 세션의 이후 커밋은 무조건 통과. 결과적으로 **세션당 1회, 첫 커밋 직전**에만 발동한다. 마커를 `.git/` 아래 둬서 `.gitignore` 추가가 불필요하다.
- **예외 경로**: `--amend`, `SDD_SKIP_WORKLOG=1` 명시, rebase/merge/cherry-pick 진행 중(`.git/{rebase-merge,rebase-apply,MERGE_HEAD,CHERRY_PICK_HEAD}` 존재)은 새 작업 단위가 아니므로 통과. 판정 불가 상황(jq 부재, git repo 아님)은 **fail-open** — 알림 수준 게이트가 환경 문제로 작업을 막으면 안 된다.
- **검증**: 가짜 payload 주입으로 총 12 케이스 실행, 전부 기대와 일치. 통과 계열 — 비-commit / `git log` / `git status` / `--amend` / `SDD_SKIP_WORKLOG=1` / `grep "git commit" AGENTS.md`. 차단 계열 — `git commit -m` / `git add -A && git commit` / `git -C . commit` / `git -c user.name=x commit` / `GIT_EDITOR=true git commit` / `cd /tmp; git commit`. 별도로 마커 동작(통과 시 생성 → 같은 세션 재커밋 무조건 통과)과 SessionStart 훅 출력을 파일 유무 양쪽으로 확인. 테스트 마커는 정리했다.
- **검증 중 잡은 오탐 2건 (둘 다 수정함)**: 최초 정규식 `git( +-[^ ]+)* +commit`은 (a) **인자 안의 텍스트를 커밋으로 오인**했다 — `grep "git commit" AGENTS.md` 가 차단됐고, 실제로 이 훅을 테스트하던 Bash 호출 자체가 라이브 게이트에 걸려 현재 세션 마커가 생성된 걸로 발각됐다. 명령 경계(`^` 또는 `; & | (` 뒤, 선택적 `VAR=val` 접두 허용)에서 시작하는 `git` 만 보도록 앵커를 걸어 해소. (b) 값을 받는 전역 옵션을 못 넘겨 **`git -C . commit` 이 빠져나갔다** — `-[Cc] <value>` 를 명시적으로 건너뛰도록 보강. 남은 알려진 한계는 `git log --grep commit` 처럼 `commit` 이 옵션의 값으로 오는 형태를 커밋으로 오인하는 것(드물고, 실패 방향이 "차단"이라 눈에 띈다 — 스크립트 주석에 명시).
- **한계 (의도된 것)**: (a) 커밋으로 끝나지 않는 SDD 단계(discussion·plan-review)는 못 잡는다 — B안의 영역. (b) 한 세션에 여러 작업 단위가 있으면 첫 단위만 강제된다. (c) **훅은 Claude Code 전용이라 `.codex/` 레인에는 강제력이 없다** — Codex에는 동등한 메커니즘이 없어 산문 규약만 남는다. 이 비대칭은 수용한 것이지 미해결 버그가 아니다.
- **미처리**: repo-specific 불변 규칙은 AGENTS.md가 아니라 spec Guardrails가 단일 소스(§4)이므로, 이 게이트의 존재는 `_sdd/spec/main.md`에 반영돼야 한다. AGENTS.md §5 직접 편집은 `SDD-HARNESS` 마커 안쪽이라 템플릿 재생성 시 덮일 위험이 있어 하지 않았다. spec 반영·커밋은 사용자 확인 대기.
- **포인터**: `.claude/hooks/worklog-gate.sh` · `.claude/hooks/worklog-context.sh` · `.claude/settings.json` · AGENTS.md §5(강제 대상 규약)

## 2/12:20 훅을 하네스 자산으로 승격 — spec-create/spec-upgrade가 소비 repo에 설치

- **무엇/왜**: 1/의 프로토타입은 샘플 1회 설치에 불과했다. 스킬이 "산문 규약 배포기"가 아니라 **실행되는 하네스 배포기**가 되려면 `spec-create`/`spec-upgrade`가 소비 repo에 훅을 깔아야 한다. 사용자 확정: 기본 설치 + announce(opt-in 아님). 체인 = feature-draft → plan-review → implementation → implementation-review.
- **결과**: 훅 자산을 하네스 4번째 산출물군으로 편입. 정본 `references/hooks/{worklog-gate.sh,worklog-context.sh}` + 4벌 미러(바이트 동일), `spec-create` `#### 3e` 신설 / `spec-upgrade` Step 6 확장(각 claude·codex 2벌 동일), 하네스 템플릿 §5에 게이트 1줄(4벌), dogfooding으로 이 repo 산출물을 정본과 일치.
- **프로토타입 대비 3개 전제 제거**: `jq` 전용 → `jq`→`python3` fallback(둘 다 없으면 fail-open + SessionStart가 "게이트 비활성" 경고 — 조용한 무력화 금지), exec bit 의존 → `bash <path>` 등록(chmod 단계 불필요), 루트 고정 누락 → `cd "${CLAUDE_PROJECT_DIR:-.}"`.
- **plan-review High 3**: (a) PreToolUse `matcher: "Bash"`가 계약·AC에 없었다 — 빠지면 모든 도구 호출에 훅이 붙고 잘못 지정하면 침묵 무력화라, draft가 스스로 세운 "조용한 무력화 금지"를 그 지점에서 깨고 있었다. (b) 전파 표면에 `## Companion Assets` 누락 + census가 pair/orphan 방식이라 "4벌 모두에서 누락"을 원리적으로 못 잡음 → "기존 자산이 등장하는 모든 섹션 = 신규 자산의 최소 등장 표면" 1:1 대조 AC 추가. (c) AC가 line number 앵커라 자기 task 때문에 stale해짐 → content 앵커 + grep 토큰(`references/hooks/`). Medium 5 중 **M4가 실제 버그**였고 변이 테스트로 확정 — `cd` 제거본은 하위 디렉토리 cwd에서 로그를 썼는데도 DENY(오탐 차단), 정본은 ALLOW.
- **implementation-review (correctness ∥ simplicity) → fix 1회**: correctness Blocker 없음·AC 26/26 MET, Medium 3 + Low 5. simplicity Medium 3 + Low 1. 반영한 Medium 6건 —
  - **silent bypass(가장 중요)**: `git commit`은 명령 경계로 엄밀히 판별하면서 예외(`--amend`/`SDD_SKIP_WORKLOG=1`)는 문자열 전체를 훑는 비대칭이라, `git commit -m "note: use --amend later"`와 `echo "SDD_SKIP_WORKLOG=1"; git commit -m x`가 **경고 없이 통과**했다(실측). 이번 feature가 `SDD_SKIP_WORKLOG` 리터럴을 repo 어휘로 편입시키므로 확률이 낮지 않았다. 리뷰어 권고는 "워드 바운드 + 잔여 한계는 주석 명시"였으나, **판별 전에 인용 구간을 sed로 제거**해 실제로 닫았다(인용부호 안은 명령이 아니라 데이터다). 부수 효과로 유일한 false-DENY(`echo "a|git commit b"`)도 해소.
  - 명령 경계 클래스에 백틱·`{` 추가(`` `git commit` ``, `{ git commit; }` 감지). 헤더의 "알려진 한계" 주석이 사실과 반대였던 것도 정정 — `git log --grep commit`은 실제로 정상 ALLOW였고, 진짜 한계는 `sudo`/`bash -c` 중첩 호출·인용된 `-C` 경로·백슬래시 줄바꿈이다.
  - `settings.json` 병합의 **교체 단위 모호**: 멱등 키는 안쪽 `command`인데 계약인 `matcher`는 바깥 그룹 필드라, 안쪽만 교체하는 해석이면 matcher 없이 등록된 과거 설치를 정정하지 못한다(이 repo가 Task 5 직전까지 그 상태였다). "바깥 그룹 객체 전체 교체"로 못박음.
  - deny JSON의 jq/python3 이중 구현 18줄 → 순수 bash `printf` 2줄. **읽는 쪽은 파서가 필수지만 쓰는 쪽은 아니다**(구조 고정, 삽입값은 reason 하나, `"`·`\` 없음). AC2가 증명 대상에서 구조적 성립으로 바뀜.
  - `git rev-parse --git-dir` 성공 후의 빈 문자열 방어 삭제(도달 불가). 같은 모양인 `parse_payload` 쪽은 도달 가능이라 유지.
  - `spec-create` announce 3항목이 3e와 Output Contract에 이중 서술 → 3e는 포인터로 축약(형제 스킬 `spec-upgrade`가 이미 접힌 형태).
- **검증**: 자체 structural check 29/29(통제 PATH로 jq/python3 강제 마스킹, 12케이스 × 2파서 = 24판정). fix 후 회귀 재실행 29/29 유지 + 우회 6종 전부 DENY / 정상 4종 ALLOW 추가 확인. 미러 4벌·2벌 md5 동일, `git diff --check` 무출력.
- **하네스 템플릿 §0 교체 (사용자 판정)**: 템플릿 §0이 사용자 **개인 global CLAUDE.md 5개 산문 지침**을 그대로 배포하고 있었다. 이 repo AGENTS.md의 4개 원칙(`docs/agentic_coding_principle.md` 유래)이 드리프트로 보였으나 실은 반대 — `plan-review-agent`(claude·codex)가 finding의 `Principle Link` 값으로 그 이름들을 인용한다. 5개 지침엔 **`Goal-Driven Execution` 축이 아예 없고**(SDD의 falsifiable AC·RED→GREEN·증거 테이블이 전부 이 축), 라벨이 없어 인용 불가이며, "with me"·"please don't hesitate" 같은 개인 협업 어투가 남의 repo로 배포된다. 사용자 판정 = **4개 원칙 + 2개 조항 흡수**(지침 1의 unattended 조항, 지침 5의 개선 제안 장려). 템플릿 4벌 교체 + 이 repo AGENTS.md를 템플릿에 정합(§2 브랜치 규칙 줄 복원, env.md 불릿 전문화, 헤딩 뒤 빈 줄 정규화).
- **미처리(후속 spec-sync 대상)**: `_sdd/spec/usage-guide.md:30`이 하네스 산출물을 `AGENTS.md`/`CLAUDE.md`/`.gitignore`로만 열거해 훅 자산이 빠져 있고, `docs/SDD_CONCEPT.md:15`가 하네스를 "문서 규약" 레이어로 서술한다(실행 자산 편입으로 후행 갱신 대상). draft Scope에서 명시적 Out.
- **advisory(fix 안 함, Low)**: `session_id`가 파일명으로 무검증 사용(`session_id="../evil"`로 `.git/evil` 생성 실측 — 생산자가 Claude Code UUID라 실전 위험 없음) · `CLAUDE_PROJECT_DIR` 미설정 + 하위 디렉토리 cwd면 false DENY(fail-closed 방향) · `spec-create` Step 3 도입부 "필요 시"가 3e의 "별도 opt-in 아님"과 어조 상충 · `settings.json`이 파싱 불가 JSON일 때의 지시 부재 · 게이트 헤더 주석이 코드 옆 주석과 6/6 중복(5벌 복제 자산이라 1줄 = 5줄 비용).
- **포인터**: `_sdd/drafts/2026-07-29_feature_draft_worklog_hook_harness_asset.md` · `.claude/skills/spec-create/SKILL.md` `#### 3e` · `.claude/skills/spec-upgrade/SKILL.md` Step 6 · `references/hooks/`(4벌) · `references/agents-harness-template.md` §0·§5(4벌) · `AGENTS.md`

## 3/12:35 spec-sync (implemented, evidence 있음) — 훅 자산 편입을 global spec에 반영 + docs 후행 갱신

- **무엇/왜**: 2/의 delta를 global spec에 반영해 체인 종료. draft Part 1 7항목(하네스 산출물 계약 확장 / 정본·미러 규율 / verbatim·키 수준 병합 / 등록 형태 / 조용한 무력화 금지 / Codex 비대칭 / §0 교체) 전부 IMPLEMENTED로 승격.
- **결과**: `main.md` 4.6.12→4.6.13. anti-duplication 준수 — **새 결정 테이블 행을 만들지 않았다.** 하네스 레이어의 canonical은 §3 핵심 설계 산문이라 그 문단을 확장("규약 + 규약을 강제하는 실행 자산")했고, 설치 계약(operating rule)만 Guardrails 새 불릿이 소유한다. 기존 workspace commit 경계 불릿에 겹쳐 싣지 않았다 — 한 불릿이 두 판정 주체가 되는 것을 피했다. `usage-guide.md` Scenario 1에 훅 산출물 + announce, `components.md`에 Codex 비대칭 행 + `references/hooks/` 정본 행, `decision_log.md`·`logs/changelog.md` 신규 entry.
- **§0 교체를 repo-wide invariant로 승격**: 이름 결합이 하네스 템플릿 4벌 · `plan-review-agent` 2벌 · `docs/agentic_coding_principle.md` 세 표면에 걸쳐 Invariant Test를 통과한다. "이름을 바꾸면 `Principle Link` 인용이 끊긴다"를 §3 산문에 고정했다.
- **`docs/SDD_CONCEPT.md` ko·en 짝 갱신**: spec-sync agent의 수정 대상은 `_sdd/spec/`뿐이라 권한 경계 밖이었고 `🚧 Planned`로 남겨졌다. 메인 루프가 이어서 닫았다 — 레이어 표의 `Harness (AGENTS.md)` → `Harness (AGENTS.md + 훅 자산)`, 본문에 "하네스는 문서 규약만이 아니다 — 규약 중 모델 재량으로 흘릴 수 있는 것은 실행 자산이 강제한다" 추가. **ko만 고치면 v4.6.10이 고정한 en 세대 격차가 재발**하므로 짝으로 처리하고 `main.md`의 해당 Planned 불릿을 소거했다.
- **계약 오류 선언 1건 (검증 범위)**: Task 2 AC2("`§0~§5` 리터럴 히트 수 불변")가 sync 직후 18→20으로 깨졌는데, 원인은 이 sync가 §0 변경을 *서술*하며 `decision_log`·`changelog`에 남긴 항목 2건이었다. append-only 이력은 변경을 서술하며 리터럴이 느는 게 정상이므로 AC의 측정 범위에 이력 파일이 들어간 것이 오류였다. **단언은 그대로 두고 범위만 지시 표면으로 좁혔다** — 지시 표면은 HEAD 대비 14→14로 완전 불변임을 파일별 대조로 확인.
- **검증**: 회귀 29/29(범위 정정 후), `git diff --check` 무출력, ko/en 하네스 서술 대칭 확인.
- **미처리**: `usage-guide.md`에 `spec-upgrade` 시나리오가 없어 훅 설치의 upgrade 경로는 문서화되지 않았다(기존 구조상 결번이며 이번 delta가 만든 문제 아님). 시나리오 신설 여부 판정 유보.
- **포인터**: `_sdd/spec/main.md` §2 Guardrails·§3 핵심 설계 · `_sdd/spec/usage-guide.md` Scenario 1 · `_sdd/spec/components.md` · `_sdd/spec/decision_log.md` 2026-07-29 entry · `_sdd/spec/logs/changelog.md` v4.6.13 · `docs/SDD_CONCEPT.md`·`docs/en/SDD_CONCEPT.md` §1 · `_sdd/drafts/_processed_2026-07-29_feature_draft_worklog_hook_harness_asset.md`

## 4/12:34 advisory 2건 반영 — settings.json 파싱 실패 지시 + Step 3 어조 모순

- **무엇/왜**: 3/까지 남겨둔 Low advisory 6건 중 사용자가 2건을 선별 지시. 나머지 4건은 유지 판정(아래).
- **결과 (1) `settings.json` 파싱 불가 시 지시 추가**: 리뷰어는 "`hooks` 키 부재"로 보고 Low를 매겼으나, 실제 위험은 키 부재가 아니라 **파일이 깨진 JSON일 때 모델이 통째로 덮어써 사용자 설정을 날리는 것**이다 — 실패 모드가 데이터 손실이라 값어치가 있다. "파싱 불가면 덮어쓰지 않는다. 훅 등록만 건너뛰고 최종 보고에 명시(스크립트 복사는 진행)"를 `spec-create` 3e와 `spec-upgrade` Step 6 **양쪽**에 넣었다 — 두 스킬이 같은 산출물을 만들어야 하므로 계약 동일성 유지가 조건이다.
- **결과 (2) `spec-create` Step 3 도입부 어조 모순 해소**: "필요 시 아래 파일을 보강한다"인데 그 목록에 훅이 있고 3e는 "opt-in 아님"이라 못박아, 재량으로 읽히면 훅이 안 깔린다. 이 repo의 산출물은 **지시문 자체**라 지시문 내 모순은 기능 결함이다. "단 훅 자산은 재량이 아니다 — 하네스를 생성/병합하면 항상 함께 설치한다(3e)"를 도입부에 붙였다.
- **유지 판정 4건**: (a) `session_id` 파일명 무검증 — 사용자가 범위에서 제외. (b) `CLAUDE_PROJECT_DIR` 미설정 시 false DENY — fail-**closed** 방향이라 조용히 뚫리지 않고, 고치려면 repo 루트를 추측해야 해 코드가 늘고 나빠진다. (c) 게이트 헤더 주석 중복 — 리뷰어도 취향 판정으로 분류했고 "배포 자산이라 소비 repo 독자가 파일 상단만 보고 파악해야 한다"는 반대 근거가 성립. (d) SessionStart 등록 유실 시 경고 소실 — M2(바깥 그룹 객체 전체 교체)로 이미 닫힘.
- **검증**: 회귀 29/29 유지, `파싱 불가` 지시가 2스킬 × 2벌 = 4곳 전수 존재, claude↔codex 2벌 md5 동일, `git diff --check` 무출력.
- **포인터**: `.claude/skills/spec-create/SKILL.md:150`(Step 3 도입부)·`#### 3e` 병합 규칙 · `.claude/skills/spec-upgrade/SKILL.md` Step 6

## 5/18:45 논의 + feature-draft — 하네스 재주입 훅(harness-context.sh)

- **무엇/왜**: compact·clear 후 `CLAUDE.md` 포인터만 재주입되고 하네스 본문은 사라진다. 포인터를 따라 읽을지는 모델 재량이라 실제로 누락된다 — work log 규약이 산문만으로 안 지켜져 커밋 게이트를 만든 것과 **같은 실패 모드**다. 사용자가 훅으로 닫자고 제안, 범위는 "하네스 자산으로 승격"(이 repo 국지 설치 아님)으로 확정.
- **결과 (설계)**: 사용자 표현은 "AGENTS.md를 한 번 읽어두게"였으나 **"읽으라는 지시"가 아니라 "전문 주입"**을 권고해 채택됨. 근거: 지시는 재량이 남아 닫으려는 실패 모드를 재생산하고, 모델이 순순히 읽어도 Read 왕복으로 같은 분량이 들어와 비용 이점도 없다("잘 되면 본전, 안 되면 실패").
- **결과 (실측)**: Claude Code 2.1.220 바이너리를 직접 뜯어 SessionStart의 `matcherMetadata = {fieldToMatch:"source", values:["startup","resume","clear","compact","fork"]}`와 매칭 함수 `BFy`(패턴이 `/^[a-zA-Z0-9_|]+$/`면 `|` split 후 정확 일치)를 확인. 따라서 `"matcher": "clear|compact"`가 유효하다. `startup`은 첫 턴에 읽는 게 정상 동작, `resume`·`fork`는 컨텍스트가 복원되므로 제외 — "컨텍스트가 소실됐는데 포인터만 남는" 상황은 clear·compact 둘뿐이다.
- **결과 (census 선행 실측)**: 세 번째 스크립트가 생기면 `두 스크립트`/`스크립트 2개`/`두 훅` 리터럴이 전부 틀린다. 4벌 SKILL.md에 각 5곳 = 20곳을 draft 작성 전에 떠 놓고 task AC에 반영.
- **plan-review 1패스 — BLOCKED(High 4) 전건 반영**: (H1) census AC가 `_sdd/spec/`·append-only 이력을 범위에 넣어 **도달 불가**였다 — 이 feature가 못 고치는 표면이다. 범위를 소유 표면으로 좁히고 `spec-sync` 인계 목록 산출을 AC로 대체. (H2) matcher가 틀리면 훅은 **무증상 미발동**인데 이를 반증할 AC가 하나도 없었다 — Task 4에 런타임 발동 관찰 AC 추가, 증거 없으면 미충족 보고. (H3) **설계 오류**: Scope Out의 "하네스는 크기가 고정된다" 근거가 거짓 — 소비 repo의 `AGENTS.md`는 마커 블록 + 사용자 내용이라 크기 비고정이고, 이 repo는 우연히 전체가 마커 블록이라 dogfooding으로 절대 안 드러난다. 전문 주입은 유지하되(마커 밖도 그 repo의 작업 규약이다) 거짓 근거를 걷고 가정으로 명시, 마커 밖 문장 픽스처 AC 추가. (H4) read-only census가 마지막이 아니고 뒤에 docs 수정 task가 왔다 — 순서 교체. Medium 2건(JSON 추출 규칙 미정의, RED AC가 `implementation` 스킬 규칙 중복)도 반영.
- **포인터**: `_sdd/drafts/2026-07-29_feature_draft_harness_context_reinjection_hook.md`

## 6/19:20 구현 — harness-context.sh Task 1~6 + 리뷰 게이트 fix 1회

- **무엇/왜**: draft Task 1~6 구현. 정본 `harness-context.sh` + 미러 3 + dogfooding, `spec-create` §3e / `spec-upgrade` Step 6을 "work log 훅 자산" 한정에서 "훅 자산" 일반으로 확장, 개수 리터럴 census 20곳 갱신, docs ko/en 후행.
- **결과 (검증)**: structural check 29/29 PASS. **변이 테스트 4건 전부 검출** — 스크립트를 마커 블록만 출력하도록 변이 / §3e JSON에서 matcher 제거 / settings.json matcher를 `clear,compact`로 변이 / Companion Assets에서 항목 제거.
- **결과 (런타임 관찰, Task 4 AC4)**: 정적 근거만으로는 "matcher가 틀려도 무증상 미발동"을 배제할 수 없어, 픽스처 repo에 훅+settings.json만 심고 헤드리스 `claude` 세션을 굴려 `/clear`·`/compact`를 유발했다. 트랜스크립트에 `hook_success / hookName: "SessionStart:clear"` 와 `"SessionStart:compact"` 양쪽 기록, content에 픽스처 sentinel 포함(= 리드줄만이 아니라 본문 전체가 들어감). 음성 대조로 `startup` 세션 발동 0건도 확인. 리뷰어가 자기 sentinel로 clear leg을 독립 재현했다.
- **실측된 dogfooding 맹점**: 변이 1에서 **AC1은 통과하고 AC4만 실패**했다. 이 repo의 `AGENTS.md`가 통째로 마커 블록이라, 마커 밖 내용을 가진 소비 repo에서만 드러나는 결함을 dogfooding으로는 절대 못 잡는다. plan-review H3이 예측한 그대로이며, 그때 추가한 픽스처 AC가 없었으면 놓쳤을 것이다.
- **리뷰 게이트(2 reviewer 병렬) — Blocker 없음, Medium 7 fix 1회 반영**:
  - correctness H1 — **T6.AC4 인계 목록 NOT MET**. 개수 리터럴 grep 단일 경로로 뜨다 보니 *이름으로 열거하는* 표면(`usage-guide.md:32`, `components.md:78`)과 역할 서술(`main.md:112`)을 통째로 놓쳤고, 반대로 손대면 안 되는 append-only 이력(`decision_log.md:11`)을 목록에 넣었다. 이 repo의 알려진 재발 패턴(단일 grep이 census를 대체)과 같은 모드. **세 경로 합집합 + append-only 분리**로 재산출.
  - correctness M1 — announce가 재주입 발동을 무조건으로 단언해, matcher 미지원 버전의 무증상 미발동을 소비 repo가 알 길이 없었다. spec `main.md:65`의 "강제 자산은 조용히 무력화되지 않는다" 원칙 밖에 새 자산만 놓이는 셈. 버전 의존 + 확인법(`[harness]` 줄) + 주입 분량을 announce에 명시. (근본 대안인 stdin `source` 판정은 JSON 파서 의존을 새로 떠안아 기각.)
  - simplicity M1~M3 — (스크립트, matcher) 대응이 파일당 4~5회 재열거. 인접 canonical의 재진술 3곳 삭제/축약.
  - simplicity M4 — §3e 불릿의 "이 두 source" 선행사가 어긋나 오독됨(직전 호명은 `startup`·`resume`·`fork` 셋). 결론 문장 분리.
  - simplicity M5 = correctness Low — `SDD_CONCEPT.md` 레이어 표 행이 아직 "강제하는"만 말해 8줄 아래 문단과 어긋남. 문단만 고치고 표를 안 본 누락. ko/en 대칭 수정.
- **미반영 advisory(Low)**: (a) 신규 스크립트 파일 모드 644 vs 형제 755 — `bash <path>` 호출이라 기능 무관하나 커밋되면 100644로 굳는다. (b) `[ -f AGENTS.md ]`만 봐서 빈/읽기불가 파일이면 리드줄만 출력. (c) 한 그룹 객체에 두 command가 손편집으로 섞인 형상의 병합 판정 문언. (d) `set -uo pipefail` 실효 없음(형제 일관성으로 유지 권고).
- **포인터**: `_sdd/drafts/2026-07-29_feature_draft_harness_context_reinjection_hook.md` · `.claude/skills/spec-create/SKILL.md` `#### 3e` · `.claude/skills/spec-upgrade/SKILL.md` Step 6 · `.claude/hooks/harness-context.sh` · `docs/SDD_CONCEPT.md`·`docs/en/SDD_CONCEPT.md` §1

## 7/20:10 spec-sync (implemented, evidence 있음) — 재주입 훅을 하네스 실행 자산 계약에 편입

- **무엇/왜**: `_processed_2026-07-29_feature_draft_harness_context_reinjection_hook.md` Part 1 delta 6건을 코드 실측(훅 4벌+dogfooding md5 1종, `settings.json` SessionStart 2그룹, §3e/Step 6 일반화, docs ko/en) 대조 후 전부 IMPLEMENTED로 승격. `🚧 Planned` 신규 등록 0건.
- **결과**: `main.md` 4.6.14 — §2 하네스 설치 불릿 확장(훅 3개 + 역할 열거, 계약이 훅 자산 일반, SessionStart matcher 스크립트별 상이 + 미지원 런타임 무증상 미발동을 announce 조항에 합류), §3 harness 산문에 실행 자산 두 방향(강제 / 사라진 규약 복구) + "지시가 아니라 내용 주입" 근거. `components.md` Platform Notes·Strategic Code Map, `usage-guide.md` Scenario 1. 새 guardrail·결정 테이블 행 0(기존 판정 주체 단일 유지), append-only 이력(`decision_log`·`changelog`의 "스크립트 2개" 문장) 무수정 — 새 entry로만 기록.
- **선행 planned 종결**: v4.6.13의 `docs/SDD_CONCEPT.md` ko·en `🚧 Planned`는 이번 feature Task 5에서 종결(잔여 없음).
- **잔여 Open Question**: `usage-guide.md`에 `spec-upgrade` 시나리오 절이 없어 기존 repo의 훅 자산 업그레이드 경로가 문서화되지 않는다(별도 feature 여부 사용자 확인 필요).
- **포인터**: `_sdd/spec/decision_log.md` 2026-07-29 재주입 entry · `_sdd/spec/logs/changelog.md` v4.6.14

## 8/20:45 feature-draft — review agent 반환 출력 다이어트 + plan-review 입력 상한

- **무엇/왜**: review agent 체감 지연에 대해 사용자가 "lite review agent 신설"을 검토했다. 실측하니 agent 본문은 80~115줄로 작고, 병목은 리포트 **작성(추론)** 이다. lite 신설은 계약을 복제해 claude md ↔ codex toml 짝 propagation 부담(이 repo가 반복해서 앓은 실패 모드)으로 환전하는 셈이라 반대 의견을 냈고, 대신 출력 다이어트 + 입력 상한을 제안해 사용자가 채택했다. model/effort 티어 변경은 사용자가 이미 시도해 효과 없음이 확인돼 제외.
- **사용자 확정 범위**: Medium finding 포맷은 4개 agent 모두 현행 블록 유지(강등 없음), 입력 상한은 plan-review에만, lite agent 신설 금지.
- **작성 중 실측으로 범위 1건 축소**: `pr-review-agent`의 `Correctness 신호`는 제거 후보였으나 orchestrator가 실제 소비한다 — `.claude/skills/pr-review/SKILL.md:94`(반환 기대)와 Output Format `Signals` 줄 `:140`(`AC MET X of N / test pass F%`). 유지 확정. 반면 `implementation-review-agent`의 `Progress Overview`는 relay 목록(`.claude/skills/implementation-review/SKILL.md:37`)에 없고 `Verification ledger`에서 도출 가능해 제거 대상으로 확정.
- **결과**: `_sdd/drafts/2026-07-29_feature_draft_review_agent_output_diet.md` (규모 판정 적격, task 6개 — Progress Overview 제거 / plan-review 판정행 PASS 접기 / plan-review 읽기 계단 / simplicity 판정행 PASS 접기 / wrapper relay 문구 4미러 / census 검증). 신규 불변식 2건: 판정 표는 "점검 대상 전량이 개별 행 또는 PASS 접기 한 줄 중 하나에 귀속", plan-review 읽기는 `Glob`→`Grep`→`Read`→`UNKNOWN` 계단.
- **plan-review High 2 + Medium 2 + Low 2 전량 반영**:
  - **H2가 실질적 설계 오류였다** — 초안은 "Target File 본문 Read 전면 금지"였는데, 리뷰어가 *자기 자신*을 반례로 들었다: 이 draft의 AC들(잔여 반환 항목 5개, rubric 6행 유지, relay 목록 확인)은 본문을 읽어야 판정이 갈린다. 금지는 `Verification Weakness` smell의 판별력을 없앤다. 게다가 agent가 이미 보유한 `Grep`이 계획 어디에도 없어 금지만 있고 대체 수단이 비었다. → 절대 금지를 **도구 계단**(Glob 존재확인 → Grep content anchor → Read는 Grep으로 안 닫힐 때만 → UNKNOWN+limitation)으로 교체.
  - **H1**: "필요한 범위만 읽는다"가 Step 3 외에 Input 절(`plan-review-agent.md:38`)에도 있어, Step 3만 고치면 상한 전/후 규칙이 한 파일에 공존한다. census 패턴(`6행`/`5행`)으로는 안 잡힌다 → Input 절 축약 AC + `필요한 범위만 읽는다` 0회 census 추가.
  - **M2**: Error Handling 표의 "supporting context 부족" 행이 같은 규칙을 이중 서술 → Step 3 포인터로 축약(규칙 소유 1곳).
  - **M1**: census AC가 "교체하기로 한 지점의 잔존 0"이라는 자기참조였다 → 절대 기준선으로 교체(변경 전 `6행`/`5행` 12줄, `Progress Overview` 2줄, `필요한 범위만 읽는다` 2줄 → 각 0건).
  - Low 2: Task 1 조건부 AC4 삭제(AC1·AC2에 흡수), Task 5에 implementation-review relay 문구 판정 AC 추가.
- **포인터**: `_sdd/drafts/2026-07-29_feature_draft_review_agent_output_diet.md` · 브랜치 `refactor/review-agent-output-diet`

## 9/23:40 implementation — review agent 반환 출력 다이어트 + plan-review 읽기 계단

- **무엇/왜**: draft `2026-07-29_feature_draft_review_agent_output_diet.md` Task 1~6 실행. 브랜치 `refactor/review-agent-output-diet`.
- **결과**: 자산 10개 변경 — agent 3종(claude md + codex toml), wrapper SKILL 4벌.
  - T1 `implementation-review-agent`: 반환에서 `Progress Overview` 삭제(항목 6→5).
  - T2/T4 `plan-review-agent`·`simplicity-review-agent`: 판정행을 "문제 있는 것만 개별 행 + `PASS: <이름 나열>` 한 줄 접기"로 교체. AC1은 "6개(5개) 전부가 둘 중 **정확히 하나**에 귀속"으로 재작성 — 점검 의무는 유지, 출력 의무만 완화.
  - T3 `plan-review-agent`: 읽기 규칙을 `Glob`→`Grep`→`Read`→`UNKNOWN` 도구 계단 + 정지 규칙으로 교체하고, 같은 규칙을 중복 서술하던 Input 절·Error Handling 행을 Step 3 포인터로 축약(규칙 소유 1곳).
  - T5 wrapper relay 문구 4벌: `smell 6행 판정`→`smell 판정`, `차원 5행 판정`→`차원 판정`.
- **Task 5 AC6 판정**: `implementation-review` SKILL의 relay 문구 `simplicity: 5개 차원 판정`(claude `:38` / codex `:65`)은 Task 4 이후 형식(개별 행 + PASS 접기로 5개 전량 귀속)과 의미가 일치하므로 **미변경으로 판정**한다.
- **검증**: 프레임워크가 없어 structural-check 스크립트로 RED→GREEN(초기 28 FAIL → 최종 52 PASS / 0 FAIL). census 3종 전부 0건(`6행`/`5행` 12→0, `Progress Overview` 2→0, `필요한 범위만 읽는다` 2→0). 미러 3쌍의 변경 payload diff 동일, codex 적응 delta(`Codex Agent Boundary`·`spawn_agent`·gpt 모델 허용값) 보존.
- **계약 오류 선언 2회 — 둘 다 check 버그이지 draft 계약 문제 아님**: (a) 마크다운 표 구분선이 `|---`로 시작해 `^| ` 카운트 기대값이 틀렸다(8→7, 6→5). (b) `awk -v`가 `\+`를 escape 처리해 `+` 수량자로 만들어 `Step 4: Classify + Return` 앵커가 빈 섹션을 반환, **위장 FAIL**을 냈다. 중단·분할 규칙 2를 점검했으나 AC 문면은 두 번 다 유효해 해당 없음.
- **implementation-review(correctness) Medium 4 → fix 4**: 리뷰어가 변이 테스트 12건으로 **내 check 스크립트의 위장 PASS 3건**을 잡았다 — (M2) 계단 2단계를 통째로 지워도 3단계 산문의 `Grep` 문자열이 토큰 검사를 만족시켜 통과, (M1) 정지 규칙 문장을 지워도 `닫히지 않`이 3단계 산문에 있어 통과, (M4) codex 분기가 `tools` 리터럴 부재만 봐 AC6과 인과가 없었다. 보강 후 재변이 3건 전부 검출 확인. (M3)는 위의 Task 5 AC6 판정 기록 누락으로, 이 항목이 그 fix다.
- **포인터**: draft `_sdd/drafts/2026-07-29_feature_draft_review_agent_output_diet.md` · check `scratchpad/check_output_diet.sh`

## 10/23:50 implementation 마감 — simplicity 게이트 반영 + 계약 조정

- **simplicity-review Medium 2 → fix 2**:
  - **완전성 불변식 3중 서술** — 출력 다이어트가 목적인 변경이 스스로 같은 명제를 3번 적었다: AC1 positive("6개 전부가 … 정확히 하나에 귀속") + AC1 대우("어느 쪽에도 없는 smell은 점검 누락이다") + Step 6 말미("6개 smell 전부가 둘 중 하나에 귀속돼야 한다"). Step 6의 `나머지는`이 이미 분할을 완결하므로 뒤 문장은 함의이고, 대우는 positive의 재천명이다. → **소유자를 AC1 절 1곳으로** 두고 나머지 2회 삭제(plan-review·simplicity-review 각 claude/codex 2벌).
  - **Input 절 죽은 문장** — `plan-review-agent:38`에 남긴 "읽는 규칙은 Step 3이 소유한다"는 실행 지시가 아니라 저작 시점 소유권 메모다. Step 3은 제목부터 `Read Supporting Context`인 필수 단계이고 Error Handling 행이 이미 가리킨다. 삭제해도 agent 경로 불변(draft Task 3 AC3이 "축약 **또는 삭제**"를 허용).
- **계약 오류 선언 3회차 (이번엔 진짜 계약 오류)**: draft Task 2 AC1 / Task 4 AC1이 "완전성 불변식이 AC 절과 반환 형식 절 **양쪽** 문면에 있어야" 한다고 세운 가정이 틀렸다 — 이 repo 규범은 판정 주체 1곳이다. draft AC를 "Step 6에는 재서술하지 않는다"로 뒤집고, check을 '부재가 PASS'로 반전한 뒤 RED(10건) → fix → GREEN을 다시 관찰했다.
- **최종 검증**: structural-check 66 passed / 0 failed. census 5종 전부 0건(`6행`·`5행`·`Progress Overview`·`필요한 범위만 읽는다`·`어느 쪽에도 없는`). 미러 3쌍 payload 동일, codex TOML 3종 `tomllib` 파싱 성공 + 키 3종 보존, `git diff --check` clean.
- **잔존 advisory (fix 안 함)**: correctness L1(T5 relay 검사가 `Blocker Status` 1개만 확인) · L2(Step 3 정지 규칙의 "판정" 단위 미명시 → smell별임을 덧붙이면 명확) · L3(Input 절 축약으로 spec guardrail 출처 열거가 Step 3 3단계 괄호로만 남음) · L4(AC1 줄 단독으로는 PASS 접기의 이름 열거를 강제하지 않음). simplicity L: Error Handling 괄호가 Hard Rule 5 재서술, 차원 판정을 완전 1줄로 더 합칠 여지.
- **포인터**: 위 `## 9/23:40` 항목 · draft `_sdd/drafts/2026-07-29_feature_draft_review_agent_output_diet.md`
