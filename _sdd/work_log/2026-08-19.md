# Work Log — 2026-08-19

## 1/12:43 Codex simplicity dispatch schema blocker 진단 (GPT-5)

- 무엇/왜: 플러그인 설치 후 `implementation` 마감 품질 게이트에서 simplicity reviewer가 `agent_type` 미지원으로 spawn되지 않는 현상을 diagnose-only로 조사했다.
- 결과: 설치 캐시와 repo의 Codex `implementation-review`는 byte-identical이며, 둘 다 mailbox `spawn_agent`에 `agent_type: "explorer"`를 필수로 요구한다. 현재 활성 schema는 `task_name`·`fork_turns`·`message`·선택적 `model`·`reasoning_effort`만 제공하므로 계약의 fail-closed 분기가 정확히 발동한다. 같은 전제가 `pr-review`·`investigate`·`write-phased`에도 있어 공통 runtime adapter drift로 분류했다. 제품·spec 수정은 하지 않았다.
- 포인터: `plugins/sdd-skills-codex/skills/implementation-review/SKILL.md:17`, `plugins/sdd-skills-codex/skills/pr-review/SKILL.md`, `plugins/sdd-skills-codex/skills/investigate/SKILL.md`, `plugins/sdd-skills-codex/skills/write-phased/SKILL.md`

## 2/12:57 Codex generic sub-agent schema 호환 수정 (GPT-5)

- 무엇/왜: 사용자 승인에 따라 `agent_type` 미지원 schema에서도 필수 simplicity gate와 optional helper가 정상 dispatch되도록 공통 Codex Runtime Adapter drift를 수정했다.
- 결과: `agent_type`을 선택적 role selector로 분리하고 미지원 시 framed `message` 계약을 주입한 범용 sub-agent를 사용하도록 Codex 스킬 4개와 PR 예시를 갱신했다. plugin 1.0.1·global spec v4.26.0으로 동기화했다. mandatory payload `agent_type` 0건, nonblocker 계약 4/4, legacy native-explorer 문구 0건, Claude diff 0, plugin validator·`investigate`/`write-phased` quick validator·`git diff --check` PASS. review 스킬 quick validator 2개는 기존 `argument-hint` 허용목록 불일치로 미통과해 plugin validator와 구조 census로 보완했다.
- 포인터: `plugins/sdd-skills-codex/.codex-plugin/plugin.json`, `plugins/sdd-skills-codex/skills/{implementation-review,pr-review,investigate,write-phased}/SKILL.md`, `_sdd/spec/main.md`, `_sdd/spec/components.md`, `_sdd/spec/decision_log.md`
